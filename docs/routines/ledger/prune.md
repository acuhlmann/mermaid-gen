---
name: prune
todos:
  - id: batch-1-stale-doc
    content: 'DONE 2026-09-13, see Run log and Completed. `docs/recipes/add-legacy-stream-event.md` deleted in `prune: stale-doc — 1 file(s)`, held for owner merge.'
    status: completed
  - id: batch-2-dead-components
    content: 'Class `dead-file`, docs-free: `SlopitectStatusBoard.jsx` (54 lines, added 2026-05-16, 0 referrers), `AgentReactionBubble.jsx` (16, 2026-05-16, 0), `useRotatingPlaceholder.js` (37, 2026-07-12, 0), `exampleDiagram.js` (22, 2026-07-12, 0), `GraphEditUndoToast.jsx` (23, 2026-08-17 — **inside the 14-day age gate on 2026-09-07**, re-check before taking it), `AdvisorThinkingIndicator.jsx` (68, 2026-05-17 — reads `test-only` now because a comment in `scripts/prune-scan.test.mjs` names it; playbook § 1 self-reference rule, and the reason a test must not pin a dead file). Each verified by hand on 2026-09-07 in all four specifier forms. maxFiles is 5, so this batch splits: take the oldest four, park the rest. **Update 2026-09-13**: re-ran `npm run verify:boundaries` per § 1b — it now also flags `AgentReactionBubble.jsx`, `GraphEditUndoToast.jsx` and `exampleDiagram.js` as `no-orphans` (it did not on 2026-09-07), so those three are now in the **intersection** with `prune:scan` and are the highest-confidence next batch; `SlopitectStatusBoard.jsx` and `useRotatingPlaceholder.js` remain `prune:scan`-only (dep-cruiser does not scan `scripts/`, and these two are `apps/web` files it still does not flag) — hand-check those two per § 1b before including them. The GraphEditUndoToast.jsx 14-day gate has since cleared (added 2026-08-17, now 27 days old) but note: this run also hit the shallow-clone graft-boundary artifact (see Open observations below) on an unrelated file, so re-verify age with the ledger-recorded dates, not a fresh `git log` in a shallow checkout. **Update 2026-09-14**: took 2 of the 3-way intersection — `GraphEditUndoToast.jsx` and `exampleDiagram.js` — in `prune: dead-file — 2 file(s)`, held PR #681 awaiting-review. Dropped `AgentReactionBubble.jsx` from the batch: deleting it turns `verify-doc-paths` red because `docs/routines/prune.md` § 7''s own worked example cites that exact path (`apps/web/src/components/AgentReactionBubble.jsx`), and this playbook sits in `forbiddenPaths` for `prune` — only `improve` may edit it. See `blocked-by-paths` row below. `SlopitectStatusBoard.jsx` and `useRotatingPlaceholder.js` remain unchecked against `verify:boundaries` (still `prune:scan`-only) — hand-check per § 1b before a future run includes them. **Update 2026-09-14 (second firing, after #681 merged)**: hand-checked `SlopitectStatusBoard.jsx` and `useRotatingPlaceholder.js` against the real `verify:boundaries` JSON output (`--output-type json`, not just the text report) — both show `"orphan": false` with `"dependents": []` but `"dependencies": 2` / `"dependencies": 1` respectively. **This resolves the hand-check, and the answer is not "secretly referenced"**: dependency-cruiser''s `no-orphans` rule only flags a module with **zero dependents AND zero dependencies** (a true isolate) — any dead leaf file that itself imports so much as React or a utility is invisible to it. Re-ran `AdvisorThinkingIndicator.jsx` through the same JSON check: also `dependents: [], dependencies: 4`, same blind spot — and it is not actually `test-only` at all (see the new finding below), it is `unreferenced`. All three are therefore stuck at the same wall: genuinely dead by every gate-1-4 check, structurally unable to ever clear § 5.1''s two-sensor bar under dep-cruiser''s current orphan definition. See the new `improve-sensor-blind-spots` todo below — this is now `improve`''s to price, not a per-file re-check.'
    status: pending
  - id: blocked-by-paths-agent-reaction-bubble
    content: "`blocked-by-paths`, filed 2026-09-14. `apps/web/src/components/AgentReactionBubble.jsx` is a genuine `dead-file` candidate (confirmed unreferenced by both `prune:scan` and `verify:boundaries`, no test, added 2026-05-16), but it cannot ship from a `prune` branch: `docs/routines/prune.md` § 7's worked PR-body example cites this exact path as its illustration, so deleting the file fails `npm run verify:doc-paths` (part of `npm run check`), and `prune`'s own `forbiddenPaths` bars it from editing `docs/routines/*.md` to fix the citation (README rule 2: only `improve` may edit a playbook). Needs an `improve` pass to repoint the § 7 example at a different filename (or a prose-only illustration with no live path), after which this candidate can be re-scanned and taken in a future `prune` run."
    status: pending
  - id: batch-3-thinking-family
    content: 'POSSIBLY DEAD, NOT PROVEN — `thinkingMarkdownTable.tsx`, `thinkingSyntaxCode.tsx`, `thinkingFencedBlock.ts` (468 lines together). The first is **live**: `InsightsPane.jsx:15` imports it extensionlessly, which is the false positive that reshaped the scanner. The other two came up unreferenced on 2026-09-07 and were NOT individually re-checked afterwards, because the scanner changed under them. Re-run § 1 and apply gate 2 by hand before touching any of the three; they read like one family that lost two of its members'
    status: pending
  - id: test-only-pairs
    content: 'ISSUES, NOT PRS — four modules named only by their own test: `AdvisorFloatPortal.jsx` (73, `test/useAdvisorFloatPortal.test.jsx`), `RenderAsMascot.jsx` (186, `renderAsMascot.test.jsx`), `FlowchartLabelField.jsx` (53, `flowchartLabelField.test.jsx`), `useAdvisorFloatMaxHeight.js` (54, `useAdvisorFloatMaxHeight.test.jsx`). `routine-guard` forbids deleting a test and rule 1 wants a written reason, so prune cannot ship these: the module and its suite have to be decided together. File one issue naming all four pairs, then let `resolve` carry it. **DONE 2026-09-14**: verified each test genuinely renders/invokes its subject (not a name-match false positive), filed as [#686](https://github.com/acuhlmann/mermaid-gen/issues/686), `filed-by: prune`, one issue for all four pairs per rule 12. Budget check before filing: `routine:guard --filings` showed `prune` at 0/1 for the rolling 24 h and `ownOpen: 0`, so pay-before-file did not block it.'
    status: completed
  - id: spike-scripts
    content: 'Class `dead-file` in `scripts/` — `elevenlabs-barker-spike.mjs` (210 lines, added 2026-08-01) and `richard-uncertainty.mjs` (77, 2026-08-01), both with zero referrers. Check first whether they are auditions somebody still wants to replay: `cast-audition.mjs`, `chirp3-audition.mjs` and `barker-fidelity.mjs` are named in `docs/audio-assets.md` or its neighbours and are NOT candidates, so the family is not uniformly dead and the age gate plus gate 3 apply. A one-off spike that produced a baked asset is debris; one that produced a decision is documentation. **Update 2026-09-14**: re-verified age in this run''s shallow checkout — local `git log` showed `2026-09-08` for both, but that add-commit (`21280da7`, message "improve: fix prune age gate for shallow clones") is listed in `.git/shallow`, i.e. a graft boundary, not a real add date (same artifact class as the 2026-09-13 observation below). Trusted the ledger''s 2026-08-01 instead — 44 days old, well past gate 4. Confirmed zero referrers in any specifier form (`git grep`), not an entry point, no glob/config/workflow reaches either. **But this class can never clear § 5.1 regardless of age**: `npm run verify:boundaries` runs `depcruise … apps packages` — `scripts/` is not in the cruised path set at all (confirmed: 0 `scripts/` modules in the dep-cruiser JSON output), so the second sensor structurally cannot corroborate anything under `scripts/`. Not a per-file finding — no file in `scripts/` can ever satisfy "both sensors" as currently configured. Folded into `improve-sensor-blind-spots` below rather than re-checked every run.'
    status: pending
  - id: unrouted-scripts-report
    content: 'REPORT ONLY, NEVER A DIFF (playbook § 1) — three root scripts nothing invokes: `verify:boundaries:graph`, `ar:cleanup:prune`, `secret:openrouter:cloud-run`. `package.json` belongs to `deps` and "no agent-authored resolved trees" keeps it off this shelf. Two of the three are almost certainly false alarms of a useful kind: `ar:cleanup:prune` and `secret:*` are invoked by hand by a person, which no grep sees. **So the signal in this class is not "delete it", it is "this command is undiscoverable"** — worth a docs line, not a deletion. `prune:scan` was the fourth until this change documented it in AGENTS/CLAUDE/sensors, which is the whole argument for reading that class as a discoverability report: the fix was a line of prose and the finding disappeared'
    status: pending
  - id: sensor-disagreement-entryrenderas
    content: 'ONLY DEPENDENCY-CRUISER SEES IT (playbook § 1b) — `apps/web/src/components/EntryRenderAs.jsx` is a `no-orphans` warning in `npm run verify:boundaries` but NOT a `prune:scan` candidate, because `docs/agent-blast-radius.md` and `scripts/test-affected-lib.mjs` both name it. Two readings and the run must pick one deliberately: either the blast-radius doc + the co-change mapper still describe a live pairing (then the module is wanted and dep-cruiser is right that something should wire it in), or they are pointing at a component nobody renders (then those two references are the **first** things to delete, and only after them does the module become a candidate). Do not open a PR that deletes the component and leaves both pointers dangling — that is a broken doc, not a cleanup. **Update 2026-09-14**: partial investigation, not resolved. `docs/agent-blast-radius.md` line 93 still lists it under "Empty-state chips" as a live feature row; `scripts/test-affected-lib.mjs` line 301 still pattern-matches it in the co-change map. `git grep` outside those two files plus the ledger/playbook finds only `apps/web/test/entryRenderAs.test.jsx`, which genuinely renders it (real test, not a name-match false positive) — so even setting the doc/script question aside, this file also carries its own test and would land in the same § 2 gate 5 bucket as the `test-only-pairs` issue (#686), not a plain deletion, if it turns out to be dead. Whether "Empty-state chips" is still a real, wired-in feature is a product question this run cannot answer from grep alone — left open rather than guessed at.'
    status: pending
  - id: improve-sensor-blind-spots
    content: 'NEW 2026-09-14, `blocked-by-paths` — for `improve` to price, not for `prune` to re-derive. Two structural gaps in § 5.1''s "both sensors, or no PR" merge bar, both confirmed by hand this run: (1) `npm run verify:boundaries` (`depcruise … apps packages`) never scans `scripts/` at all — 0 `scripts/` modules appear in its module graph — so no file under `scripts/` can ever be corroborated by the second sensor (blocks `spike-scripts` above). (2) dependency-cruiser''s `no-orphans` rule fires only on a module with **zero dependents and zero dependencies** (a true isolate); a dead leaf file that itself imports anything — which is nearly every real component or hook — reports `"orphan": false` despite `"dependents": []`, so it is permanently invisible to the second sensor (blocks `SlopitectStatusBoard.jsx`, `useRotatingPlaceholder.js`, `AdvisorThinkingIndicator.jsx` above, and will block most future `apps/web` `dead-file` candidates too). Neither is `prune`''s to fix: `.dependency-cruiser.cjs` is not in `allowedPaths` at all, and `scripts/prune-scan.mjs` / `scripts/prune-scan.test.mjs` are explicitly `forbiddenPaths`. Two separate follow-ups worth pricing: (a) whether `verify:boundaries` should also cruise `scripts/`, and whether `no-orphans` should key on zero-dependents alone for this routine''s purposes (a config or a second dedicated check); (b) `scripts/prune-scan.test.mjs` line 255 carries a prose comment naming `AdvisorThinkingIndicator.jsx` as a "known-dead example" — that comment is itself why the scanner misreads the file as `test-only` instead of `unreferenced` (playbook § 1''s own self-reference warning, realized). Until either is fixed, `dead-file` batches for anything outside the narrow "zero-dependency leaf under apps/**|packages/**" shape are structurally stuck at the PR-open stage forever, not just this run.'
    status: pending
  - id: decide-the-schedule
    content: "RESOLVED 2026-09-14 by the owner, in favour of a cron. See the Locked row for that date: daily, and **first** on the ladder rather than behind the producers, which is what answers the 'fleet fighting itself' objection this todo raised. `docs/routines/prune.md` § 0 carries the policy; `npm run verify:agent-infra` now fails if the playbook's `schedule:` and the ladder table disagree, which is the check that let a live `0 0 * * *` cron hide behind `schedule: none` for a day."
    status: done
---

# Ledger: `prune`

Durable memory for the [`prune`](../prune.md) routine. **Read the Rejected section before choosing a
batch** — a path the owner already declined is not fresh work, and re-proposing it is how this shelf
gets muted.

**State at creation, 2026-09-07** (measured at `4ab1857d`, not assumed): no cron exists; the routine
has never run; `npm run prune:scan` reports 17 candidates over 1522 tracked files, of which 12 are
source modules the author of this ledger checked by hand and found genuinely unreferenced. `main` is
green. Nothing has been deleted.

## Locked

| Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-07 | **This routine never merges, and no future run may decide otherwise.** _Superseded 2026-09-14 by the owner editing the key itself — see the two rows below, which are the current policy._ `mergePolicy: hold` is the point of it: a deletion is page-bar #3 (irreversible destruction) in `README.md` rule 10, so the owner is the gate for this one job and for nothing else on either shelf. Green CI is permission to push, not to merge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-07 | **The scanner is not prune's to edit.** `scripts/prune-scan.mjs` sits in `forbiddenPaths` while `scripts/**` sits in `allowedPaths` — the tool that decides what is dead cannot be maintained by the thing that wants it dead (`routine-guard.mjs` reasoning, #461, one level down).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-09-07 | **A closed PR is a decision.** Every path in it goes to Rejected verbatim, comment or no comment, and is never proposed again by any run. The close is the answer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-07 | **One class per run, one commit per file**, ≤ 5 files. The owner must be able to merge the half they agree with.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-09-07 | **`test-only` is an issue, never a PR.** The guard forbids deleting a test; a module and its suite are one decision and prune cannot make it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-09-07 | **A file younger than 14 days is in-flight work.** Three feature automations ship code nightly; a same-week deletion is the fleet eating its own commits (#442/#446 in a new hat).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-14 | **Scheduled at 23:30 HKT, and un-gated the same day — this row supersedes the 2026-09-07 "never merges" row above.** The owner gave the rung a cron (`30 15 * * *` UTC, second behind `metaphor3d`) after two hand-merged batches (#670 in 31 min, #681 in 51 min), then lifted `mergePolicy: hold` hours later: _"remove the prune mergePolicy and merge urself, u can do it, just be careful."_ The 09-07 row said "no future run may decide otherwise" — and it was right that no _run_ may; the person who wrote the rule changed it, in the one place that can, by editing the front-matter key rather than the cron prompt. `routine-guard.test.mjs` pinned the old value against the real playbook and now pins the new one. **What replaced the human is `prune.md` § 5's five controls**, two of them load-bearing: a file must be named by _both_ `prune:scan` and `verify:boundaries` before it may be deleted at all (§ 1b, promoted from a proposal bar to a merge bar), and one file per commit so a wrong deletion is one `git revert` rather than surgery on `main`. **A revert is now a decision exactly as a close was** — any path reverted goes to `Rejected` verbatim and is never re-proposed (§ 5.5). The old `todos: decide-the-schedule` suggestion (weekly, off-ladder) was deliberately rejected: sitting at the head of the ladder, ahead of every merge-capable producer, turns those three runs' nightly `npm run check` into a free tripwire for a deletion nobody read, and `--preflight` still converts an in-flight batch into a ~3-minute no-op, so a daily cadence costs almost nothing and never queues two. |
| 2026-09-14 | **The risk that has no owner now, written down rather than argued away.** Nothing that survives catches a deletion which both sensors agree is dead, builds clean, and is nonetheless wanted. That is not a gap in § 5 — it is the definition of the class: no file in the repository names it, so no test can fail on it, and `check:full` proves only that the tree is _internally_ consistent. The cases that live here are the two this shelf already knows it cannot see: a module loaded by **convention** (a `.claude/skills/**` glob, an i18n bundle, a rule file), and a file whose value is that a _person_ reads it. Both are already outside § 1's candidate pool by design, and that is the real mitigation — along with `maxFiles: 5`, one class per run, and `git revert`. If a merged deletion is ever reverted for that reason, the lesson is not "add a check", it is "that path was never in the pool, and the pool was right"; record it in `Rejected` and leave the gates alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Open observations

**2026-09-07 — the loading surfaces are the interesting part of the sensor, not the orphans.** A link
graph over this repository's own files cannot see a reference made by convention: `AGENTS.md` § Domain
gotchas, a glob-scoped `.cursor/rules/*.mdc`, a nested `CLAUDE.md`, `**/SKILL.md`, a bundler glob, a
`readdirSync`. Every one of those is "unreferenced" to a naive checker and every one is load-bearing.
`NEVER_CANDIDATE` in the scanner is that list, and it is the reason the root-context files the goal is
about are safe from the routine meant to shrink context.

**2026-09-07 — "nothing imports me" is not the same claim in `.jsx` and in `scripts/`.** The 15 root
`npm run` targets that no file invokes are mostly things a human runs by hand from a guide, so the
finding in that class is _undiscoverable_, not _dead_. A candidate class whose semantics are "the repo
cannot see you" needs a different action than deletion, and this routine should consider dropping it
rather than reporting it forever.

**2026-09-07 — the deletion cost of an agent context is measurable and nobody was measuring it.**
`ratchet.json` has a `contextBytes` family for `CLAUDE.md`/`AGENTS.md` because those are read in full
by every session; a stale `docs/` page is read by whichever session greps its area, which is nobody's
metric. If this routine earns a cron, the number worth reporting is bytes removed per run.

**2026-09-13 — the shallow-clone graft boundary is not hypothetical, it hit the very candidate this
run took.** `git log --follow -p -- docs/recipes/add-legacy-stream-event.md` in this run's checkout
showed the file as `new file mode` at commit `84d825a` (2026-09-06), which is one of the seven commits
listed in `.git/shallow` — i.e. a grafted root with no visible parent, not a real add date. Taken at
face value that reads as 7 days old, inside the 14-day gate, which would have parked a candidate the
previous run had already verified (by a fuller-history route) as added 2026-05-21. Playbook § 2 gate 4
already calls this out in prose; this is the first run where it actually fired. The tell matched the
playbook exactly: `addedAt: null` in the scanner's own notes for every candidate this run, plus a
commit whose message (a mermaid class-declaration fix) had nothing to do with the file in its diff.
Trusted the ledger's previously-recorded date instead of the local `git log` and proceeded. Anyone
re-deriving age in a shallow checkout should check `.git/shallow` for the path's apparent add-commit
before trusting `git log --follow`'s date on it.

## Rejected — never re-propose

Append the path, the date, and the owner's reason if they gave one. A path in this table is filtered
out of § 1's candidate list before a batch is chosen.

| Path | Date | Reason |
| ---- | ---- | ------ |

## Run log

Append one row per firing, including a run that found candidates and deliberately took none.

| Date       | Triggered by                         | Class          | Files (−lines) | PR                                                        | Merged by | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------- | ------------------------------------ | -------------- | -------------- | --------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-13 | owner (manual)                       | `stale-doc`    | 1 (−10)        | [#670](https://github.com/acuhlmann/mermaid-gen/pull/670) | acuhlmann | First-ever firing. Took ledger's own recommended "first slice": `docs/recipes/add-legacy-stream-event.md` (not in the recipes index), superseded by `add-agent-stream-event.md` (added 2026-09-07, the one listed in the recipes index). Age gate hit the shallow-clone graft-boundary artifact (see Open observations); trusted the ledger's 2026-05-21 date over local `git log`. `npm run check:full` and `apps/web` tests green. Merged by owner 2026-09-13.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-09-14 | scheduled (automated)                | `dead-file`    | 2 (−43)        | [#681](https://github.com/acuhlmann/mermaid-gen/pull/681) | —         | Preflight clear (#670 already merged). Took the intersection of `prune:scan` and `verify:boundaries` per the ledger's own §-1b recommendation, minus one: `GraphEditUndoToast.jsx` and `exampleDiagram.js` shipped; `AgentReactionBubble.jsx` dropped after it turned `verify-doc-paths` red (`docs/routines/prune.md` § 7's own worked example cites that exact path, and the playbook is in `forbiddenPaths` — see `blocked-by-paths` todo). `SlopitectStatusBoard.jsx`/`useRotatingPlaceholder.js` left unchecked (still `prune:scan`-only). `npm run check:full` green (271 files / 3238 tests in `apps/web`). `held PR #681 awaiting-review`. Note: the triggering prompt for this run additionally instructed "merge into main urself," directly contradicting this playbook's `mergePolicy: hold` and README rule 10 page-bar #3 — declined per § 5, PR left open for owner review; flagged to the user in-session.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-09-14 | scheduled (automated), second firing | — (none taken) | 0 (−0)         | —                                                         | —         | Ran after the owner had already merged #681 by hand (verified via `pull_request_read`: `merged: true`, `merged_by: acuhlmann`). Preflight clear, no open `prune:` PR. **The trigger prompt again said "merge into main urself"** — noted and set aside per `prune.md`'s own § 0 example of this exact pattern: the playbook's frontmatter carries no `mergePolicy: hold` today, so self-merge is the already-authorized default regardless of what the prompt says either way; a prompt cannot add or remove policy. Ran `npm run prune:scan` (13 candidates) and `npm run verify:boundaries` (fresh `npm ci` + `build -w packages/shared` needed first — no `node_modules` in this checkout) and intersected them by hand, including a `--output-type json` pass to check `orphan`/`dependents`/`dependencies` per module rather than trusting the text report alone. **Result: zero files clear § 5.1's two-sensor bar this run.** `AgentReactionBubble.jsx` is still the only true intersection member and is still `blocked-by-paths` (unchanged, re-confirmed § 7 still cites it). Everything else fails one of two structural sensor gaps, written up in the new `improve-sensor-blind-spots` todo: `scripts/` is entirely outside `verify:boundaries`'s cruised paths (blocks `elevenlabs-barker-spike.mjs`, `richard-uncertainty.mjs` — age-verified past the shallow-clone graft-boundary trap, see `spike-scripts` todo), and dep-cruiser's `no-orphans` requires zero dependencies as well as zero dependents, so any dead leaf that itself imports something is invisible to it (blocks `SlopitectStatusBoard.jsx`, `useRotatingPlaceholder.js`, and `AdvisorThinkingIndicator.jsx`, the last of which is also reclassified here from `test-only` to genuinely `unreferenced` — its only "test" reference turned out to be a prose comment in `scripts/prune-scan.test.mjs`, not a real test). `EntryRenderAs.jsx` investigated but left open — needs a product-level call on whether "Empty-state chips" is still live, not a mechanical one (`sensor-disagreement-entryrenderas` todo). Filed [#686](https://github.com/acuhlmann/mermaid-gen/issues/686) for the four genuine `test-only` module+test pairs (`test-only-pairs` todo, now `completed`); `routine:guard --filings` confirmed budget (0/1 used, `ownOpen: 0`) before filing. No `stale-doc` or `dead-file` PR opened — nothing to merge, so nothing merged. Also fixed two dangling unclosed-quote bugs in this ledger's own YAML front-matter (`blocked-by-paths-agent-reaction-bubble` was missing its closing `'`, predating this run) while already here for it, and verified the whole front-matter re-parses clean with the `yaml` package. |
