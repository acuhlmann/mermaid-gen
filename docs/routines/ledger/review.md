---
name: review
todos:
  - id: first-run
    content: 'First supervised run — confirm the 24h window resolves, both review axes report, the five benches run key-free, and the PR self-merges'
    status: completed
  - id: label-bootstrap
    content: 'Create the four missing triage labels (needs-triage, needs-info, ready-for-agent, ready-for-human) on first use — only wontfix exists today'
    status: completed
  - id: bench-variance
    content: 'Collect bench latency p95 across ~10 runs before deciding whether latency belongs in the ratchet'
    status: pending
---

# Ledger: `review`

Durable memory for the [`review`](../review.md) routine. Read the run log before filing anything —
a finding already reported is not a new finding.

## Locked

Owner decisions this routine must not re-litigate. Add a dated row rather than arguing with one.

| Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-20 | `review` **may fix one bug per run**, and a fix ships only with a test that fails without it — written first, observed red. A bug it cannot make a test fail for is filed, not fixed.                                                                                                                                                                                                                                                                        |
| 2026-08-20 | It opens a PR and **merges it itself** once CI is green. The PR is your reference, not a gate.                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-20 | Bench snapshots are **not** committed by this routine. `apps/server/bench-results/` is on the don't-touch list and a daily snapshot would bury the meaningful baselines.                                                                                                                                                                                                                                                                                     |
| 2026-08-20 | Latency is informational. Only an **expectation** drift — a case whose accept/reject flipped — earns an issue.                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-21 | `issue_write`'s `labels` param **auto-creates** a missing label on first use (verified: `ready-for-agent` didn't exist, issue #348 created with it, label now exists). No separate bootstrap action needed — `needs-triage`/`needs-info`/`ready-for-human` will create themselves the first time a finding actually needs one.                                                                                                                               |
| 2026-08-21 | A pre-existing (not-from-this-window) doc/script bug found while executing the playbook's own steps (e.g. a broken bench command) is still in scope — "your own machinery is fair game" per this run's fire payload, and the review step that surfaces it (Benches) is itself part of the last-24h diff (PR #337 created `review.md`). Filed, not fixed (out of `allowedPaths` — `docs/routines/review.md` and `CLAUDE.md` are not in `review`'s allowlist). |

## Open observations

Things seen but not yet worth an issue. Promote to an issue when a second run confirms, or drop
when three runs pass without recurrence.

- **2026-09-04 — #516's `suite.cases` move is attributed to a cause that produced 4 of its 13.**
  `ratchet.json` went 4223 → 4236 explained as growth "from the two new reverse-direction guard
  tests added in #515". Re-measured with the ratchet's own `TEST_CASE_RE` per rev: `c74b1cd`
  **4230**, `11d6fec` (#514) **4232**, `014450d` (#515) **4236** — 7 was pre-existing unclaimed
  drift, 2 came from #514, 4 from #515, and the parameterised loop counts as **one** static
  `test(` rather than two. Tightening only, nothing loosened, so no rule is broken; the written
  reason simply does not match the number it justifies. Recorded rather than filed, matching the
  precedent of this ledger's own 4169/4170 note — a tracker that fills with arithmetic about
  budgets nobody's build reads is the speculation §3's confidence bar exists to keep out.
- **2026-09-04 — #519's shoulder push applies to orbiting accents, whose geometry its rationale
  does not describe.** `makeNodes` excludes only `substrate` and `path` roles, so an `accent` —
  which `nodePosition` already places `site.radius + 1.4 +` outward, i.e. well past the shoulder —
  gets the same outward `site.radius * 0.6`. The PR argues the change entirely in terms of
  landmarks standing _on_ an island. The measured sweep covers accents (the toaster's
  `was-it-my-fault` is one) and reports no regression, and this run's #526 cap now bounds the worst
  of it, so this is a rationale that does not cover its own blast radius rather than a defect.
  Watching, not filing.
- **2026-08-21 — `officeLogStore.js` and `officeWorkingMemoryStore.js` duplicate the day-reconcile
  shape.** Both add a `loadedDay` var + `reconcileOfficeDay()` doing the same "clear on rollover"
  logic independently (Standards axis, Fowler "Duplicated Code"). Not fixed here — one bug per run,
  and this is a style judgement call, not the bug. Could move into `officeAmbienceStorage.js` if a
  future `improve` slice wants it.
- **2026-08-21 — `officeAmbienceStorage.js`'s `dayStampOf` is now a dead one-line wrapper** around
  the newly-exported `officeDayStamp`, with 5 call sites left unrenamed (Standards axis, Fowler
  "Middle Man"). Cosmetic.
- **2026-08-21 — `graphEditIdFromDescriptor` maps any `node_N`-shaped id to `~node:N` unconditionally**,
  not scoped to mindmap. Spec axis flagged it as a latent edge case (a flowchart node literally named
  `node_5`), not a reproduced bug — grep confirms flowchart/state edit code never emits `node_N`-shaped
  ids today. Watching, not filing.
- **2026-08-21 — a second known CI flake matched the "your own machinery" invitation.**
  `apps/server/test/anythingRuntimeBrowser.test.js` → "a browser that hangs on startup does not
  reject a valid page" went red under `npm run check` and again under `npm run precommit`
  (full-suite contention), green both times in isolation. This is the flake `improve` documented in
  `sensors.md` this same morning and already filed as issue #347 — not a regression from this run's
  fix, not duplicated here.
- **2026-08-26 — `metaphorFlatKindEdit.js`'s `isFamilySource` field is built into every one of the
  11 kinds' returned objects but never read anywhere** (`flatGraphEditAdapter` doesn't consult it;
  `metaphorFlatGraphEditForSource` sniffs `doc.metaphor` against the registry directly instead) —
  Standards axis, Fowler "Speculative Generality." Single unused object property, invisible to
  tooling (not a variable, so `no-unused-vars` can't see it). Matches the precedent of the
  2026-08-21 `dayStampOf` finding: cosmetic, recorded here rather than filed.
- **2026-08-26 — `connectNodes` in the same file is a same-signature passthrough over `connect`**
  before export (Fowler "Middle Man," flagged as a nit by the reviewing agent itself) — but it
  mirrors the repo's existing `connect{Family}Nodes` naming convention used by the city/garden
  adapters in `canvasGraphEdit.js`, so this reads as intentional consistency rather than a defect.
  Watching, not filing.
- **2026-08-27 — `mermaidSourceLocate.js` now carries two independently-defined sequence-arrow
  grammars**: the new `SEQUENCE_MSG_LINE_RE` (broad arrow-character class, used only to index
  messages for `messageId`-based lookup) and the pre-existing per-call `arrowRe` (narrow arrow-type
  allowlist, used only for the legacy label-based fallback path) — Standards axis, Fowler
  "Duplicated Code" / latent "Shotgun Surgery." Traced both call paths: they are self-consistent
  independently (messageId lookup never consults `arrowRe` and vice versa), and no arrow shape in
  today's supported grammar makes the two disagree, so this is a forward maintenance risk rather
  than a reproduced bug — same class as the 2026-08-21 `graphEditIdFromDescriptor` finding
  ("latent edge case... not a reproduced bug... watching, not filing"). Watching, not filing.
- **2026-08-27 — `metaphorCompositeEdit.js`'s `layerDelegate` is a fourth kind-dispatch switch**
  (tree/city/garden/flat-kinds) returning the identical 6-key mutator shape from four different
  import sources, alongside the pre-existing `graphEditAdapterFor` dispatcher in
  `canvasGraphEdit.js` — Standards axis, Fowler "Repeated Switches." Not a CLAUDE.md violation (the
  family-table convention already accepts one mutator+adapter row per kind) and adding a fifth base
  kind means touching both regardless of shape; cosmetic, recorded here rather than filed.
- **2026-08-27 — `metaphorCompositeEdit.js`'s `ensureGloballyUniqueId` mutates `item.id` in place**
  on the parsed doc rather than returning a new value, which its name doesn't signal — Standards
  axis, Fowler "Mysterious Name." Harmless (call sites rely on the mutation), single-instance,
  cosmetic. Recorded here rather than filed.
- **2026-08-30 — this routine appears to have skipped its 2026-08-29 scheduled firing.** The last
  `review` row before this one is 2026-08-28 (PR #433), yet `resolve` (#434, #443) and `improve`
  (#440) both ran and merged on 2026-08-29 with no `review` row in between — `preflight` found no
  stale open PR blocking a firing, so this isn't the "one branch at a time" stop from README §5.
  This run widened its window back to the last commit `review` actually covered (`38092b4`) rather
  than the literal last-24-hours command, to avoid leaving `9fcf704..8c058d7` permanently
  unreviewed. Not something this routine can diagnose from inside a run (it has no visibility into
  why its own cron didn't fire); recording it here so a human — or a future run that notices the
  same gap twice — knows to check the schedule/trigger rather than assume every day gets covered.

- **2026-08-30 (2) — `subwayNetworkLayout.js`'s `bounds.radius` loop reads the unscaled
  `subwayPlatformRadius(station.traffic)` where every other consumer now reads the station's drawn
  `station.platformRadius`** (interchange x1.25 / ordinary x0.8) — flagged independently by both
  the Standards and Spec axes, which is why it is recorded rather than dropped. Traced to a
  verdict: it is **unreachable**, not latent-but-possible. The plate's `+0.9` margin absorbs the
  shortfall until the unscaled radius exceeds 3.6, which needs a station traffic sum above ~438,
  and `MetaphorItemSchema` caps `traffic` at 20 (`packages/shared/src/metaphorSchema.ts`) — so
  it would take 22+ items interchanged onto one station. A regression test was written and
  **passed against the unfixed code**, which is the "test that examines nothing" shape the playbook
  warns about, so it was reverted rather than committed green. Real inconsistency, no possible
  wrong render; belongs to a cleanup slice, not a bug fix.
- **2026-08-30 (2) — `MachineScene.jsx:107` converts only half a coordinate.**
  `position={[0, axle.placard[1], axle.placard[2] - axle.center[2]]}` converts z out of scene space
  but hard-codes x to `0`, relying on `machineGearLayout.js:151` always setting
  `placard[0] === center[0]`. The comment above it (`:104`) also describes the opposite of what the
  code does — it says the placard "is applied outside this bed's group", but the `<ItemLabel>` is
  inside that group. Latent: no observable difference today, so no test can go red on behaviour.
  Same class as the 2026-08-26 `isFamilySource` finding.
- **2026-08-30 (2) — `docs/agents/ratchet.json`'s `$comment` disagrees with the numbers it
  justifies.** It reads "Root pair 223 KB -> 74 KB"; measured at `4778443` the pair is
  39,582 + 42,497 = 82,079 bytes, which is what #453's PR body says (82 KB). Separately #454 moved
  `suite.cases` 4149 -> 4155 without touching the `$comment`, which still reads "suite cases
  4122 -> 4143". The `$comment` is the ratchet's only place for a written reason, so both are
  inaccuracies in the artifact that exists to carry them. Out of `review`'s `allowedPaths`; too
  small to file.
- **2026-08-30 (2) — `routine-guard.mjs`'s `branchPrefix` contradicts its own docstring.**
  `routinePrMatchers`' comment says generated names like `claude/eager-hopper-74jcfu` "say nothing
  about who opened a PR", yet the playbooks pin exactly those observed one-time names as branch
  prefixes and `routine-guard.test.mjs` locks the coincidence in as a contract. The next generated
  name silently stops matching; the title prefix is the only real matcher. Harmless today (the
  title prefix carries the check), so watching rather than filing.
- **2026-08-30 (2) — `metaphorCityEdit.js`'s `findCityLink` is a fourth spelling of the same
  directed-link predicate** (`hasDirectedLink` in `metaphorFlatItemsCore.js`, `deleteCityEdge:152`,
  `metaphorFlatKindEdit.js:108`), and `isInterchange` is computed in three places
  (`subwayNetworkLayout.js:284`, `SubwayScene.jsx:217` and `:228`) now that `platformRadius` moved
  onto the station row but the predicate deriving it did not. Both Fowler "Duplicated Code",
  both cosmetic; candidates for an `improve` slice.

- **2026-08-31 — three evidence-accuracy nits across the routine shelf, recorded rather than filed.**
  Each is real and each is worth less than the tracker space, per the playbook's confidence bar;
  they are noted in #475's closing section so a run that touches those files sees them.
  (a) #470's PR body and the `canvas-graph-edit` ledger's "Tests before → after" proof-of-work
  column both say `12 → 15`; measured on `bb8de9f` the file had **13** `it` blocks, so it was
  13 → 15 (two new cases plus one assertion added to an existing case). (b) `docs/agents/ratchet.json`'s
  `$comment` and `docs/routines/ledger/improve.md` both say the suite budget was tightened to its
  "measured 394/**4169**", against a committed `suite.cases.budget` of **4170** — raised in the
  #456 merge, which explained the arithmetic in its commit message but corrected neither doc.
  `verify:ratchet` is green on this tree regardless (measured 395 / 4186), so nothing is broken;
  the written reason simply does not match the number it justifies, and `$comment` is that file's
  only field for one. (c) `docs/routines/ledger/resolve.md`'s run log has a stray second
  `| --- |` separator row inside the table, which GFM renders as a data row of dashes — same
  breakage class as #441, two entries above it in the same file.
- **2026-08-31 — the domain finding from this run's fix could not be written where rule 8 says it
  belongs.** `docs/agents/domains/metaphor3d.md` is outside `review`'s `allowedPaths` (only
  `docs/routines/ledger/review.md`, `apps/**`, `packages/**`), so the troika-`onSync`-is-a-one-shot
  finding lives in `metaphorLabelDepth.js`'s own docblock instead — at the code, which is rule 8's
  actual goal, but not in the file every metaphor session auto-loads. Worth a decision if it
  recurs: either widen `review`'s paths to the domain files, or accept that a `review` fix records
  its finding at the code and lets the next `metaphor3d` firing lift it into the domain doc.

- **2026-09-01 — #488's PR body claims a blast radius the diff contradicts.** The body says
  "**Stations themselves never move** — they are what the labels, glyphs, markers and hover anchors
  are placed from — so the change is confined to the drawn channel", but `routePoint` went from
  `site.anchor[1] + 0.16` to `fusedSurfaceHeightAt(x, z, sites) + CHANNEL_RIDE` for every
  non-`bridge` layer. A station sits at `0.52 * radius` from centre, so `dome = smoothstep(0.48)`
  ~= 0.47 and each station drops ~53% of its island's height; `anchor = [x, point[1] + 0.9, z]`
  moves with it. The move is deliberate and tested — the PR's own new case is called "stands a
  station on the ground rather than on its site crest" — and `routeAlongSurface`'s docstring scopes
  the same sentence correctly ("The stations themselves do not move" is true _of that function_).
  So this is an inaccurate PR body rather than a defect, and it makes the Evidence section's
  framing (+13% subject pixels) and declutter (70 -> 69) deltas not attributable to
  `routeAlongSurface` alone. Recorded, not filed — same class as the 2026-08-26 `isFamilySource`
  finding.
- **2026-09-01 — `FusedCompositeScene.jsx` resolves the ocean/plaza colour with a copy of
  `WorldGround`'s own expression.** `hasIslands ? (theme.waterColor ?? '#27afe2') : (theme.groundColor
?? '#1e293b')` now exists in both files (Fowler "Duplicated Code"), because `FusedPath` needs the
  backdrop its ink is contrasted against and has no way to ask the primitive for it. The diff's own
  comment says as much ("Same expression as the disc's own"), and the two literals are the drift
  risk rather than the branch. One shared exported helper would close it. Cosmetic; recorded rather
  than filed.
- **2026-09-01 — `bootstrapServer.js`/`loadServerApp.js` had no test importing either.** The Spec
  axis flagged that nothing in `apps/server/test` referenced `src/index.js` before or after #493's
  split, so `npm run check` could not see a startup-ordering regression at all — which is why the
  200-vs-503 bug (#497) shipped green. Partly closed by this run's `startupHealth.test.js`, but that
  covers the health contract only: the ordering itself (socket bound before the dynamic import,
  `runtimeReady` flipped after `attachRoutes` resolves) is still unexercised.

- **2026-09-02 — `deleteCompositeEdge` is now the duplicated twin of a helper its own file already
  imports.** #508 pointed `renameCompositeEdge` at the shared `renameLinkedEdge`
  (`metaphorFlatItemsCore.js`), which was the right call, and left `deleteCompositeEdge`
  (`metaphorCompositeEdit.js:313`) as a hand-rolled copy of `deleteLinkedEdge` — same filter, same
  `before`-length check, same `fail('missing')` — while `serializeCompositeDoc` (`:83`) stays
  byte-identical to `serializeMetaphorFlatDoc`. Standards axis, Fowler "Duplicated Code"; zero risk
  today, but the divergence is now one-sided and the delete twin is the one that will drift.
  Overlaps #476's framing (the automation's `allowedPaths`, not the design, produces the
  duplication) so not filed separately — promote if a third copy appears.
- **2026-09-02 — #505 added a second complexity warning to `subwayNetworkLayout.js`**
  (`subwayRouteSign` at 18 > 12; the file's own `subwayNetworkLayout` went 22 → 23). `apps/web`
  measures 835 against a budget of 836, so nothing is breached and no issue was filed — but the
  cost lands in `improve`'s queue with no trace in #505's body, which is otherwise exhaustive about
  measurements. Same file family as #465 and #447. Worth an issue only if it pushes `apps/web` over.
- **2026-09-04 (2) — `labelLines` splits on `\n` only, while the same file documents that troika also
  wraps at `MAX_LABEL_EM`.** #534's whole thesis is that an unreported line under-claims the declutter
  box and leaves the extra line hanging _down_, onto the thing being named. A wrapped line is
  indistinguishable from a `\n` one on screen and is invisible to both `labelPlateEm`'s new
  `LINE_EM * lines.length` height and to `labelStackLiftEm` (`lift` comes out 0). The schema permits a
  120-character label (`packages/shared/src/metaphorSchema.ts` line 165) against `maxWidth={size * 16}`, so
  it is reachable — but no drawn label in the three composite fixtures exceeds 16 em today, so this is
  the half of the abstraction the fix left leaking rather than a live defect. Not filed: no
  reproduction. Promote if a wrapped label shows up in a fixture or a probe.
- **2026-09-04 (2) — #537 left its own run-log PR cell at `TBD`** (`docs/automations/ledger/canvas-graph-edit.md:27`),
  and its body claims three todo statuses were updated where the diff touches two (`gantt-journey` was
  already `blocked`). #538 backfilled exactly this with a second commit on the same PR, so the
  convention exists. Also: #536's body quotes `routine:guard --reachable` for
  `metaphorCompositeEdit.js` as "`canvas-graph-edit` (plus `deps, resolve, review`)"; measured today it
  is `deps, improve, resolve, review, canvas-graph-edit, metaphor3d` — `metaphor3d`'s
  `apps/web/src/utils/metaphor*` glob has covered it since 2026-09-03, so the file is co-owned by two
  feature automations and the quote understates exactly the one-fleet-per-window collision the label
  implies. Not filed — the ledger is `canvas-graph-edit`'s to write (README rule 2) and none of it
  changes what shipped. Promote if a second run repeats the `TBD`.
- **2026-09-05 — `--filings`' REST fallback asks for `per_page=200`; GitHub clamps it to 100, silently.**
  `scripts/routine-guard.mjs` line 693. The `gh` route asks `--limit 200` and gets 200; `gh` is absent in the
  cloud sandbox (README rule 9), so the REST route is the one every real run takes. Measured today: the
  call returns **100** issues, lowest number **#456**, while the oldest open finding is **#460** — four
  issues from the edge. Nothing is mis-measured yet (`summarizeBacklog` reported 10 open, and the
  eleventh is #452, excluded by `isLogIssue` rather than by the clamp), but the two routes already
  disagree about the window, and when the boundary passes #460 the backlog size, `oldest`, and the
  `pay-before-file` debt all quietly shrink. Same class as the module's own `=== 'open'` note, one
  parameter over; the fix is `per_page=100` plus `page=2`, or a `Link`-header loop. **Not filed:**
  `--reachable scripts/routine-guard.mjs` prints `frozen`, so a `ready-for-agent` label would be rule
  11's false promise and `needs-triage` on a frozen path is the owner's file, which rule 10 does not put
  on the page bar. Recorded here so `digest` can carry it.
- **2026-09-05 — the picked link's casing is depth-tested while its core is not** (#550,
  `MetaphorSceneChrome.jsx:620-644`). `LINK_PICK_CASING_OPACITY`'s own docstring says the halo "carries
  the read across a tower it crosses", but only the core gets `depthTest={!picked}` /
  `renderOrder={PICKED_LINK_ORDER}`; the 12.6 px casing keeps `renderOrder={-1}` and the material default
  `depthTest: true`. On a city — the case both comments cite — the core floats over the tower and its
  halo is clipped by it, so the picked link pinches wherever it crosses geometry: the artifact the
  depth-free rung was added to prevent, half-applied. Not filed (cosmetic, and no fixture renders it);
  the fix is the same two props on the casing at `PICKED_LINK_ORDER - 1`. Promote if a probe shows it.
- **2026-09-05 — #523's wedge stamp reaches every render path, not just the canvas.**
  `stampPieSliceHitTargets` was wired into `renderMermaidSvg` (`renderMermaidPreview.js:32,52`) and
  `renderMermaidPreviewSvg` is a thin wrapper over it, so `exportDiagram.js:429,437`,
  `InsightsEmbeddedDiagram.jsx:101` and `ExampleDiagramPreview.jsx:35` all now emit synthetic
  `id="diagram-0-node-N"` wrappers and `<title>` hover tooltips into exported SVG/PNG. The stated goal
  was `resolveTargetUnder`'s `closest('g.node')` on the live canvas; an export gaining node ids is
  behaviour nobody asked for. Harmless as shipped (this run's #556 fix makes those ids _correct_, which
  is arguably an improvement to the export too), so not filed — but it is a Spec-axis miss worth a
  sentence if a third render path picks it up.
- **2026-09-05 — four record defects, none of them changing what shipped, all of the same class: a
  comment or a body asserting what the code beside it does not do.** (a) `c24ac87` — the commit message,
  `routine-guard.mjs:869` and `routine-guard.test.mjs:1136` all say #531's glob pointed into
  `apps/web/src/hooks/`, "a directory that has never existed"; it exists with 30+ files, CLAUDE.md routes
  new hooks there, and `office-life.md:28` globs it two hours later — the real #531 defect was two
  nonexistent _files_ inside a real directory. The sensor works; the narrative baked into its source is
  wrong, in a commit whose subject is voiding a filing built on unchecked facts. (b) #541 — the pair
  claimed "for both script and style" is the script case twice: `anythingPolicyLint.test.ts:139`'s
  `unclosedStyle` closes its `<style>` and puts the URL in a closed `<script>`, so the `<style>`-side
  `|$` widening has no negative case. (c) #554 — "still respecting string literals" has no pin; all three
  new tests exercise comments only. (d) #475 — `routine-guard.test.mjs:1159` says "Neither playbook below
  is `digest`" three lines above `postflightOkMessage({ name: 'digest', … })`. Not filed individually:
  rule 12 forbids minting a number per occurrence of a class, and none is a live defect. Promote as one
  standing issue if a fifth appears.

- **2026-09-06 — the proof-of-work miscount class reached its own promotion bar and is one filing short.**
  The 2026-09-05 entry above lists four instances and says "promote as one standing issue if a fifth
  appears." Three more landed this window: #577's body says `metaphorCompositeEdit.test.js` went
  "22 -> 28" (measured **21 -> 28**, and the "fails 2 of the 6" negative-control sentence inherits the
  miscount); #582's verification table says `officeFloorWander.test.jsx` gained **2** cases (three `it`
  blocks were added — its "Red without the fix: 2" column is right, the third case is green either
  way); and commit `9e67b03`'s message says the planner drops "1295 -> 1175" where the PR body and the
  measurement both say **1217**. That is seven, past the bar. **Not filed: `maxIssues` spent** — #587
  and #588 were the higher-value picks (a live placement defect and a guide that misleads every
  graph-edit session). `blocked-by-budget`; first filing of the next run unless something worse lands.
- **2026-09-06 — #584's handoff row is wrong about the size of the queue it exists to hand off.**
  `docs/automations/ledger/metaphor3d.md`'s `complexity-queue-547` todo says it "took 9 of #547's 17
  complexity rows (#569 #570 #571 #580 #581 #583). 8 remain." Rows actually taken: 8 — #581's own PR
  body opens "**Not a #547 row** — this repairs a lint-budget breach I introduced myself." The row's
  own enumeration then lists 10 functions, and ESLint measures **13** `complexity` warnings across
  those files, silently omitting `MetaphorRenderer.jsx`'s `CityBuilding` (18), `LayerSlab` (14) and
  `MetaphorBaseScene` (15). Low impact but not zero: this row _is_ the next `metaphor3d` firing's work
  queue, and #584's stated purpose is that the list not be re-derived from scratch. Outside `review`'s
  `allowedPaths`; `metaphor3d` owns it and will re-derive on its next firing.
- **2026-09-06 — #561's "pinned by a new case covering both halves" covers one half twice.**
  `useDeskActions.test.jsx:110` calls `result.current.imSomeone(...)` twice, untagged and
  `'talk'` — both the **inbound** half. The outbound half is `pushOfficeImReply({ …, channel: 'talk' })`
  at `OfficeLayer.jsx:1484`; nothing references `handleTalkReply` or that call, so deleting the
  `channel: 'talk'` argument there leaves the suite green. The #552 fix itself is correct; only its
  claimed coverage is not. One assertion on the outbound push would close it.
- **2026-09-06 — `renameEdge` and `deleteEdge` now disagree about how a parallel-edge match is passed.**
  `useFlowchartGraphEdit.js:280` calls `adapter.deleteEdge(current, from, to, matchLabel, matchIndex)`
  (positional) while `:379` calls `adapter.renameEdge(current, from, to, label, { edgeLabel, edgeIndex })`
  (object). #402 grouped the clump for the three `rename*Edge` adapters only; `deleteFlowchartEdge`,
  `deleteStateEdge`, `deleteSequenceEdge` and `deleteClassEdge` keep the positionals. Both params are
  optional everywhere, so passing the wrong shape **silently picks the wrong parallel edge** rather than
  throwing — the exact hazard the object param was introduced to remove, now living one hunk from its own
  fix. Not filed (no live defect — every call site is currently correct); the fix is to convert the four
  `delete*Edge` signatures in one pass. Promote if a fifth adapter arrives, or if anyone edits that hook.
- **2026-09-06 — `class Animal hot` declares a second, unaddressable class, and the comment defending
  the regex describes flowchart syntax.** While fixing the `:::` bug this run, measured against the
  pinned mermaid: in a real `classDiagram`, `class Animal hot` renders **two** boxes — `classId-Animal-0`
  and `classId-Animalhot-1` — so it is not "the styling form which assigns a class to nodes rather than
  declaring one" (that is flowchart syntax, unreachable behind `isClassFamilySource`). `Animalhot` is a
  rendered box no mutator can address: a smaller instance of the bug fixed this run. The wrong
  parenthetical was removed from `mermaidClassEdit.js` as part of that fix; the **test** at
  `mermaidClassEdit.test.js:274` still carries the second half of the error — its comment claims "the
  styling line that refers to it is rewritten with it rather than orphaned" while its only assertion is
  `expect(renamed.source).toMatch(/class Pet/)`, and the `class Animal hot` line is in fact left
  untouched. Left alone deliberately: the playbook forbids widening a fix beyond its bug, and correcting
  the comment without deciding what `Animalhot` should do would just move the wrong claim.
- **2026-09-06 — `outwardReach` lost the coalesce the paragraph below it argues for.**
  `fusedCompositePlanner.js:484` is now `Math.max(0.85, site.radius * 0.6)` (was `(site.radius ?? 1) * 0.6`).
  `Math.max(0.85, NaN)` is `NaN`, so a site without a radius yields a NaN label offset where the old form
  floored at 0.85. Unreachable today — `makeSites` always routes through `siteRadiusFor`, and #527 was
  right that `?? 1` was dead — but the comment three lines below defends the `0.85` floor as
  "belt-and-braces … so lowering that clamp someday cannot silently park labels back on their nodes",
  and removing the coalesce converts that same future edit from "label parks on its node" into "label
  position is NaN". Cosmetic unless the clamp changes; recorded so the next editor of that line knows.
- **2026-09-06 — the `fusedWorldSurface.js` split (#500) duplicated `clamp` and `finite`, with a
  different `clamp`.** Both helpers now exist in `fusedWorldSurface.js:15-21` and in
  `fusedCompositePlanner.js`, and the new `clamp` is a _different_ implementation
  (`value < min ? min : …` vs `Math.max(min, Math.min(max, value))`) — they differ on NaN. Fowler
  Duplicated Code; the split's own rationale is "no closure over planner state", which a shared numeric
  leaf would not violate. Cosmetic, recorded rather than filed.
- **2026-09-06 — #574's body files its follow-up against a PR number, not the issue.** It says a bare
  `class Duck` was "filed as #573 with the evidence"; **#573 is a pull request** (the closed duplicate of
  #572's Docker-Hub retry). The issue is #575, which #576 closed. Commit `bab1660` gets it right by
  naming no number. Adjacent, same window: **#572 and #573 are two PRs from the same Cursor automation
  for the same 502**, differing only in backoff (45 s merged, 30 s closed) — a small instance of the
  "two automations pricing the same bug the same night" shape `README.md`'s one-fleet rule exists to
  prevent. Both cosmetic; recorded, not filed.

## Run log

Append one row per firing, including quiet runs.

| Date           | Commits reviewed                                                                                                                                                                  | Issues filed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Benches                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-21     | `748c9c4..origin/main` (PR #337 NFR routines, PR #345 Cursor day-rollover + mindmap fix, PR #346 improve routine's first run) — 20 commits total                                  | #348 (bug fixed this run, filed for record), #349 (benchMermaid.js documented command broken)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | mermaid 87.5% accept (needs `--import ./scripts/register-antv-layout-esm.mjs --import tsx`, see #349), infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 8/16 accepted — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | First supervised run, [PR #350](https://github.com/acuhlmann/mermaid-gen/pull/350). Fixed one bug: `officeWorkingMemoryStore.js`'s day-rollover write path (`rememberWorkingMemoryBeat`/`stampWorkingMemoryBoard`) dropped the beat that triggered the rollover, because `rowFor()` read/merged stale pre-rollover state before `persist()`'s `reconcileOfficeDay()` wiped it. Regression test added and observed red on unfixed code, green after. `npm run check` clean except the pre-existing, already-filed #347 flake (confirmed transient by isolated re-run).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-21     | `e8d36f5..origin/main` (PRs #356, #357, #358, #359, #360, #361, #362, #364, #365) — 18 commits total                                                                              | #367 (`docs/automations/` shelf from PR #358 not mirrored into CLAUDE.md/AGENTS.md, Standards axis), #368 (duplicated participant-guard chain in `mermaidSequenceEdit.js` across 6 call sites, Standards axis / Fowler Duplicated Code)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 34.78% (8/23) accepted — all five `expectationMatch: 100`, no drift from prior run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Second run (out-of-schedule, human-triggered), [PR #369](https://github.com/acuhlmann/mermaid-gen/pull/369). Standards, Spec and the archislop trap checklist ran as three parallel sub-agents against the largest window yet (18 commits / 9 PRs / 55 files / +2327 −300, spanning NFR-infra fixes, a new `docs/automations/` playbook shelf, an Anything generation-bench wiring change, a new Mermaid `sequenceDiagram` canvas-graph-edit family, its own follow-up bugfix (#364, duplicate-message disambiguation), test-coverage additions, and a Metaphor3D panel/layer-key feature. Spec axis found both #360 and its fix #364 do exactly what their PR bodies claim (independently re-verified `findSequenceMessageRange`'s `(from,to,label)` matching and the lifecycle-line sweep); trap checklist found no `vi.mock`/empty-sweep/act-block/locale-parity/wire-field/shared-dist-rebuild failures. **No bug fixed this run** — both findings (the doc-mirroring gap and the duplicated guard chain) are real but neither has an observable behavior difference a regression test could go red against, so per §3 ("a bug you cannot make a test fail for gets filed, not fixed") both were filed instead. `DiagramCanvas.jsx`'s ratchet growth from PR #360 was already filed as #363 by the `improve` routine — not re-filed. `npm run check` has one red test inherited from `main`, not this diff (docs-only): `scripts/verify-ratchet.test.mjs` → "the repository currently satisfies its own ratchet" fails on the same `monolithLoc` violation #363 already tracks — same pattern `improve`'s first run (PR #346) documented for a different pre-existing flake. Confirmed pre-existing by running `node scripts/verify-ratchet.mjs` directly against `origin/main` before making any change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-08-24     | `192981b^..5943c02` (PRs #384, #385, #386, #387, #388, #389, #390, #392) — 16 commits total                                                                                       | #393 (Anything corpus fixture `runtime-svg-gettotallength` no longer rejects under the browser engine — expectation drift, Benches step)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54% — all `expectationMatch: 100`, no drift; **anything 36% (9/25) accepted, `expectationMatch: 96` — 1 regression** (`runtime-svg-gettotallength` flips accept under `browser`/`auto` engine, stays correctly rejected under `jsdom`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Third run (out-of-schedule, human-triggered), [PR pending]. Standards, Spec and the trap checklist ran as three parallel sub-agents against an 8-PR/60-file/+2006 −374 window spanning an NFR routine gather-filter fix + `DiagramCanvas.jsx`/`copilot.ts` monolith splits (#385), an Anything runtime-safe-JS craft-rule change (#386), the final canvas graph-edit family for chart `data.values` rows (#387), a docs sync (#388), new graph-edit-resolver regression tests (#389), a Metaphor3D ground/label-rank pass (#390), and an `improve` ratchet fix (#392). Spec axis re-verified every quantitative claim across all 8 PR bodies against the actual diff (LOC counts, test counts, constants, allowlists, budget numbers) — 0 mismatches, only a cosmetic stale code comment in #390 ("~10 rounds" vs the actual 12-iteration loop, not worth filing). Standards axis found 3 minor Fowler judgement calls (duplicated setup boilerplate across `chartGraphEdit.js`'s three mutators, a growing if-cascade in `canvasGraphEdit.js`/`useFlowchartGraphEdit.js` for the new `chart` family, a doc-comment enumeration now duplicated in three places) but explicitly judged none a defect worth fixing or filing — the repeated-switch shape is the documented adapter-per-family cost `docs/canvas-graph-edit.md` already accepts. Trap checklist: 0/8 patterns fired (no orphaned `chart-mark` wire field, no empty-sweep tests, no unpinned floor-test clock/seed, no locale drift — none of those areas were touched). **No bug fixed this run** — nothing from the Standards/Spec/trap axes cleared the "confident enough to prove with a failing-first test" bar. **One real finding did surface, from Benches, not Review**: the `runtime-svg-gettotallength` fixture `#386` added this same window rests on a false premise — `getTotalLength()` is `SVGGeometryElement`-only per SVG2, which real Chromium implements on `<rect>` (only jsdom's stub throws unconditionally), so the fixture silently passes under `jsdom` and silently fails under `browser`/`auto` in any environment with Chromium present (this one included). Root-caused and filed as #393 rather than fixed inline, per §4 ("an expectation drift... earns an issue") rather than §3's fix mechanism, which is scoped to the Review step's own findings. `npm run check` green throughout (see PR for full output); bench snapshots not committed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-25     | `5943c02..origin/main` (PRs #395, #396, #397, #398, #399, #400, #401, #403) — 16 commits total                                                                                    | #404 (`pickFlowchartEdgeRef`/`pickStateEdgeRef` byte-identical function bodies — Standards axis, Fowler Duplicated Code)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 30.77% (8/26) accepted — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Fourth run (scheduled), [PR pending]. Standards, Spec and the trap checklist ran as three parallel sub-agents against an 8-PR/39-file/+2066 −223 window spanning the flowchart (#395) and state (#401) parallel-edge disambiguation fixes, the Anything `getTotalLength` fixture fix (#396) and its follow-up d3 `forceLink` node-id craft rule (#397), the Metaphor3D city/garden canvas graph-edit family (#398), a docs sync (#399), a Metaphor3D link-readability/direction pass (#400), and the `improve` routine's `FusedCompositeScene` complexity fix (#403). Spec axis re-verified all 8 PR bodies against the actual diff — 0 wrong implementations, 0 scope creep, one trivial inaccuracy (#401 claimed "4 new test cases," diff has 5, not worth filing). Trap checklist: 8/8 checks pass (no orphaned `vi.mock`, both claimed sweeps — `metaphorLinkRoutes.test.js`'s theme×kind sweep and `anythingRuntimeCheck.test.js`'s corpus sweep — carry non-empty companion assertions, no `rerender`+`advanceTimersByTimeAsync` co-block, no locale-only key additions, no producer-only wire field, no unbuilt `packages/shared` import; office/floor checks N/A, nothing in that diff). Standards axis found two Fowler "Duplicated Code" judgement calls: the city/garden mutator boilerplate (already-declined precedent from the 2026-08-24 run's `chartGraphEdit.js` finding, not re-filed) and `pickFlowchartEdgeRef`/`pickStateEdgeRef` in `mermaidFlowchartEdit.js`/`mermaidStateEdit.js` — verified byte-identical function bodies (self-documented in the state version's own docstring as "Mirrors `pickFlowchartEdgeRef`"), the third independent implementation of the same parallel-edge-pick rule in this codebase. Filed as #404 rather than fixed — no observable behavior difference a regression test could go red against (pure refactor), so it doesn't clear §3's "confident enough to prove with a failing-first test" bar for the routine's one-bug-per-run budget. **No bug fixed this run.** `npm run check` green throughout; bench snapshots not committed (all 5 deleted after reading — none showed drift).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-26     | `a17b437..origin/main` (PRs #406, #407, #408, #409, #410, #411, #412, #413) — 17 commits total                                                                                    | none                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 29.63% (8/27) accepted — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Fifth run (scheduled), [PR pending]. Standards, Spec and the trap checklist ran as three parallel sub-agents against an 8-PR/38-file/+1518 −260 window spanning the `resolve` routine's shared `pickParallelEdgeRef` extraction closing #404 (#406), its own ledger PR-number fixup (#407, docs-only), the `anything` automation's d3 drag/TDZ and Matter `isSleeping` craft rules (#408), the Metaphor3D flat-kind canvas graph-edit family extended to the remaining 11 kinds via a factory (#409), a docs sync (#410, docs-only), new regression tests for the flat-scene helpers and parallel-edge wiring (#411), a Metaphor3D short-screen chrome/overlay-collision fix (#412), and the `improve` routine's dead-code removal behind #391's stale lint-warning gap (#413). Spec axis re-verified all 6 code-bearing PR bodies against the actual diff and by running the touched test files directly — 0 wrong implementations, 0 scope creep; every concrete claim checked out (#406's 9-case contract test and 4 call sites, #408's 3 design-guide rules and fixture, #409's exact 4-link/7-no-link kind split, #411's 60/60 passing across the 4 touched test files, #412's exact rail selector/width and 4-panel DOM order, #413's 5 named dead-code removals plus apps/server lint warnings at exactly 222); one minor gap noted, not filed — #413's diff also drops an unused `registerAppTool` import from a 6th file (`registerMcpApps.js`) not named in the PR's headline list, though disclosed in the same commit's `ratchet.json` comment, "not a functional problem." Trap checklist: 8/8 checks pass (no orphaned `vi.mock`, the one new sweep in `metaphorOverlayStyles.test.js` iterates a fixed non-empty 4-element array with a per-item assertion, no `rerender`+`advanceTimersByTimeAsync` co-block, no producer-only wire field — #409's new adapter routes through the existing `graphEditAdapterFor` dispatcher and #412's CSS/JSX changes have matching tests; office/floor, locale and `packages/shared`-rebuild checks N/A, nothing in this window touches those areas — confirmed `packages/` diff is empty despite #406's "shared" PR title, the extraction actually landed under `apps/web/src/utils/`). Standards axis found no hard standards violations (the wire-contract rule for #406's extraction and #409's new family was followed — both call sites verified, `docs/canvas-graph-edit.md` updated) and two Fowler judgement calls, both single-instance and tooling-invisible: `isFamilySource` on every kind in `metaphorFlatKindEdit.js` built but never read (Speculative Generality), and `connectNodes` as a same-signature passthrough over `connect` before export (Middle Man, self-flagged as a nit — mirrors the repo's existing `connect{Family}Nodes` naming convention). Neither cleared the "confident enough to prove with a failing-first test" bar or the materiality bar the 2026-08-21 `dayStampOf` precedent set for filing a single cosmetic dead-code item — recorded as open observations instead of filed. **No bug fixed this run, no issue filed.** `npm run check` green throughout; bench snapshots not committed (all 5 deleted after reading — none showed drift).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-27     | `cc2a47b..origin/main` (PRs #415, #416, #417, #418, #419, #420, #421, #423) — 9 commits total                                                                                     | none                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 28.57% (8/28) accepted — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Sixth run (scheduled), [PR pending]. Standards, Spec and the trap checklist ran as three parallel sub-agents against a 9-commit/31-file/+1364 −206 window spanning the `resolve` routine's #402 re-count/relabel-to-`ready-for-human` (#415, docs-only), the `anything` automation's KPI-formatter craft rules + `runtime-fmt-on-data-row` corpus fixture (#416, 28 fixtures total), Metaphor3D composite-layer canvas graph edit — the last metaphor family excluded from graph edit — via a new `metaphorCompositeEdit.js` (#417), a docs sync (#418, docs-only), new regression tests guarding composite selection/delegation/link-purge/guard-failure behavior (#419), a Metaphor3D fix lifting a fused site's own label clear of its skyline via `labelLift` (#420), a sequence-diagram duplicate-message disambiguation fix threading a `messageId` precedence through `findSequenceMessageRange` (#421, plus its own inline follow-up commit), and the `improve` routine's apps/web lint-warning-regression fix + CLAUDE.md register-drift correction (#423). Spec axis re-verified all 6 code-bearing PR bodies against the actual diff and by running the touched test files directly (150/150 pass across the 7 named files; 101/101 on #421's exact 4 claimed files) — 0 wrong implementations, 0 scope creep, 0 false claims; read the full new `metaphorCompositeEdit.js` (326 lines) end to end and confirmed delegation, re-id-on-collision, cross-descendant link purge, and the post-Add `metaphorKind` selection fix all work as claimed; confirmed #420's non-empty-sweep companion assertion (`expect(carried).toBeGreaterThan(0)`) is real, not vacuous; confirmed #421's `messageId` branch returns before any label logic runs, so precedence is genuine; confirmed #423's `mermaidSourceLocate.js` extraction is byte-identical logic (diffed old vs. new), pure code motion. Trap checklist: 0/8 fails (2 `vi.mock` specifiers both resolve; the `fusedCompositePlanner.test.js` label-offset and `labelLift` sweeps both carry non-empty companion assertions; no `rerender`+`advanceTimersByTimeAsync` co-block found in the diff at all; no office/floor, locale, or `packages/shared` files touched — all N/A; `metaphorKind`/`messageId` are both produced and consumed client-side in the same diff, no producer-only wire field; `copilotUserEdit.ts`'s 2-line change is a UI error-string update, not a new field). Standards axis found three single-instance, tooling-invisible Fowler judgement calls, none a hard CLAUDE.md violation: `mermaidSourceLocate.js`'s new `SEQUENCE_MSG_LINE_RE` (broad, indexes messages for `messageId` lookup) duplicates the pre-existing per-call `arrowRe` (narrow allowlist, legacy label-fallback path) as two independently-maintained sequence-arrow grammars — traced both call paths as self-consistent today with no live divergence in currently-supported arrow syntax, so this is a forward drift risk rather than a reproduced bug, matching the 2026-08-21 `graphEditIdFromDescriptor` "watching, not filing" precedent rather than the byte-identical-triplicate bar that got #404 filed; `metaphorCompositeEdit.js`'s `layerDelegate` is a fourth kind-dispatch switch beside the pre-existing `graphEditAdapterFor` (Repeated Switches, but the family-table convention already accepts one dispatcher touch per new kind); and `ensureGloballyUniqueId`'s in-place `item.id` mutation isn't signaled by its name (Mysterious Name, harmless, single call site). None cleared the "confident enough to prove with a failing-first test" bar — recorded as open observations instead of filed. **No bug fixed this run, no issue filed.** `npm run check` green throughout; bench snapshots not committed (all 5 deleted after reading — none showed drift).                                                                                                                                                                                                                                                                |
| 2026-08-28     | `e66560a..origin/main` (PRs #425, #426, #427, #428, #429, #430, #432) — 14 commits total                                                                                          | none                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 27.59% (8/29, corpus grew 28→29 from #427's new fixture, accept count unchanged) — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Seventh run (scheduled), [PR pending]. Standards, Spec and the trap checklist ran as three parallel sub-agents against a 14-commit/30-file/+1370 −109 window spanning the office `officeFloorWander.test.jsx` flaky-CI seeded-PRNG fix (#425), a behavior-preserving `assignSiteLabelPlacement` complexity-warning split with golden-value tests (#426, closes #422), the `anything` automation's NodeList/HTMLCollection `.slice()` craft rule + `runtime-nodelist-slice` corpus fixture (#427), canvas graph edit extended to infographic `chart-*`/`hierarchy-structure` flat arrays and Mermaid `classDiagram` (#428), a sequence-diagram `messageId` bugfix for label-less-then-labeled messages (#429), a Metaphor3D fix making a fused composite rank names round-robin across layers instead of by raw world-geometry magnitude so no layer's names get shut out (#430, measured 15/36→22/36 named journey stages across 3 fixtures × 3 viewports), and the `improve` routine's dead-code/complexity cleanup that filed #431 for the two new `complexity` warnings #430 introduced rather than fixing them same-day (#432). Spec axis re-verified all 7 code-bearing PR bodies against the actual diff, ran the touched test files/lint/bench directly rather than trusting claims — 0 wrong implementations, 0 scope creep; spot-checked the most load-bearing numeric claims and confirmed exact: #426's 3 test cases and byte-identical `length > 0.01` predicate direction, #427's 29-fixture `expectationMatch: 100`, #429's 107 passing tests across touched files, #430's exact 4-case/6-case test counts and verbatim negative-result doc mirror in CLAUDE.md/AGENTS.md, #432's ESLint-confirmed complexity scores (19 and 15, both left unfixed as claimed) and exact 851-warning total. One minor gap found, not a bug: #428's wordcloud `text`/`weight` flat-array path (`flatItemLabelKey`/`flatItemDefaultAttrs`) was implemented but had no test coverage — only plain `chart-*`/`hierarchy-structure` were tested. Standards axis found one judgement call matching this repo's own documented trap: PR #430's two new `fusedCompositePlanner.test.js` cases ("keeps every site above every landmark", "carries a layerKey on every ranked body") swept `world.sites.filter(...)`/`world.nodes` with no companion non-empty assertion — `Math.min(...[])`/`Math.max(...[])` resolve to ±Infinity, so an empty array would pass vacuously; not vacuous today (the fixture's arrays are non-empty) but exactly the "sweep over a derived set needs a companion assertion" pattern CLAUDE.md calls out by name. No hard CLAUDE.md violations; no other Fowler smells worth recording. Trap checklist otherwise 0/7 fires: no unresolved `vi.mock` specifiers, no `rerender`+`advanceTimersByTimeAsync` co-block, no locale-only key additions, no producer-only wire field, no unbuilt `packages/shared` import (nothing under `packages/` changed), no office/floor activity composition outside `floorActivityFor` (only the wander-bias test's own PRNG seeding changed, not the activity dispatch). **No production bug found this run, so no issue filed and no fix counted against the one-bug budget** — but both real findings were safe to close directly as pure test additions (README's "tests may be added freely" — no production code touched, no existing assertion weakened): added non-empty companion assertions (`expect(...length).toBeGreaterThan(0)`) to both flagged `fusedCompositePlanner.test.js` cases, and added two wordcloud round-trip tests (add-sibling-with-default-weight, rename-via-`text`-not-`label`) to `infographicGraphEdit.test.js` closing the Spec-axis coverage gap — read the wordcloud code path end to end first and found it correctly implemented (both tests pass without any production change). `npm run check` green throughout (258 web test files / 2883 tests, 57 server tests, typecheck/strict islands/lint/dep-cruiser/wire/doc-paths clean); bench snapshots not committed (all 5 deleted after reading — none showed drift). |
| 2026-08-29     | `38092b4..origin/main` (PRs #434, #435, #436, #437, #438, #439, #440) — 10 commits total                                                                                          | #441 (`docs/canvas-graph-edit.md`'s Mermaid ER row has unescaped `\|\|--o{` splitting the markdown table — cosmetic, filed rather than fixed since the file is outside `review`'s `allowedPaths`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 26.67% (8/30, corpus grew 29→30 from #436's new fixture, accept count unchanged) — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Eighth run (scheduled), [PR pending]. Standards, Spec and the trap checklist ran as three parallel sub-agents against a 10-commit/29-file/+1054 −118 window spanning two docs-only `resolve` ledger updates (#434, #435), the `anything` automation's Matter.Body API craft rules + `runtime-matter-setstatic` fixture (#436), a new Mermaid `erDiagram` canvas-graph-edit family (#437, `mermaidErEdit.js`, 339 new lines — the biggest change), a Metaphor3D fix giving the archipelago `chain` placard its own label-placement plan (`labelLift`/`labelOffset`/`namedByMember`, closing the last group-placard exception CLAUDE.md tracked) (#438), a bugfix unifying labeled-classDiagram-relation and colon-less-erDiagram-relation parsing (#439), and the `improve` routine fixing a live Slop Chat crash — three `OfficeLayer.jsx` handlers called an undefined `setMessengerOpen` (#440). **Spec axis**: all 7 PRs CONFIRMED against the actual diff and by running the named test files directly (including reverting #440's fix locally to observe its regression test go red, then re-applying it green) — 0 mismatches. **Trap checklist**: 0/8 hits (checked all 8 explicitly, several genuinely N/A for this window — no locale/floor/packages files touched). **Standards axis found one real bug, fixed this run**: PR #437/#439 gave `mermaidErEdit.js`'s `renameErNode` an unconditional template that always appended a `" : "` label suffix, unlike its sibling `renameClassNode` (`mermaidClassEdit.js`), which guards that suffix on a truthy label — renaming an entity in a colon-less ER relation (a form this same window's own fix taught `parseErRelation` to accept) produced a relation line ending in a bare `\|\|--o{ Purchase :` with an empty trailing label. The existing `mermaidErEdit.test.js` regression test for this exact path used an unanchored substring match that passed either way and masked the bug — tightened to a full-line anchor, observed it fail against the unfixed code (red), then added the same label-guard `renameClassNode` already uses (green). Standards axis also flagged a cosmetic docs-table rendering nit in the new erDiagram family-table row — filed as #441 rather than fixed, since `docs/canvas-graph-edit.md` is outside this routine's `allowedPaths` (and, per the meta-irony, drafting this exact row taught the same lesson twice — a literal `\|\|--o{` in ledger prose must be pipe-escaped or it splits this very table). `npm run check` green throughout (260 web test files / 2900 tests, 57 server tests, typecheck/strict islands/lint (0 errors, pre-existing warning counts unchanged)/dep-cruiser (0 errors)/wire/doc-paths all clean); bench snapshots not committed (all 5 deleted after reading — none showed drift). **Backfilled 2026-08-30 by the fleet tune-up, not by a routine.** This row shipped inside PR #442 and never merged, so the ledger had no 2026-08-29 entry and the 08-30 run wrongly concluded the firing "appears to have been skipped" — a false inference from `--preflight`'s silence, since the open-PR check README rule 5 promises did not exist until this commit. The `renameErNode` fix itself is **on main by another route**: Cursor's `critical-bug-memory` automation landed the same label guard in #446 (`a7be8f6c`) with a stronger test than #442 carried (an anchored match _and_ an explicit `not.toMatch(/Purchase\s*:\s*$/m)`). #442 closed unmerged, redundant. The durable lesson is the collision, not the bug: two fleets hunting the same 24-hour window found one defect twice and paid for it twice.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-30     | `38092b4..origin/main` (12 PRs: #434, #435, #436, #437, #438, #439, #440, #443, #444, #445, #446, #448) — 18 commits total                                                        | #449 (`groupIdentity.js`'s `groupSlots()` exported and fully tested but never called at either of the two call sites its own docstring names — Standards axis, Fowler Speculative Generality + latent Duplicated Code)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | mermaid 87.5% accept, infographic 57.14%, chart 38.46%, metaphor 61.54%, anything 26.67% (8/30, corpus grew 29→30 from #436's new `runtime-matter-setstatic` fixture, accept count unchanged) — all five `expectationMatch: 100`, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Eleventh run (scheduled). **Window note:** the ledger has no `review` row for 2026-08-29 even though `resolve` (#443) and `improve` (#440) both ran that day — the scheduled firing appears to have been skipped. Widened the window from the literal "last 24 hours" back to `38092b4` (the last commit this routine actually reviewed, PR #433's own merge) so `9fcf704..8c058d7` isn't left permanently unreviewed; flagged as an open observation below rather than guessing at the cause. Standards, Spec and the trap checklist ran as three parallel sub-agents against the resulting 18-commit/51-file/+2496 −192 window spanning the `anything` automation's Matter.Body craft rules + `runtime-matter-setstatic` fixture (#436), Mermaid erDiagram canvas graph edit (#437), a Metaphor3D fix giving the archipelago chain placard its own label-lift/offset plan (#438), a labeled-class/colon-less-ER parsing bugfix (#439), the `improve` routine's Slop Chat crash fix + minor cleanup (#440), Mermaid timeline/pie + infographic compare-* canvas graph edit (#444), a Metaphor3D feature drawing the categorical grouping axis as a colour tint across City/Garden/Archipelago/FusedComposite scenes (#445), a timeline/pie graph-edit-id + colon-less-ER-rename bugfix (#446), the `improve` routine's dead-code cleanup dropping `apps/web` lintWarnings to 836 (#448), and three docs/ledger-only `resolve` commits (#434, #435, #443). Spec axis reran the touched test suites and lint directly rather than trusting claims — 0 wrong implementations, 0 scope creep across all 9 code-bearing PRs; every spot-checked numeric claim matched exactly (test counts, `expectationMatch`, lint-warning totals, the ~1/3 collision-probability math in #445) with one exception: #438's claim that the pre-fix `islandCrestY` expression had "three copies" — grep found only 2 (`FusedCompositePrimitives.jsx` uses an unrelated formula), a trivial inaccuracy in an already-merged PR description, not worth filing. Trap checklist: 8/8 pass (the one new `vi.mock` pair in `useFlowchartGraphEdit.test.jsx` resolves to real files; every `Math.min`/`Math.max`/filter sweep in the new grouping and graph-edit code operates on structurally non-empty sets; no `rerender`+timer-advance co-block; no locale files touched; every new field — `groupIndex`, `namedByMember`, `labelOffset`, `labelLift`, `districtIndexOf` — has a verified consumer; nothing under `packages/shared` changed; `OfficeLayer.jsx`'s fix touches no floor-activity composition). Standards axis found the filed #449 plus three judgement calls not worth filing: duplicated colon/label-splitting logic between `mermaidClassEdit.js` and `mermaidErEdit.js` (matches the repo's already-accepted per-family-file convention); `openDeskCommsPanel('slopChat', ...)` repeated inline across 5 callbacks in `OfficeLayer.jsx` (minor, single-file); and an unrelated `treeRadialLayout.js` `place()` signature drive-by bundled into the archipelago PR (harmless, Divergent-Change-adjacent). **No bug fixed this run** — every finding is a duplication/dead-code/documentation judgement call with no observable behavior difference a regression test could go red against, so none clears §3's bar; #449 was filed instead. `npm run check` green throughout; bench snapshots not committed (all 5 deleted after reading — none showed drift).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-30 (2) | `597a7a5..bb8de9f` (9 PRs: #450, #451, #453, #378, #379, #454, #457, #458, #459) — 53 files, +4715 -2002                                                                          | #460 (subway route sign: the reach clamp collapses `ROUTE_SIGN_STANDOFF` to 0.264 on every single-line network and to **exactly 0.000** on a 4-line/1-stop map, reproducing the on-the-platform defect `subwayRouteSign` exists to prevent); #461 (`resolve` can widen its own budget — #453 gave it `docs/**`, which matches its own playbook, and neither `forbiddenPaths` nor `ALWAYS_FORBIDDEN` covers front-matter); #462 (`metaphorGroupPlacards.test.js` is the one metaphor web suite missing from `METAPHOR_BLAST_TESTS`, so pre-push and `check:affected` cannot see it); #463 (Metaphor3D link mutators `renameCityEdge`/`deleteCityEdge` are unreachable — no metaphor selection ever yields a `kind:'edge'` descriptor)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | mermaid 87.5% (16 cases), infographic 57.14% (14), chart 38.46% (13), metaphor 61.54% (13), anything 25.81% (8/31 — corpus grew 30→31 from #458's new `runtime-matter-runner-tick` fixture, accept count unchanged at 8) — all five `expectationMatch: 100`, exit 0, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Twelfth run (scheduled). **Window:** started from `597a7a5`, the last commit the 2026-08-30 run reviewed, rather than the literal 24 hours, which would have re-reviewed five already-covered PRs. Standards, Spec and the trap checklist ran as three parallel sub-agents. **Trap checklist 8/8 pass** — the diff adds no `vi.mock` at all, no `act`/fake-timer usage, no office or locale files, nothing under `packages/shared`; every new sweep is length-asserted or structurally non-empty; the three new layout fields (`line.sign`, `station.platformRadius`, `axle.placard`) each have a consumer in the same commit. **No bug fixed this run** — second consecutive. Every finding failed §3's bar for a different, recorded reason: #460's fix must rewrite `metaphorGroupPlacards.test.js`'s "stays inside the reach" case, which #457 wrote two days ago _on purpose_, so it is a design decision needing render evidence, not a regression repair; #461 and #462 are in `scripts/**` and `docs/**`, outside this routine's `allowedPaths`; #463 is a 3D hit-testing feature. The one candidate inside budget (the `bounds.radius` unscaled-radius inconsistency, flagged by both axes) was traced to unreachable — the schema caps `traffic` at 20 — and its regression test passed against the unfixed code, so it was reverted rather than committed as a green test that examines nothing. **Axis disagreement worth recording:** Spec called `renameCityEdge` reachable because it is registered as `METAPHOR_CITY_ADAPTER.renameEdge`; the trap pass called it unreachable. Resolved by hand in favour of the trap pass — _registered_ is not _reachable_, the `descriptor.kind` gate upstream decides, and conflating the two is how a dead path gets documented as shipped (#463). Spec's #1 (`routine-guard` printing `0/undefined files` for a report routine) is already open as PR #456, not re-filed. `npm run check` green; bench snapshots deleted, not committed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-31     | `bb8de9f..22783c5` (7 PRs: #456, #464, #466, #467, #468, #469, #470) — 12 commits, 21 files, +693 -91                                                                             | #471 (`ItemLabel`'s declutter `apply` closure captures a stale `plateOpacity` for a label whose `pinned` prop is already true — the accent chip lift silently undone ~110 ms later on subway interchanges and tree trunks), #472 (`docs/canvas-graph-edit.md` points twice at #463 as the tracker for the 3D link-hit descriptor; #467 closed #463 by taking the _document it_ branch, so six adapters' worth of tested-but-dead `renameEdge`/`deleteEdge` are now untracked), #473 (#466's strict-island sensor is unreachable from both files that can cause the drift: `packages/eslint-config/typeCheckedIsland.js` selects zero tests **and** zero fallbacks, `apps/server/tsconfig.strict.json` falls back to the server suite which never runs `scripts/*.test.mjs`), #474 (`metaphorDrawOrder.js` + the `metaphor3d.md` entry it seeded state that `renderOrder` is what decides the stem and pin vs the name; three.js renders the whole opaque list before the whole transparent list, so the ladder decides only the caption and `depthWrite` decided the rest — a wrong causal rule in an auto-loaded domain file, plus a false "unchanged behaviour" claim in the test header), #475 (`routine-guard.test.mjs` cannot fail on the string #456 shipped; `improve.md` § 4 still lists the gap #466 closed; `verify:strict-islands` has no npm script, no `sensors.md` entry, and an entry-point guard that differs from its five siblings), #476 (`metaphorFlatKindEdit.js`'s `findFlatLink`/`renameEdge` are verbatim copies of `metaphorCityEdit.js`'s — the `canvas-graph-edit` playbook's `allowedPaths` don't glob the shared `metaphorFlatItemsCore.js`, so the budget rule is what produced the copy); plus a comment on #462 with two more `METAPHOR_BLAST_TESTS` omissions                                                                    | **fixed**: the accented label's `depthTest` never followed a moving accent. #468 delivered the drop through drei's `onSync`; drei asks troika to sync every render but troika's `sync()` drops the callback unless `_needsSync` is set, and only its `SYNCABLE_PROPS` setters (`text`, `fontSize`, `letterSpacing`, `maxWidth`, the anchors) set it — being accented is none of them. So on a Refine turn that moves the accent, the new accent's glyphs stayed depth-tested (the fix never reached the item the user just marked) and the old one's stayed depth-free (a back-row name floating in front of the geometry between it and the camera — the trap the lift is scoped to one item to avoid). Found independently by both axes. Extracted to `apps/web/src/components/metaphorScenes/metaphorLabelDepth.js` so the accent-change case could be driven without a renderer (`@react-three/test-renderer` is not a dependency and a routine may not add one); the two failing cases were observed red against the module in its `onSync`-only shape before the `useLayoutEffect` was added. | all five key-free benches `expectationMatch: 100` — mermaid 16 cases, infographic 14, chart 13, metaphor 13, anything 32. No expectation drift, no issue. Latency informational: mermaid p50 11.9 ms / p95 44.9 ms on a shared runner. Snapshots deleted rather than committed (`bench-results/` is on the don't-touch list).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-09-01     | `22783c5..f57b97a` (17 PRs incl. #477, #479, #480, #483, #484, #485, #486, #487, #488, #489, #490, #491, #493, #496) — 28 commits, 54 files, +2665 -587                           | #497 (the startup-window `/api/health` answers 200, so Cloud Run's HTTP startup probe confirms exactly the open TCP socket its own comment says it does not — **fixed this run**); #498 (three `deps:` PRs shipped diffs `routine-guard` would reject — an agent-authored `package-lock.json` in `0c08b90` and three edits to `docs/routines/deps.md`/`README.md`/`digest.md` — hours after ADR-0017 made both refusable; none ran postflight and nothing in CI does, so the guard binds only runs that call it); #499 (four ratchet budgets regressed: `apps/server` lintWarnings +2 from dead `dotenv`/`resolveSessionIdFromRequest`/`repoRoot` stranded by #493's split, `apps/web` +1 from `routePoint` crossing `max-params` at 6, and both root context files ~3.3 KB over from ADR-0017 prose mirrored into each); #500 (`fusedCompositePlanner.js` 1139 → 1261 lines carrying `FUSED_SEA_LEVEL_Y`/`fusedSurfaceHeightAt`/`routeAlongSurface` — pure, separately tested, already imported from `fusedCompositePrimitives.jsx`, i.e. the extraction the same PR series did correctly with `metaphorLabelDepth.js`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | mermaid 87.5% (16 cases), infographic 57.14% (14), chart 38.46% (13), metaphor 61.54% (13), anything 24.24% (8/33 — corpus grew 31→33, accept count unchanged at 8) — all five `expectationMatch: 100`, exit 0, no drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Thirteenth run (scheduled). **fixed**: the startup-window `/api/health` returned 200 while `attachRoutes` was still loading. #490 added an HTTP startup probe whose own comment says it "confirms /api/health, not just an open TCP socket"; #493, ninety minutes later, made that endpoint answer 200 before any route existed. Cloud Run treats 2xx as started, so with `--min-instances=0` every visit after an idle period was released into a bare Express app — `Cannot GET /`, and `coldStartGate.js`'s 90 s waking poll never ran because the shell it lives in 404'd. Found independently by both axes. Handler extracted to `apps/server/src/startupHealth.js` (ADR-0005) so the wire contract could be driven without booting the app; the three cases were observed red against the shipped 200 (`200 !== 503`) before the change. 503 satisfies both consumers — the probe holds traffic on the same 240 s budget (`failureThreshold=24 × periodSeconds=10`) Cloud Run allowed for the plain socket bind before the split, and `isHealthReadyResponse` already reads `!response.ok` as "still waking". Also added the three missing non-empty assertions to `metaphorFusedRoutes.test.js` (trap-checklist hit: the file landed in #488 the same night `improve` closed that exact class in five other files).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-09-02     | `f57b97a..c74b1cd` (9 PRs: #494, #499/#502, #501, #402/#503, #504, #505, #506, #507, #508) — 12 commits, 33 files, +783 -323                                                      | #509 (this run's fix, filed for the record — `subwayRouteSign` scores nothing when `stations` is omitted: `platformClearance` returns `Infinity`, every candidate ties, `>` never fires and the first candidate wins, so the crowding penalty and the near-edge tie-break are both inert), #510 (#402's param grouping stopped half-way — `renameEdge` takes a union-of-dialects object `{edgeLabel, edgeIndex, messageLabel, messageId}` where `messageLabel` is `edgeLabel` restated, while `deleteEdge` 100 lines above is still positional), #511 (#502's root-doc trim left the `/schedule` subscription-login caveat and the cursor.com link in `AGENTS.md` alone — the one-file state `CLAUDE.md`'s own preamble forbids), #512 (`canvas-graph-edit.md` §14 calls `renameCompositeEdge` shipped without the #495 unreachable-from-canvas caveat every sibling sentence carries), #513 (`deps` ledger's 04:37Z row transposes the dompurify pins — records monaco-editor at `3.4.11`, tree and its own todo both say `3.4.8`). Commented on #460 (re-measured post-#505, see below) and #499 (the remaining ratchet breach has an owner).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 5/5 clean, `expectationMatch` 100 across all: mermaid 16 cases / accept 87.5, infographic 14 / 57.14, chart 13 / 38.46, metaphor 13 / 61.54, anything 34 / 23.53. No expectation drift, no issue. Snapshots not committed (don't-touch).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Bug fixed: #509.** Two regression tests observed red first (`expected 0 to be greater than 1`, `expected -1.5 to be greater than 0`), then green; production placements verified byte-for-byte unchanged on single-line, 2-line and 4-line shapes, because `subwayNetworkLayout` always passes non-empty `stations` and the fix only fires when it is empty. Also added the two companion non-empty assertions the trap checklist wants on `metaphorGroupPlacards.test.js`'s nested `for (a of lines) for (b of lines)` sweep, which asserts nothing on a single-line network. **Not fixed, deliberately: #460.** Re-measured and it is worse after #505, not better — `reachZ` on a single-line network is one platform radius (0.636) while `ROUTE_SIGN_LATERAL` is 1.5, so the clamp rejects all six track-side candidates and every route falls through to `terminusSign`. `DEFAULT_LINE = 'Main line'`, so every document that does not set `line` is single-line: #505's placement is unreachable on the default shape, which still measures 0.264 clearance (4 lines × 1 stop measures -0.000). Two or more lines give `reachZ` 2.436 and work as intended, which is consistent with #505's 24/24 being measured on multi-line fixtures. Filed as a comment on #460 rather than a second issue — same file, same clamp, same root cause already written up there. Not fixed here because every candidate fix changes multi-line placements too and so invalidates #505's rendered measurement; `docs/automations/metaphor3d.md` requires viewports and before/after numbers in the PR, and `--reachable` puts `metaphor3d` on that file. **Ratchet:** `apps/server` 224 vs 222, the only breach left from #499 — `loadServerApp.js`'s three dead identifiers from #493's bootstrap split; `--reachable` → `deps, resolve, review`, so #499's `ready-for-agent` is keepable and `resolve` reaches it tonight. Not taken here: it is not a defect in this window, and one bug per run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-09-04     | `c74b1cd..aa80bcf` (11 PRs: #514, #515, #516, #512/#517, #518, #519, #520, #521, #522, #523/#524) — 11 commits, 19 files, +313 -55                                                | #526 (this run's fix, filed for the record — a fused landmark's name is drawn nearer a **different** landmark than the one it names: #519's shoulder push is sized from the site and blind to the next island, so on the shipped festival composite `artist-check-in`'s name sat 2.10 from itself and **0.68** from `shuttle-control`, and `shuttle-control`'s 1.90 from itself and 1.57 from `artist-check-in` — both read as the wrong landmark's, the very failure the push was added to prevent, moved one island over); #527 (#519's label-reach block: `?? 1` and the `0.85` floor can never fire given `siteRadiusFor`'s `[1.8, 4.6]` clamp; the `0.12 lower bound in nodePosition` is cited as the **cause** of the degenerate-bearing branch when it is what prevents it — measured minima 1.05/0.50/0.53, zero nodes entering it on any fixture, the only live producer being an authored `item.position`, which is also why the golden-angle walk #519 advertises has no test; and the 45-pixel-square anecdote in the auto-loaded `metaphor3d.md` names three landmarks that sit on three **different** sites, one of them an orbiting accent); #528 (#515's reverse-direction guard covers two bundles, not the "all three" its PR body and `ratchet.json`'s `$comment` both record — `GRAPH_EDIT_BLAST_TESTS`, the bundle `canvas-graph-edit` grows nightly, has no on-disk sweep; the forward existence guard skips `ISOMETRIC_FLOOR_BLAST_TESTS`, the bundle #515 hand-extended by 10; both sweeps read `apps/web/test` only while `METAPHOR_BLAST_TESTS` spans three workspaces); #529 (`isometric-floor-tests.md`'s Test file map is 8 of 33 `officeFloor*` suites behind — the doc half of the "add it to that list **and the agent doc**" instruction #515's own commit restated, and now the only unguarded leg of that three-way invariant) | 5/5 clean, `expectationMatch` **100** across all, no expectation drift: mermaid 16 cases / accept 87.5, infographic 14 / 57.14, chart 13 / 38.46, metaphor 13 / 61.54, anything 35 / 22.86. The anything corpus is 34→35 and its accept rate 23.53→22.86 purely from #522's new `runtime-const-self-reference-init` fixture, which is `expectedAccept: false` — the corpus property `benchAnything.js`'s own header warns is not a quality signal. Latency informational only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Bug fixed: #526.** Regression test written first and observed red (`expected 0.676… to be greater than 2.095…`), then green; the fix is a **cap**, not a replacement — three of ten names untouched, the seven trimmed keep 0.83–2.22 units against the 0.58 #519 replaced, and the two mis-attributed names now read as their own. Landed as a new sibling module (`fusedLabelReach.js`) rather than in `fusedCompositePlanner.js`, which is 1287 lines with #500 open against its LOC. Trap checklist **clean** — and the window _closes_ three instances of it (two `layout.lines.length` companions in `metaphorGroupPlacards.test.js`, one `assert.ok(onDisk.length > 0)` in the new sweep). Verified but clean, no finding: #514's two regression tests reproduce red against the pre-fix `return worst` and `subwayNetworkLayout` output is byte-identical old-vs-new; #522's fixture really is rejected `runtime_error` ("Cannot access 'card' before initialization"); #524's `DiagramCanvas.jsx:1063` claim holds; #515's fix works end-to-end (a floor source file now selects all 40 suites).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-09-04 (2) | `aa80bcf..502b252` (7 PRs: #530, #511/#532, #525, #533, #534, #537, #538) — 9 commits, 20 files, +516 -101                                                                        | #539 (this run's fix, filed for the record — a path **station**'s name is drawn nearer a different station than the one it names: #530's `clampNodeLabelReach` reaches `plan.nodes` only, while a station's push is a flat `0.72` along the bearing from its SITE's centre, so two stations at a similar angle are walked along the same line; on the shipped toaster composite `bread-enters` and `coil-hesitates` sit 0.18 apart and `bread-enters`'s name landed **0.720 from its own station and 0.701 from `coil-hesitates`** — #530's failure exactly, one grammar over); #540 (`clampLabelReach` has no lower bound, so it multiplies straight through the `Math.max(0.85, …)` floor `makeNodes` documents three files away — the landmark side's tightest shipped pair trims to 0.831, just under it, and this run's station clamp takes a real pair to **0.080**; the module's own assertion `away > own` holds for arbitrarily small `own`, so nothing tests the lower end and neither branch of the trade-off is written down as a decision); #541 (#538's `stripNonLoadContexts` fix is pinned **only** by a bench-corpus fixture `npm test` never executes — `anythingRuntimeCheck.test.js:231` filters the corpus to `kind === 'runtime' && expectedCode === 'runtime_error'` and the new fixture is `quality`/`unclosed_tag`, so reverting the `                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | $`alternation leaves the whole suite green;`packages/shared/test/anythingPolicyLint.test.ts`already carries the analogous case and was not extended); #542 (#534 baked its **superseded** measurement into two shipped source comments —`SubwayScene.jsx:233`and`subwayNetworkLayout.js:294`say "5 of 26", the pre-run diagnosis from the metaphor3d ledger's queue item, while the PR body, the new test,`metaphor3d.md:221`and the ledger row all say "6 of 29"; plus`labelDeclutter.js:164`'s typedoc still documents `entry.object` as "billboard", the exact thing #534 changed to the lifted inner group)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 5/5 clean, `expectationMatch` **100** across all, no expectation drift: mermaid 16 cases / accept 87.5, infographic 14 / 57.14, chart 13 / 38.46, metaphor 13 / 61.54, anything 36 / 22.22. The anything corpus is 35→36 and its accept rate 22.86→22.22 purely from #538's new `quality-unclosed-script-with-comment` fixture, which is `expectedAccept: false` — the corpus property `benchAnything.js`'s own header warns is not a quality signal. Latency informational only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Bug fixed: #539.** Regression test written first and observed red (`expected 0.7006… to be greater than 0.7199…`), then green. `clampNodeLabelReach` generalised to `clampLabelReach(bodies, positionOf)` — the body's own point is a parameter because stations carry `point`, not `position` — and applied per path; 12 stations across the three shipped composites, 5 untouched, 7 trimmed. Not widened: stations are compared only against their own path's stations, which is the confusion the shared bearing actually produces and the only one measurable here. Trap checklist **clean**; the new sweep carries its own `compared > 0` companion. Verified but clean, no finding: #538's "a well-formed document strips identically to before" holds (`$` matches only at end of input, so the lazy body still prefers a real closing tag) and the wider strip opens no hole — an unclosed document hiding a **quoted** URL is still `external_url`, because `stripJsComments` is quote-aware, and an unclosed one without a URL falls through to `unclosed_tag`; #534's `lift` is exactly 0 for a one-line label so registering the inner group is a no-op at zero lift, `station.title |     | item.label`correctly covers`subwayStationTitle`returning`''`, and `SubwayScene.jsx`is the only consumer of`subwayNetworkLayout`; #530's `clampNodeLabelReach(nodes)`really does sit safely before`assignSiteLabelPlacement`, which writes `site.labelOffset` only and never touches a node's. |
| 2026-09-05     | `502b252..5cec03b` (12 PRs: #555/#510, #554, #553, #551, #550, #549, #548, #541, #475, #499, #542, #528, plus 4d48cfb/c24ac87/8eba1f8/14551ca) — 26 commits, 70 files, +5082 -398 | #556 (this run's fix, filed for the record — a pie wedge is stamped with its **DOM position** while `mermaidPieEdit.js` addresses slices by **source position**, and the pinned mermaid drops sections out of the DOM by two rules `stampPieSliceHitTargets` did not model: `addSection`'s `if (!sections.has(label))` merges a repeated label into its first line, and `createPieArcs`' `.filter((d) => (d.value / sum) * 100 >= 1)` never draws a section under 1% of the total. Measured against `mermaid.render` at the pinned `^11.17.2`: `pie / "Mice" : 1 / "Dogs" : 386 / "Cats" : 85` renders **two** wedges, and tapping the big lilac one — Dogs — renamed **Mice**; the duplicate-label shape does the same one wedge over. The existing suite covered only the inverse case, on a fixture where the two orders coincide); #557 (#550's `LINK_EDITABLE_METAPHORS` cites `metaphorLinkPick.test.js`, which does not exist — the real sweep at `metaphorGraphEdit.test.js:352` iterates a **hardcoded** seven kinds and never reads the registry, so six of the eleven flat kinds are unchecked and the promised "fails the test until it is added here" cannot fire; and the same doc block's "everything else returns `not-graph` on purpose" is already false for **composite**, which registers `canLink: true` with a real `renameCompositeEdge` and renders `MetaphorLinks` but is gated off at `MetaphorRenderer.jsx:1339`, leaving exactly the dead code #495 named)                                                                                                                                                                                                                                                                                                                                                                            | 5/5 clean, `expectationMatch` 100 on every corpus (mermaid 87.5 accept, infographic 57.1, chart 38.5, metaphor 61.5, anything 24.3); no drift. Latency informational: mermaid p50 9.69 ms / p95 39.61 ms                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-06     | `5cec03b..3f7db8b` (23 PRs: #558, #559, #560–#572, #574, #576, #577, #579–#586) — 56 commits, 70 files, +3248 -650                                                                | #587 (subway `reachZ` floor is not inert for low-traffic multi-lane networks — #568's "byte-identical" claim holds only at the default traffic; `ready-for-agent`), #588 (`docs/canvas-graph-edit.md:156` still says the composite layer set is fixed, which #577 made false — found by BOTH axes independently; `ready-for-agent`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | all five green, `expectationMatch` 100 on every corpus (mermaid 16 / infographic 14 / chart 13 / metaphor 13 / anything 38); no drift. Latency informational: mermaid p50 8.25 ms, p95 30.46 ms.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **`class Duck:::fancy` — a rendered, clickable class no mutator could address** (`mermaidClassEdit.js`). `CLASS_BARE_DECL_RE`/`CLASS_BLOCK_OPEN_RE` captured `(\S+)`, swallowing mermaid's `:::` STYLE_SEPARATOR, so `collectClassIds` learned the phantom `Duck:::fancy` and never `Duck`; rename/delete/connect all answered `missing` on a visible box. #575's failure one syntax over. Both groups captured separately; rename carries the suffix through so styling survives. Measured against the pinned mermaid in jsdom; 5 tests written first and observed red.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
