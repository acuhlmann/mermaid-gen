---
name: improve
todos:
  - id: register-loc
    content: 'Recompute the CLAUDE.md file-size budget table — every row was stale when this routine was written'
    status: completed
  - id: register-coupling
    content: 'Refresh balanced-coupling-priorities.md Implementation progress rows and the Last reviewed date'
    status: completed
  - id: scoreboard-unlisted
    content: 'File issues for over-threshold files absent from legacy-monoliths.js (officeCast.js 2789, officeMomentStore.js 938) — issue, never a new suppression'
    status: pending
  - id: sensor-floor-inverse
    content: 'Inverse check: every apps/web/test/officeFloor* file must appear in ISOMETRIC_FLOOR_BLAST_TESTS'
    status: completed
  - id: floor-tests-doc-rows
    content: "docs/agents/isometric-floor-tests.md's suite table is missing 6 of the officeFloor* suites the 2026-09-02 run added to ISOMETRIC_FLOOR_BLAST_TESTS (officeFloorBoard, officeFloorCamera, officeFloorCoffeeWalk, officeFloorIntro, officeFloorNextStore, officeFloorSceneJoin). The lib's own docstring says keep the two in sync. Not done that night because an accurate one-line description of each needs reading the six suites, and a wrong row is worse than a missing one — a clean item-6 (doc drift) slice."
    status: pending
  - id: sensor-strict-island-sync
    content: 'Assert SERVER_STRICT_ISLAND_FILES matches apps/server/tsconfig.strict.json'
    status: completed
  - id: sensor-guidance-sync
    content: 'Assert formatter.cjs GUIDANCE agrees with guidance.js'
    status: pending
  - id: routine-guard-postflight-message-test
    content: "#475 finding 1: scripts/routine-guard.test.mjs's postflight-message test never captures stdout, so reverting #456's ternary leaves it green. Its preferred fix (extract an exported postflightOkMessage) needs scripts/routine-guard.mjs, which is ALWAYS_FORBIDDEN for every routine including this one — but its second option does not: spawn the CLI and assert stdout, which touches only the test file (improve-reachable). Also worth generalising off the hard-coded `digest` playbook so a second report-tier routine is not invisible to it. The 2026-09-02 run closed #475's finding 2 (the stale § 4 known-gap line) but left this half."
    status: pending
  - id: glossary-unchecked
    content: 'GLOSSARY.md is in no verify:doc-paths scan list; decide whether to add it or explain why not'
    status: pending
  - id: readme-dup
    content: 'README.md and docs/guide/README.md duplicate the operator table — reconcile or collapse one into a pointer'
    status: pending
  - id: ts-leaves
    content: 'Begin apps/web/src/utils leaf conversions per convert-js-leaf-to-ts.md — apps/web is 3.8% TypeScript'
    status: pending
  - id: runtime-fallback-budget
    content: "Give runAnythingJsdomCheck its own budget instead of resharing the browser rung's — the shared clock makes anythingRuntimeBrowser.test.js flaky on cold machines (needs apps/server/src/tools, outside this routine's allowlist — filed as issue #347)"
    status: completed
  - id: lint-promotion-evidence
    content: 'All 1206 ESLint findings are warnings and zero are errors; gather quiet-period evidence for one rule and file it for a human decision'
    status: pending
  - id: ratchet-no-gate-sensor
    content: "Add a sensor asserting no scripts/*.test.mjs file measures the live repo against docs/agents/ratchet.json and hard-asserts zero violations, so 'the ratchet gates no build' can't quietly stop being true again the way it did 2026-08-21"
    status: pending
  - id: max-params-canvas-edge-rename
    content: "renameFlowchartEdge/renameStateEdge/renameSequenceEdge (a third instance found 2026-08-26) all cross max-params (6 > 5) adding parallel-edge disambiguation; fix needs a call-site change in apps/web/src/features/canvas/useFlowchartGraphEdit.js, outside this routine's allowedPaths — filed as issue #402, updated 2026-08-26 with the sequence instance"
    status: pending
  - id: mcpserver-dead-code
    content: "apps/server/src/mcp/mcpServer.js carries two of the same dead-code warnings fixed elsewhere this run: an unused `textResult` import and a useless `let appSessionId = null` initializer (immediately overwritten before any read). Left alone because any touch to mcpServer.js trips check:affected's `mcp-tool-without-test` wire-cochange sensor (hard-fails the diff unless apps/server/test/mcpServer.test.js is touched too), and padding that test file just to satisfy the sensor for a pure dead-code removal wasn't worth it. Fix both in the same slice as a real mcpServer.js/mcpServer.test.js change, not standalone."
    status: pending
  - id: assign-site-label-placement-complexity
    content: "assignSiteLabelPlacement in apps/web/src/components/metaphorScenes/fusedCompositePlanner.js crossed complexity (19 > 12) via commit 3159665 (Metaphor3D label-lift fix); in this routine's allowedPaths but needs render-and-measure verification (per this file's own CLAUDE.md gotchas on label placement) beyond a same-day slice — filed as issue #422"
    status: completed
  - id: assign-label-ranks-complexity
    content: "assignLabelRanks (fusedCompositePlanner.js) and an arrow function in labelDeclutter.js both crossed complexity (19, 15 > 12) via PR #430 ('rank a composite's names one layer at a time'), landed the same day #422 was fixed by #426 — same file family, same render-and-measure-verification concern as #422 — filed as issue #431"
    status: pending
  - id: officefloorboard-useless-escape
    content: "officeFloorBoard.js:180's SHAPE_OPEN_RE has an unnecessary `\\{` inside its character class (pre-existing since 2026-08-24, no-useless-escape). One-line mechanical fix (`\\{` → `{`), found and verified safe on 2026-08-29 but left unfixed purely to stay inside that firing's maxFiles: 10 budget — pick it up as a trivial addition to any future in-scope slice."
    status: completed
  - id: itemlabel-complexity
    content: "MetaphorSceneChrome.jsx's ItemLabel crossed complexity (16 > 12) via c2f4cd5 (accent draw-order fix, #468/#477); same render-and-measure-verification concern as #422/#431/#465 in this file family — filed as issue #478"
    status: pending
  - id: graph-edit-dispatcher-complexity
    content: "Three complexity violations from this week's graph-edit diagram-type additions were never individually caught: infographicGraphEdit.js's parseCompareForest (15, new 2026-08-29), mermaidClassEdit.js's line-307 arrow function (13, unflagged since 2026-08-27), mermaidSourceLocate.js's peekDiagramDirective (17, grown incrementally as erDiagram/timeline/pie each added a dispatch branch). Absorbed by 2026-08-29's net lint drop so no budget change was needed, but worth a real fix — filed as issue #447."
    status: pending
  - id: fused-composite-planner-split
    content: "fusedCompositePlanner.js grew 1139→1261 lines via #488 (FUSED_SEA_LEVEL_Y, fusedSurfaceHeightAt, routeAlongSurface) — a pure, already-independently-tested, already-imported-from-outside slice that matches an extraction pattern the same PR series already used for metaphorLabelDepth.js. In allowedPaths and a clean item-7 candidate for a Wednesday firing — filed as issue #500 by the review routine, not taken today alongside the same file's max-params fix to keep this run's diff to that file minimal and easy to verify in isolation."
    status: pending
  - id: apps-server-loadserverapp-dead-code
    content: "loadServerApp.js (apps/server/src/loadServerApp.js, from #493's startup-probe split) carries three dead symbols: an unused `dotenv` import, an unreferenced `resolveSessionIdFromRequest`, and a duplicate `const repoRoot` shadowed by `resolveDist`'s own. All free deletions, but the file sits outside this routine's allowedPaths (not under agents/**, routes/**, or mcp/**) — reachable only by deps/resolve/review per routine:guard --reachable. Filed as part of issue #499 (cause 1); not this routine's to fix."
    status: pending
---

# Ledger: `improve`

Durable memory for the [`improve`](../improve.md) routine. One slice per run; push the rest back
into `todos`.

## Locked

Owner decisions this routine must not re-litigate. Add a dated row rather than arguing with one.

| Date       | Decision                                                                                                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-20 | The ratchet **gates no build**. It is this routine's work queue, not a CI check. Two unattended feature automations run here daily; a metric that reddens their build teaches budget-raising, not fixing. |
| 2026-08-20 | An over-threshold file gets an **issue**, never a new `legacy-monoliths.js` suppression. The scoreboard only means something if it can go down.                                                           |
| 2026-08-20 | No lint severity is promoted `warn` → `error` by this routine. ADR-0007 wants a two-week quiet period; present evidence, a human decides.                                                                 |
| 2026-08-20 | No unprompted hub splits. `components/`, `state/`, `routes/` and `mcp/` are forbidden paths for exactly this reason.                                                                                      |

## Open observations

- **2026-08-21 — the browser rung's jsdom fallback reshares the browser's clock.** _(Resolved — see below.)_
  `apps/server/test/anythingRuntimeBrowser.test.js` → "a browser that hangs on startup does not
  reject a valid page" is flaky on cold containers, and unlike the `anythingRuntimeCheck.test.js`
  flake it fails **in isolation**, so the standing "re-run it alone" advice does not identify it.
  Root cause is in the production path, not the test: `runAnythingRuntimeCheck` passes
  `runAnythingJsdomCheck` the same `budgetMs` the browser rung already had, so the fallback has to
  spawn a child process and load its `tsx` import graph inside whatever clock is left. Documented
  in [`docs/agents/sensors.md`](../../agents/sensors.md); filed as `runtime-fallback-budget` below (issue #347)
  because the fix touches `apps/server/src/tools/`, outside this routine's allowlist.
  **Resolved outside the routine** by `resolveAnythingRuntimeFallbackTimeoutMs`: the fallback now runs on
  its own `ANYTHING_RUNTIME_FALLBACK_TIMEOUT_MS` (default `max(browser budget, 6000 ms)`). It was never a
  flake — the `sensors.md` entry is kept as a worked example of that distinction.

- **2026-08-21 — `verify-ratchet.test.mjs` had quietly turned the ratchet into a hard CI gate, and `main` was red for three merges before this routine noticed.** _(Resolved — see the matching run-log row.)_
  The `2026-08-20` Locked row above says the ratchet gates no build; `verify-ratchet.mjs`'s own
  header comment says the same. But `scripts/verify-ratchet.test.mjs` shipped a test —
  `'the repository currently satisfies its own ratchet'` — that measures the live repo and asserts
  zero violations, and `npm run test:scripts` (hence `npm run test`, hence `npm run check`) runs
  every `scripts/*.test.mjs` file unconditionally. The two statements of intent were never true in
  practice: any regression anywhere in the nine tracked monoliths fails the build for every PR, not
  just this routine's. `DiagramCanvas.jsx`'s growth in `41132a2` (PR #360) tripped it, and GitHub
  Actions confirms `main`'s `CI` workflow has been `failure` on the `test-scripts` job since that
  merge (18:08 UTC 2026-08-21) through #362 and #364 — three merges landed on red without anyone
  fixing it, because merges here aren't blocked on the check passing. Worse, the assertion was
  **structurally unsatisfiable** for a tracked violation: the only ways to make it pass are silently
  raising the budget (forbidden by the `2026-08-20` "no silent loosening" row) or splitting the
  monolith same-day (forbidden by the "no unprompted hub splits" row) — so once one violation existed,
  every future PR would be red forever. Fixed by weakening that one test's assertion (this run's
  one weakened-test budget, with this row as the written reason) rather than deleting it outright:
  `npm run routine:guard -- --postflight` hard-fails on any file whose `test()`/`it()` count falls,
  with no carve-out for README rule 1's one-per-run exception, so a straight deletion would have
  tripped the mechanical guard even though it is exactly what rule 1 permits. Kept the test present
  under an honest name, asserting `compareRatchet`'s return shape rather than zero live violations.
  `verify-ratchet.mjs`'s CLI is unchanged and still the tool this routine calls to read drift.
  **Two lessons for future `improve` runs**: a metric that says "gates no build" is only true if
  something checks that the wiring agrees — candidate `sensor-gaps` task, queue item 4. And
  `routine-guard.mjs`'s test-count check has no way to honor a justified single-test weakening
  short of keeping the test physically present — also worth a `sensor-gaps` look, though
  `routine-guard.mjs` itself is outside this routine's `allowedPaths`.

- **2026-08-23 — `lintWarnings` budgets for `packages/shared` and `apps/server` were wrong from the day the ratchet was created, and nobody had re-verified with `--with-lint` since.** _(Partially resolved — see the matching run-log row and issue #391.)_
  Two prior firings (2026-08-21, 2026-08-22) both ran `verify:ratchet` without `--with-lint` and
  logged "lintWarnings not re-measured this run" rather than skip the note entirely — which is
  exactly the gap that let this sit. Checking out `16b8af1` (the last commit to touch
  `ratchet.json`, itself unrelated to lint) in a throwaway worktree and running
  `npm run lint -w packages/shared` / `-w apps/server` there reproduced 127 and 234 warnings — the
  same counts measured today, before this run's fixes. So the recorded budgets (126, 229) were
  never accurate even at the commit that reasserted them; they trace back to the ratchet's original
  creation commit (`ecac4ea`) and were carried forward unchecked. This is a bookkeeping gap, not a
  regression under this routine's watch, so it does not get the "no silent loosening" treatment the
  same way a fresh drift would — but a budget nobody can trust is only useful once corrected, which
  is why `packages/shared` was fixed to exactly match (a real one-line lint fix, not a budget bump)
  while `apps/server`'s remaining 4-warning gap was left as a visible violation with issue #391
  rather than silently raised. **Lesson for future `improve` runs**: run `--with-lint` every firing,
  not just when "lint numbers are the question" — a metric nobody measures is a metric that can be
  wrong indefinitely without anyone noticing, the same failure class as the `verify-ratchet.test.mjs`
  entry above.

## Run log

One row per firing, including runs that changed nothing. The ratchet numbers are the trend line.

| Date       | Slice taken                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | PR                                                                                                                                                                                                          | Ratchet numbers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-21 | Register accuracy: recomputed CLAUDE.md § File-size budgets and ADR-0005's progress LOC notes against measured values; refreshed balanced-coupling-priorities.md's Implementation progress review date (rows still accurate). Ratchet drift: no violations; suite grew the right way (366→370 files, 3675→3733 cases) so tightened `docs/agents/ratchet.json`'s suite budget to lock it in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | [#346](https://github.com/acuhlmann/mermaid-gen/pull/346)                                                                                                                                                   | monolithLoc: all 9 files exactly at budget (no drift); suite files 366→370 (budget tightened to 370); suite cases 3675→3733 (budget tightened to 3733); strictIslandFiles unchanged (15/22); lintWarnings not re-measured this run                                                                                                                                                                                                                                                                                                                                                                                      | First-ever firing. Left items 3–6 (test hardening, sensor gaps, TS leaves, doc drift) in `todos` for a future run — register accuracy was the highest unfinished queue item and filled the slice on its own. `npm run check` surfaced one red test that is not this diff's (docs-only diff; CI green on the same `main`): documented as a second known flake in `sensors.md` and filed as `runtime-fallback-budget`, since the real fix is outside this routine's allowlist.                                                                                                                                                                                                                                                                                      |
| 2026-08-21 | Register accuracy (round 2, post-#360/#362 landings) + a `main`-CI-red fix: `DiagramCanvas.jsx` had drifted again since the last firing — 1968→1983, past its ratchet budget — and `copilot.ts` (1195→1270) and `officeMomentStore.js` (930→938) were stale in `balanced-coupling-priorities.md` §5's Monolith files table (a register the queue-item-1 text doesn't name by section but is the same class of drift). Corrected all three in CLAUDE.md and balanced-coupling-priorities.md. Filed [#363](https://github.com/acuhlmann/mermaid-gen/issues/363) for the `DiagramCanvas.jsx` regression (traced to `41132a2`, the sequenceDiagram graph-edit slice) rather than raising its budget — left the budget at 1968 so the violation stays visible until the file shrinks, per "no silent ratchet loosening"; suite grew the right way again (370→372 files, 3733→3774 cases) so tightened the suite budget to lock it in. **Also fixed**: discovered while measuring that `npm run check`/CI on `main` has been red since `db80e4f` (PR #360, 18:08 UTC) across three merges (#360, #362, #364) — `scripts/verify-ratchet.test.mjs`'s `'the repository currently satisfies its own ratchet'` test asserted zero live ratchet violations, exactly the hard CI gate the ratchet's own header comment and this ledger's `2026-08-20` Locked row say the design deliberately avoids. That assertion can never pass while a tracked-with-issue violation like #363 exists, short of silently raising the budget (forbidden) or a same-day monolith split (out of routine scope) — a structural deadlock, not a flake. Weakened that one test's assertion rather than deleting it (this run's one weakened-test budget, with this row as the written reason — see Open observations for why a straight deletion would have tripped `routine-guard.mjs`'s test-count check); `verify-ratchet.mjs`'s own CLI/logic is untouched and still what this routine calls directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | [#363](https://github.com/acuhlmann/mermaid-gen/issues/363) (issue); [#366](https://github.com/acuhlmann/mermaid-gen/pull/366)                                                                              | monolithLoc: DiagramCanvas.jsx 1968→1983 (violation, budget kept at 1968, issue filed); all other 8 files unchanged and at budget; suite files 370→372 (budget tightened to 372); suite cases 3733→3774 (budget tightened to 3774); strictIslandFiles unchanged (15/22); lintWarnings not re-measured this run                                                                                                                                                                                                                                                                                                          | `scoreboard-unlisted` todo already had the correct current `officeMomentStore.js` figure (938) queued from a prior observation — folded into this slice's correction rather than duplicating the measurement. Did not touch items 3–6 (test hardening, sensor gaps, TS leaves, doc drift) or the `scoreboard-unlisted`/`lint-promotion-evidence` todos — left pending for a future run. See the matching `2026-08-21` row in Open observations for the CI-red incident detail.                                                                                                                                                                                                                                                                                    |
| 2026-08-22 | Register accuracy (round 3): `DiagramCanvas.jsx` drifted further (1983→1987, still under #363) and `copilot.ts` crossed its budget for the first time (1270→1272, traced to `92e9c06`'s `metaphor3d` branch on `handleUserDiagramEdit`'s content-type allowlist). Corrected both in CLAUDE.md and `balanced-coupling-priorities.md` §5 (also refreshed its Last-reviewed date); all other 7 tracked files and `officeMomentStore.js` unchanged. Ratchet drift: filed [#381](https://github.com/acuhlmann/mermaid-gen/issues/381) for the new `copilot.ts` violation rather than raising its budget (left at 1270, same "no silent loosening" reasoning as #363); suite grew the right way again (372→376 files, 3774→3867 cases) so tightened the suite budget to lock it in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | [#381](https://github.com/acuhlmann/mermaid-gen/issues/381) (issue); [#382](https://github.com/acuhlmann/mermaid-gen/pull/382)                                                                              | monolithLoc: DiagramCanvas.jsx 1983→1987 (violation continues, budget kept at 1968, #363 still open); copilot.ts 1270→1272 (new violation, budget kept at 1270, issue filed); all other 7 files unchanged and at budget; suite files 372→376 (budget tightened to 376); suite cases 3774→3867 (budget tightened to 3867); strictIslandFiles unchanged (15/22); lintWarnings not re-measured this run                                                                                                                                                                                                                    | Register accuracy (item 1) was again the highest unfinished queue item and filled the slice on its own — did not touch items 3–8 or the pending `todos` (`scoreboard-unlisted`, `sensor-floor-inverse`, `sensor-strict-island-sync`, `sensor-guidance-sync`, `glossary-unchecked`, `readme-dup`, `ts-leaves`, `lint-promotion-evidence`, `ratchet-no-gate-sensor`), left pending for a future run.                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-23 | Register accuracy: already current — the same-day #363/#381 monolith split (`16b8af1`) had already synced CLAUDE.md, ADR-0005 and `balanced-coupling-priorities.md` to the post-split measurements (DiagramCanvas.jsx 1889, copilot.ts 1194); verified against fresh measurements, no drift, nothing to correct. Ratchet drift (item 2, first `--with-lint` run in three firings): monolithLoc unchanged and exactly at budget on all 9 files (no violations); suite grew the right way again (376→379 files, 3867→3915 cases), tightened to lock in. `--with-lint` surfaced two real violations — `packages/shared` 127/126 and `apps/server` 234/229 — that a baseline worktree at `16b8af1` (the last commit to touch `ratchet.json`) showed were **already present** at those exact counts, i.e. stale budget bookkeeping from the ratchet's original creation (`ecac4ea`) rather than fresh drift; the two prior firings' rows both note "lintWarnings not re-measured this run", so nobody had caught it. Fixed the one trivial, in-scope warning in each workspace (`no-regex-spaces` in `packages/shared/test/infographicRefinePrepass.test.ts` and `apps/server/test/infographicDslTool.test.js` — both used a literal space run in a regex where `{n}` was clearer): `packages/shared` now measures 126, exactly at budget, no ratchet change needed. `apps/server` now measures 233, still 4 over its 229 budget — the remainder is concentrated in already-tracked monolith files (`copilot.ts` alone carries 73 warnings, mostly complexity/max-lines-per-function) that do not fit a one-slice, in-scope fix, so kept the budget at 229 (no silent loosening) and filed [#391](https://github.com/acuhlmann/mermaid-gen/issues/391); also tightened `apps/web`'s lintWarnings budget 851→846 (a genuine improvement, not investigated further — no violation to explain).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [#391](https://github.com/acuhlmann/mermaid-gen/issues/391) (issue); [#392](https://github.com/acuhlmann/mermaid-gen/pull/392)                                                                              | monolithLoc: all 9 files unchanged and at budget; lintWarnings: packages/shared 127→126 (fixed, matches existing budget); apps/server 234→233 (violation persists, budget kept at 229, issue filed); apps/web 851→846 (improvement, budget tightened to 846); suite files 376→379 (budget tightened to 379); suite cases 3867→3915 (budget tightened to 3915); strictIslandFiles unchanged (15/22)                                                                                                                                                                                                                      | First run to actually exercise `--with-lint` since the ratchet's creation commit — the two prior firings both skipped it. Left items 3–8 and all pending `todos` untouched (`scoreboard-unlisted`, `sensor-floor-inverse`, `sensor-strict-island-sync`, `sensor-guidance-sync`, `glossary-unchecked`, `readme-dup`, `ts-leaves`, `lint-promotion-evidence`, `ratchet-no-gate-sensor`) — a future run should re-run `--with-lint` again (not skip it) so `apps/server`'s real trend is visible, and should treat #391 as a copilot.ts-adjacent candidate for the item-7 coupling-split queue once a clear extraction pattern exists there.                                                                                                                         |
| 2026-08-24 | Register accuracy: already current — all 9 monolithLoc files and `balanced-coupling-priorities.md` matched fresh measurements exactly, nothing to correct. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on all 9 files; suite grew the right way again (379→382 files, 3915→3968 cases), tightened to lock in. `--with-lint` found a fresh regression, not stale bookkeeping this time: `apps/web` moved 846→849 since the last budget update (#392), traced to the flowchart/state parallel-edge disambiguation work (`b1bd846`, `e7042d8`) that grew `renameFlowchartEdge`/`renameStateEdge` from 4 to 6 params (2 new `max-params` warnings) and pushed `FusedCompositeScene`'s render to a complexity of 13 (1 new `complexity` warning). Fixed the in-scope one: extracted `FusedSite` and `FusedAmbience` sub-components out of `FusedCompositeScene` (mirroring the file's own existing `PlannedNode`/`FusedPath` componentisation, so JSX ternaries/`&&`/`??` chains that were inflating the parent's cyclomatic count now live in their own function scopes — ESLint's `complexity` rule counts each function independently, so extraction is the fix, not restructuring logic) — dropped `apps/web` to 848, complexity warning gone. The other two (`max-params`) are structural: the only call site is `adapter.renameEdge(...)` in `apps/web/src/features/canvas/useFlowchartGraphEdit.js`, outside this routine's `allowedPaths`, so grouping the trailing two params into an options object can't be done in one in-scope slice. Filed [#402](https://github.com/acuhlmann/mermaid-gen/issues/402) with the exact fix and kept the budget at 846 (no silent loosening) rather than raising it to 848. `apps/server`'s 233/229 gap (#391) is unchanged and untouched this run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#402](https://github.com/acuhlmann/mermaid-gen/issues/402) (issue); [#403](https://github.com/acuhlmann/mermaid-gen/pull/403)                                                                              | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/web 846→849 (new regression, 1 of 3 fixed in place, 848 measured, budget kept at 846, remainder filed); apps/server unchanged 233 (violation persists, budget kept at 229, #391 still open); packages/shared unchanged 126 (at budget); suite files 379→382 (budget tightened to 382); suite cases 3915→3968 (budget tightened to 3968); strictIslandFiles unchanged (15/22)                                                                                                                                                                       | Register accuracy (item 1) and ratchet drift (item 2) filled the slice — a genuine one-warning fix plus an issue for the structural remainder, not a fresh queue item. Left items 3–8 and all pending `todos` untouched, plus the new `max-params-canvas-edge-rename` todo for #402's follow-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-25 | Register accuracy: already current — all 9 monolithLoc files, ADR-0005 and `balanced-coupling-priorities.md` matched fresh measurements exactly, nothing to correct. Ratchet drift (item 2, `--with-lint`): found and fixed real dead code behind `apps/server`'s persistent 233/229 gap (#391) rather than filing another issue — 11 genuinely unused imports/vars/an uncalled helper function across `chartLangChainAgent.js` (1 unused shared-constant import), `infographicLangChainAgent.js` (an entire uncalled `emitTokens` helper plus its `invokeChatModelToClient` import and a `slot` variable shadowed by a second `stateStore.getSlot` call two lines later), `mermaidLangChainAgent.js` (3 imports already independently re-exported via separate `export {...} from` statements, so the plain imports were pure dead weight), `officePersonas.js` (2 unused schema imports — `office.js` imports the same schemas directly from `@archislop/shared`, not through this file) and `infographicTransformPrompts.js` (an `originalRequest` destructured param no call site ever passes). Verified each removal against grep before deleting (no re-exports, no other in-file uses) and confirmed with a fresh `npm run check` that nothing broke. `apps/server` now measures 222, comfortably under its 229 budget — tightened to 222 and closed #391 (real fix, not stale bookkeeping like the prior two firings' gap here). Two more of the same warning class (`textResult` unused import, a `let appSessionId = null` useless-assignment) live in `mcpServer.js` but were left alone: any diff touching that file trips `check:affected`'s `mcp-tool-without-test` wire-cochange sensor, which hard-fails unless `apps/server/test/mcpServer.test.js` is touched in the same diff — padding that test file just to satisfy the sensor for a pure dead-code removal wasn't worth it, so left as the new `mcpserver-dead-code` todo for a run that's touching that file for a real reason anyway. Locked in genuine suite growth: 382→385 files, 3968→4012 cases. `apps/web`'s 848/846 gap (#402) is unchanged and untouched this run (no in-scope fix available, per #402's own analysis). Also fixed a stale "PR TBD" placeholder on the 2026-08-24 row above (pointing at #403, now confirmed merged).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | [#391](https://github.com/acuhlmann/mermaid-gen/issues/391) (closed); [#413](https://github.com/acuhlmann/mermaid-gen/pull/413)                                                                             | monolithLoc: `mermaidLangChainAgent.js` 1159→1156, `infographicLangChainAgent.js` 863→846 (both from dead-code removal, budgets tightened to match); all other 7 files unchanged and at budget; lintWarnings: apps/server 233→222 (real fix, #391 closed, budget tightened to 222); apps/web unchanged 848 (violation persists, budget kept at 846, #402 still open); packages/shared unchanged 126 (at budget); suite files 382→385 (budget tightened to 385); suite cases 3968→4012 (budget tightened to 4012); strictIslandFiles unchanged (15/22)                                                                   | Register accuracy (item 1) turned up nothing; ratchet drift (item 2) turned a three-firing-old tracked violation into a real fix instead of another status-quo note — the highest-value slice available today. Left items 3–8 and all remaining pending `todos` untouched, plus the new `mcpserver-dead-code` todo for the two warnings this run deliberately didn't touch.                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-26 | Register accuracy: CLAUDE.md § File-size budgets still carried the pre-#413 figures for `mermaidLangChainAgent.js` (~1159) and `infographicLangChainAgent.js` (~863) — `docs/agents/ratchet.json` and `balanced-coupling-priorities.md` had already been corrected to 1156/846 by that run, but the CLAUDE.md prose table was missed. Fixed both. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on all 9 files; suite grew the right way again (385→386 files, 4012→4040 cases), tightened to lock in. `--with-lint` found fresh regressions in two commits landed since the last firing: `b50d077` (sequenceDiagram parallel-edge disambiguation) added a third `max-params` instance on `renameSequenceEdge` — same pattern as #402's `renameFlowchartEdge`/`renameStateEdge`, same blocked call site (`useFlowchartGraphEdit.js`, outside `allowedPaths`) — plus 3 `no-useless-escape` warnings and a new `complexity` violation (19) on `findSequenceMessageRange`, both in `apps/web/src/utils/mermaidSourceLocate.js` (in scope); and `3159665` (Metaphor3D label-lift fix) pushed `assignSiteLabelPlacement` in `fusedCompositePlanner.js` past `complexity` (19), in scope but judged too risky for a same-day mechanical extraction given this file's own CLAUDE.md history of label-placement bugs that only render-and-measure caught. Fixed the two in-scope, low-risk items in `mermaidSourceLocate.js`: removed the three unnecessary regex-class escapes (`\=`, `\~`, `\[` → `=`, `~`, `[`, no semantic change) and extracted `findSequenceMessageRangeById` + `collectSequenceMessageMatches` out of `findSequenceMessageRange` (pure code motion, mirrors the file's existing per-branch function shape), which cleared that function's complexity warning entirely. Verified with the existing `mermaidSourceLocate.test.js`/`mermaidSequenceEdit.test.js`/`useFlowchartGraphEdit.test.jsx` suites (90/90 passing, unchanged assertions). `apps/web` dropped from 854 to 850, still 4 over its 846 budget (2 from #402's original pair, 1 new `renameSequenceEdge` instance added to #402, 1 new `assignSiteLabelPlacement` instance filed separately as #422) — kept the budget at 846 rather than raising it, per no-silent-loosening.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | [#402](https://github.com/acuhlmann/mermaid-gen/issues/402) (updated); [#422](https://github.com/acuhlmann/mermaid-gen/issues/422) (issue); [#423](https://github.com/acuhlmann/mermaid-gen/pull/423)       | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/web 848→854→850 (net +2 real regression this firing, 4 of 6 new warnings fixed in place, budget kept at 846, remainder split across #402 and new #422); apps/server unchanged 222 (at budget); packages/shared unchanged 126 (at budget); suite files 385→386 (budget tightened to 386); suite cases 4012→4040 (budget tightened to 4040); strictIslandFiles unchanged (15/22)                                                                                                                                                                     | Register accuracy (item 1) caught a doc-sync gap the 2026-08-25 run's own fix had left behind — a reminder to diff CLAUDE.md itself, not just trust the ratchet budgets it should mirror. Ratchet drift (item 2) was the highest-value slice: two commits since the last firing regressed `apps/web` lintWarnings again, and this run fixed what fit safely in scope (4 of 6 new warnings) rather than filing everything. Left items 3–8 and all remaining pending `todos` untouched, plus the new `assign-site-label-placement-complexity` todo for #422's follow-up.                                                                                                                                                                                            |
| 2026-08-27 | Register accuracy: already current — all 9 monolithLoc files, ADR-0005 and `balanced-coupling-priorities.md` §5 matched fresh measurements exactly, nothing to correct; refreshed `balanced-coupling-priorities.md`'s Last-reviewed date. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on all 9 files; suite grew the right way again (386→387 files, 4040→4066 cases), tightened to lock in. Confirmed #422 was already closed (fixed upstream by `b53b99e`/#426 — `assignSiteLabelPlacement`'s complexity dropped to trivial), but the very next commit that day, `885d215` (#430, "rank a composite's names one layer at a time"), immediately regressed the same file family with two new `complexity` violations: `assignLabelRanks` (fusedCompositePlanner.js, 19) and an arrow function in `labelDeclutter.js` (15) — same render-and-measure-verification concern as #422, filed fresh as #431 rather than reopening #422. Also found and fixed two unrelated, genuinely dead-code warnings the same `--with-lint` pass surfaced: an unused `parseSyntax` import in `apps/web/test/mermaidClassEdit.test.js` and an unused `isFlatArrayInfographic` helper in `apps/web/src/utils/infographicGraphEdit.js` (grepped first to confirm no other references before deleting either). And fixed the in-scope `deleteClassNode` complexity warning (16, new from the same-day classDiagram graph-edit feature, `779b175`) by extracting `lineReferencesClassId` then merging it with the relation-check branch into a single `classEditLineMatchesId` helper (mirroring the file's own `collectClassEdgeRefs` extraction precedent one function down) — pure code motion, verified behavior-preserving against the existing 8-case `mermaidClassEdit.test.js` suite, complexity warning fully cleared (not just reduced). `apps/web` dropped from 854 to 851, still 5 over its 846 budget (#402's three `max-params` instances, unchanged; #431's two new `complexity` instances) — kept the budget at 846 rather than raising it, per no-silent-loosening.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | [#431](https://github.com/acuhlmann/mermaid-gen/issues/431) (issue)                                                                                                                                         | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/web 854→852→851 (2 dead-code fixes + 1 complexity fix, #422 already resolved upstream, new #431 filed for its 2 successor warnings, budget kept at 846); apps/server unchanged 222 (at budget); packages/shared unchanged 126 (at budget); suite files 386→387 (budget tightened to 387); suite cases 4040→4066 (budget tightened to 4066); strictIslandFiles unchanged (15/22)                                                                                                                                                                    | Register accuracy (item 1) turned up nothing; ratchet drift (item 2) was the slice — three real fixes (2 dead code, 1 complexity extraction) plus one fresh issue for what genuinely needs render verification. Marked `assign-site-label-placement-complexity` completed (resolved upstream by #426) and added `assign-label-ranks-complexity` for #431's follow-up. Left items 3–8 and all other pending `todos` untouched.                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-28 | Register accuracy: already current — all 9 monolithLoc files matched fresh measurements exactly, nothing to correct; refreshed `balanced-coupling-priorities.md`'s Last-reviewed date. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on all 9 files; suite grew the right way again (387→389 files, 4066→4085 cases, one of the new cases from this run's own regression test), tightened to lock in. A full `--with-lint` pass measured `apps/web` at 854 (up from 851 last firing) and, on inspection, the extra warnings weren't #402/#431 growing further — both were re-confirmed present and unchanged (3 `max-params` in `mermaidFlowchartEdit.js`/`mermaidStateEdit.js`/`mermaidSequenceEdit.js`; `complexity` on `assignLabelRanks` and the `labelDeclutter.js` arrow function) — they were three separate, previously-unfiled issues. The most serious: `OfficeLayer.jsx`'s `handleMessageSomeone`/`handleStartThread`/`handleFloorMessage` all called `setMessengerOpen(true)`, a function that was never defined anywhere in the file (`no-undef`, not a style nit) — the panel's open/closed state is actually derived from `deskCommsUiStore` and opened via `openDeskCommsPanel('slopChat', ...)`, the pattern every sibling handler in the same file already uses. This is a live, user-reachable crash: picking a colleague from Slop Chat's empty-thread "New message" flow, or clicking "Message" on someone from the isometric floor, threw a `ReferenceError` instead of opening/staying open on the thread. Wrote `apps/web/test/officeLayerMessenger.test.jsx` first (drives the real "New message" → pick-colleague flow through `OfficeLayer`, asserts no `window` error event fires and the dialog is still open), confirmed it red against the unfixed code (`vitest run` exits 1 on the reproduced `ReferenceError`, even though React 18/19 reports event-handler errors asynchronously rather than letting them propagate through `fireEvent.click` — hence asserting on a captured `window` `'error'` event rather than `expect(...).toThrow()`), then fixed all three call sites to call `openDeskCommsPanel` and confirmed green. Also fixed two smaller, genuine bugs the same pass surfaced: a duplicate-object-key bug in `useVoiceInput.test.jsx`'s `createDeps()` helper, where `promptRef`/`deskPromptRef`/`slopNextPromptRef` were spread into the base object _and_ re-declared after `...overrides`, so the trailing duplicates always won and silently discarded any attempt to override those three refs (no test currently does, so removing the redundant leading declaration was zero-behaviour-change — kept the trailing ones, which is what every current test relies on); and a `max-params` instance on `treeRadialLayout.js`'s local `place()` recursive helper — unlike #402's three instances, every call site is inside the same file (not the blocked `useFlowchartGraphEdit.js`), so the trailing `parentId, startAngle, endAngle` grouped cleanly into an options object. `apps/web` dropped from 854 to 847, still 1 over its 846 budget — the gap is now exactly #402 (3) + #431 (2) minus nothing new, unchanged from last firing; kept the budget at 846 rather than raising it, per no-silent-loosening. `apps/server`/`packages/shared` unchanged (222/126, at budget).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | [officeLayerMessenger.test.jsx](https://github.com/acuhlmann/mermaid-gen/blob/main/apps/web/test/officeLayerMessenger.test.jsx) (regression test, no new issue — #402/#431 already cover the remaining gap) | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/web 851→854→847 (a real functional bug fixed with a red→green regression test, plus 2 more genuine bugs fixed; #402's 3 and #431's 2 re-confirmed open and untouched, budget kept at 846); apps/server unchanged 222 (at budget); packages/shared unchanged 126 (at budget); suite files 387→389 (budget tightened to 389); suite cases 4066→4085 (budget tightened to 4085); strictIslandFiles unchanged (15/22)                                                                                                                                  | Register accuracy (item 1) turned up nothing beyond the date refresh. Ratchet drift (item 2) was the slice, and the highlight wasn't a lint count — a fresh `--with-lint` pass surfaced a real `ReferenceError` crash in the Slop Chat "start a new thread" and floor "Message" flows that had shipped silently since `OfficeLayer.jsx`'s creation, caught it with a red-before-green regression test per README rule 1, and fixed it. Left items 3, 4, 6, 7, 8 and all pending `todos` untouched; #402 and #431 both remain correctly `ready-for-human` — verified still open and unchanged, not re-litigated.                                                                                                                                                   |
| 2026-08-29 | Register accuracy: already current on `docs/agents/ratchet.json`'s tracked figures and `balanced-coupling-priorities.md`; caught one real gap — CLAUDE.md's § File-size budgets still listed `diagramStore.js` at its pre-fix ~955 after this same run's own dead-import cleanup dropped it to 945, fixed within the same slice rather than left for tomorrow's firing. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on the other 8 files; suite grew the right way again (389→392 files, 4085→4122 cases), tightened to lock in. `--with-lint` measured `apps/web` at 850 (up from 847), and — learning last firing's lesson — traced every warning by `git blame` before assuming it was #402/#431 growing: both were re-confirmed present and unchanged (3 `max-params`; 2 `complexity` on `assignLabelRanks`/`labelDeclutter.js`), and the other 3 were two brand-new warnings from today's own timeline/pie/erDiagram graph-edit commits (an unused `eventIndex` in `mermaidTimelineEdit.js`; a `no-useless-escape` on `\|` in `mermaidErEdit.js`'s `ER_CARDINALITY_RE` character class) plus a genuinely dead `row` local in `metaphorLayouts/cityDistrictLayout.js` that had escaped every firing since the 2026-08-24 creation commit. Fixed all three, then kept pulling on the same thread rather than stopping at the day's own regressions: a full re-scan (this time with corrected file-attribution — an earlier `awk` pass in this session had been mis-crediting warnings from files with zero warnings to the next `src/...` header downstream, which is worth remembering next time this kind of manual audit runs) turned up four more genuinely dead, pre-existing, in-scope items nobody had caught: 8 unused imports in `diagramStore.js` (`clearBrowserBackupSessionId`, `clearAllArchislopAppStorage`, `clearAllDiagramCachesFromStorage`, `clearSessionScopedArchislopStorage`, `isDiagramCacheSubstantial`, `PERSISTENT_ARCHISLOP_STORAGE_KEYS`, `readDiagramCache`, `writeDiagramCache`) — the same "already independently re-exported via a separate `export {...} from` statement" dead-weight pattern the 2026-08-25 firing found in `mermaidLangChainAgent.js`; a duplicate, unused `unionSvgBBox` function in `embeddedDiagramFocus.js` (a live copy already exists and is used in `focusDiagramHighlightIds.js`); and one more dead import each in `exportDiagram.js` (`MERMAID_EXPORT_MAX_WIDTH_PX`, same import-vs-re-export shape) and `metaphorLayouts/subwayNetworkLayout.js` (`hash01Salted`, never referenced at all). Verified every deletion against grep before removing it, then a fresh `npm run check` (263 web test files / 2937 tests, 57 server tests, typecheck/strict islands/lint (0 errors)/dep-cruiser/wire/doc-paths all clean) confirmed zero behaviour change. `apps/web` dropped to 836 — comfortably under its 846 budget for the first time in five firings — so tightened the budget to 836 rather than leaving headroom unlocked. A seventh equally-safe fix (a stray `\{` in `officeFloorBoard.js`'s character class, pre-existing since 2026-08-24) and three further complexity violations (`infographicGraphEdit.js`'s brand-new `parseCompareForest`, a `mermaidClassEdit.js` arrow function unflagged since 2026-08-27, and `mermaidSourceLocate.js`'s `peekDiagramDirective` dispatcher grown incrementally across this week's diagram-type additions) were found but left unfixed purely to stay inside `maxFiles: 10` — filed together as [#447](https://github.com/acuhlmann/mermaid-gen/issues/447) rather than silently dropped. `apps/server`/`packages/shared` unchanged (222/126, at budget).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | [#447](https://github.com/acuhlmann/mermaid-gen/issues/447) (issue)                                                                                                                                         | monolithLoc: `diagramStore.js` 955→945 (dead-import removal, budget tightened to match); all other 8 files unchanged and at budget; lintWarnings: apps/web 847→850→836 (2 same-day regressions fixed, 4 older dead-code items fixed, budget tightened 846→836 — the lowest measurement in five firings; #402's 3 and #431's 2 re-confirmed open and untouched, both still `ready-for-human`); apps/server unchanged 222 (at budget); packages/shared unchanged 126 (at budget); suite files 389→392 (budget tightened to 392); suite cases 4085→4122 (budget tightened to 4122); strictIslandFiles unchanged (15/22)    | Register accuracy (item 1) caught one doc-sync gap from this run's own fix, same lesson as 2026-08-26's row — diff CLAUDE.md itself. Ratchet drift (item 2) was the highest-value slice this firing: instead of stopping at today's 2 new warnings, kept digging with corrected file-attribution and found 4 more pre-existing dead-code items nobody had ever caught, dropping `apps/web` to its best-ever measured count and tightening the budget by 10 in one run. Filed #447 for the genuine complexity drift the file budget couldn't reach this time, plus two new `todos` (`officefloorboard-useless-escape`, `graph-edit-dispatcher-complexity`) so neither is lost. Left items 3, 4, 6, 7, 8 and all other pending `todos` untouched.                   |
| 2026-08-30 | Sunday rotation ("3–8, whichever the ledger shows has gone longest untouched"): every prior firing (2026-08-21 → 2026-08-29, ten runs) had picked items 1+2 as the whole slice, so items 3–8 were tied at "never touched" — exactly the failure mode this file's own "why a rotation" note describes. Register accuracy (item 1): already current — all 9 monolithLoc files, CLAUDE.md and `balanced-coupling-priorities.md` matched fresh measurements exactly, nothing to correct. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on all 9 files. `apps/web` measured 837, one over its 836 budget — traced via a throwaway git worktree at `eeab86e` (the commit that set 836) diffed against the current tree: both new warnings trace to `28f5995` (#457, Metaphor3D subway/machine group-name change) — `machineGearLayout.js`'s `machineGearLayout` crossed `complexity` for the first time (15 > 12), and `subwayNetworkLayout.js`'s already-flagged `subwayNetworkLayout` grew worse (21→22). Both are Metaphor3D layout functions in the same render-and-measure-verification category already escalated for this file family (#422, closed by #426; #431, still open) rather than fixed same-day — filed as [#465](https://github.com/acuhlmann/mermaid-gen/issues/465) rather than attempting a same-day extraction; budget kept at 836 (no silent loosening). Then took item 4 (sensor gaps) as today's rotation slice — the first of items 3–8 touched in ten firings: added `scripts/verify-strict-islands.mjs` + `.test.mjs`, asserting `SERVER_STRICT_ISLAND_FILES` (`packages/eslint-config/typeCheckedIsland.js`) matches `apps/server/tsconfig.strict.json`'s `include` list (currently in sync, 22 files each — the sensor's job is to catch future drift, which it does today via a deliberate negative case: added a bogus entry, confirmed `node scripts/verify-strict-islands.mjs` exits 1 with a clear diff and the corresponding test goes red, then reverted and confirmed green). Reused `verify-ratchet.mjs`'s existing comment-stripping JSON-parse idiom rather than adding a JSONC dependency (none added — no new dependencies). Suite grew from the new test file (392→394 files, 4155→4169 cases), tightened to lock in. Full `npm run check` green (264 web test files / 2947 tests, 57 server tests, typecheck/strict islands/lint/dep-cruiser/wire/doc-paths all clean).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | [#465](https://github.com/acuhlmann/mermaid-gen/issues/465) (issue)                                                                                                                                         | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/web 836→837 (new Metaphor3D complexity regression from #457, budget kept at 836, filed as #465); apps/server unchanged 222 (at budget); packages/shared unchanged 126 (at budget); suite files 392→394 (budget tightened to 394); suite cases 4155→4169 (budget tightened to 4169); strictIslandFiles unchanged (15/22)                                                                                                                                                                                                                            | First Sunday firing to actually reach items 3–8: marked `sensor-strict-island-sync` completed. `sensor-guidance-sync` (formatter.cjs GUIDANCE vs guidance.js) and the other pending sensor-gap todos are left for a future Thursday/Sunday firing — item 4 is one sensor per run. Left items 3, 5, 6, 7, 8 and all other pending `todos` untouched.                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-31 | Monday rotation (item 3, test hardening). Register accuracy (item 1): already current — all 9 monolithLoc files matched CLAUDE.md's § File-size budgets and `balanced-coupling-priorities.md` exactly, nothing to correct. Ratchet drift (item 2, `--with-lint`): monolithLoc unchanged at budget on all 9 files; suite grew the right way again (394→396 files, 4170→4194 cases), tightened to lock in. `apps/web` measured 838, two over its 836 budget: #465's two Metaphor3D complexity warnings (`machineGearLayout`, `subwayNetworkLayout`) re-confirmed still open and unchanged; the remaining +1 traced to `c2f4cd5` (#468/#477, the accent draw-order fix) pushing `MetaphorSceneChrome.jsx`'s `ItemLabel` past `complexity` (16 > 12) — same render-and-measure-verification concern as #422/#431/#465 in this file family, filed as [#478](https://github.com/acuhlmann/mermaid-gen/issues/478) rather than same-day extracted; budget kept at 836 (no silent loosening). Picked up the `officefloorboard-useless-escape` todo in this same slice (pre-verified, mechanical `no-useless-escape` fix), dropping the measurement to 837 — still 1 over budget from #465/#478's unfixed remainder. The same commit's `react-hooks/exhaustive-deps` warning (stale `plateOpacity` in the declutter closure) was already filed by the `review` routine as #471 (`ready-for-agent`) — confirmed, not refiled. Then took item 3 (test hardening) as today's weekday slice, applying `review.md` §2's trap checklist to **existing** suites rather than new diffs: swept the whole `apps/web` suite by brace-matching every `act(async () => {...})` block for the `rerender(` + `advanceTimersByTimeAsync(` same-block trap (none found — the fix pattern `useOfficeRunReactions.test.js` established has held everywhere since) and for "a loop over a derived set with no companion non-empty assertion" — found the recurring `expect(new Set(x).size).toBe(x.length)` uniqueness idiom used 11 times across the suite. 4 already carried a companion guard (`castTiers.test.js:111` via `seats.length` bounds, `officeFloorPlan.test.js:275` via `MEETING_SEATS.toHaveLength`, `metaphorGroupIdentity.test.js:172` via `groups.length > 1`, `officeFloorCommute.test.js:112` already fixed by a prior run with the exact pattern below, comment and all); 6 instances across 5 files were genuinely unguarded: `castTiers.test.js` (`ALL_IDS`), `officeImPersistence.test.js` (im-history ids — pinned to the exact expected count of 3, matching the test's own dedup narrative, rather than just non-empty), `officeFloorContracts.test.js` (`FLOOR_PROP_USES` kinds), `officeFloorPlan.test.js` (three instances: `floorSeatIds()`, `FLOOR_SEATS` tiles, `FLOOR_ZONES` ids), and `fusedCompositePlanner.test.js` (`labelRank` set). Added a non-empty (or, where free, exact) companion assertion to each; all five files re-run green (122/122), confirming the additions describe already-correct behaviour rather than surface a live bug. Picked up the `officefloorboard-useless-escape` todo in the same slice (the pre-verified, trivial `\{` → `{` mechanical fix in `apps/web/src/utils/officeFloorBoard.js`, in `allowedPaths`, `officeFloorBoard.test.js` re-run green) rather than leaving it stranded again.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [#478](https://github.com/acuhlmann/mermaid-gen/issues/478) (issue); [#479](https://github.com/acuhlmann/mermaid-gen/pull/479)                                                                              | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/web 837→838→837 (new #478 regression, one mechanical fix landed same slice, budget kept at 836, #465's two + #478's one re-confirmed/newly still open); apps/server unchanged 222 (at budget); packages/shared unchanged 126 (at budget); suite files 394→396 (budget tightened to 396); suite cases 4170→4194 (budget tightened to 4194); strictIslandFiles unchanged (15/22)                                                                                                                                                                     | Register accuracy (item 1) turned up nothing. Ratchet drift (item 2) filed one fresh issue (#478) for a same-day Metaphor3D complexity regression, consistent with this file family's established render-and-measure-verification bar, and confirmed a sibling warning was already the `review` routine's #471 rather than duplicating it. Item 3 (test hardening) was today's real slice — the first Monday firing to reach it — closing a systemic vacuous-pass gap across 6 instances in 5 test files rather than a single one-off, plus the small `officefloorboard-useless-escape` todo. Marked `officefloorboard-useless-escape` completed; added `itemlabel-complexity` todo for #478's follow-up. Left items 4–8 and all other pending `todos` untouched. |
| 2026-09-01 | Register accuracy (item 1): already current — all 9 monolithLoc files matched CLAUDE.md's § File-size budgets and `balanced-coupling-priorities.md` exactly, nothing to correct. Ratchet drift (item 2, `--with-lint`) found four fresh violations, all already traced and filed by the `review` routine as [#499](https://github.com/acuhlmann/mermaid-gen/issues/499) before this firing started — worked #499 instead of re-deriving it. Of its three causes, `routine:guard --reachable` put two inside this playbook's `allowedPaths` and one outside: **fixed** `routePoint`'s max-params (6>5) in `fusedCompositePlanner.js`, introduced by #488 — grouped the trailing four call-scoped params into one options object (mechanical, single in-file call site, no logic change), verified against the file's own geometry-sensitive suite (`fusedCompositePlanner.test.js`/`metaphorGroupIdentity.test.js`/`metaphorFusedRoutes.test.js`, 49/49 green, including tests asserting actual computed station/clearance positions — a stronger guarantee here than a render screenshot for a pure signature change) rather than a full render-and-measure pass; apps/web dropped 838→837, still 1 over its 836 budget from #465's two Metaphor3D complexity warnings plus #478's one (all three re-confirmed open, budget kept at 836, no silent loosening). **Fixed** the contextBytes regression on both root files: two owner commits mirrored new ADR-0017 prose into `CLAUDE.md` and `AGENTS.md` per README rule 8's own duplication mandate, but most of it already restated `docs/routines/README.md` near-verbatim rather than being scoped/domain content — replaced the restated rules with one pointer to the README and kept only what wasn't already there (ratchet-gates-no-build, ADR-0016 powers, night ladder, host split, one-fleet-per-window), in both files identically. CLAUDE.md 42789→38788, AGENTS.md 46011→42119, both back under budget, tightened to lock in. **Not fixed**: `apps/server`'s +2 (dead code in `loadServerApp.js` from #493's split) — `--reachable` puts that file outside this playbook (not under `agents/**`/`routes/**`/`mcp/**`), so it stays deps/resolve/review's; left as a `todo` rather than re-filed (#499 already covers it). Also took the `docs/routines/deps.md`-reachable half of [#498](https://github.com/acuhlmann/mermaid-gen/issues/498) — reachable by `improve` alone, so README rule 2b's "backlog that points at you" put it ahead of today's Tuesday rotation (item 5, TypeScript leaves), which this firing did not reach. #498 asked whether three `deps:`-titled PRs touching `package-lock.json` and shelf docs were the `deps` routine breaching `routine-guard`'s `ALWAYS_FORBIDDEN`/`BUDGET_OWNERS` rules, or needed a §3b carve-out. `git log` showed all four commits in that window are authored directly by the repo owner with no `Claude-Session:` trailer (the marker every actual scheduled firing carries — contrast `review`'s own `8d2088c`), landing the same day the `deps` cron trigger itself was created: owner bootstrap work standing up the playbook and doing the first alert cleanup before any unattended `deps` firing existed, not a routine breach. The routine's own first live run (`ledger/deps.md`'s 16:30Z row) already declined the same shape of fix (a patched `uuid` needing a manifest+lockfile change) as `ALWAYS_FORBIDDEN`, so the guard was never actually broken by an unattended run. Recorded the finding in `docs/routines/ledger/deps.md` and left `deps.md` §3b's hard "never" on `package-lock.json` unchanged — no carve-out, since the "breach" was a misattribution rather than a gap; loosening it would have weakened a real safety property to fix a bug that wasn't there. Left `#500` (`fusedCompositePlanner.js` LOC growth, a clean coupling-split candidate per `review`) untouched — filed by `review`, reachable by `improve`, but deliberately not compounded with today's max-params edit to the same file; queued as a `todo` for a Wednesday item-7 firing. `npm run check` green (267 web test files, 57 server tests, typecheck/strict islands/lint (0 errors)/dep-cruiser/wire/doc-paths all clean). Did not reach items 3/4/6/7/8 or the `ts-leaves` todo — ratchet drift plus the improve-only backlog item filled the slice, per this file's own "they only become the slice when they turn up something that moved the wrong way" rule. | [#499](https://github.com/acuhlmann/mermaid-gen/issues/499) (2 of 3 causes fixed, comment left for the remainder); [#498](https://github.com/acuhlmann/mermaid-gen/issues/498) (closed)                     | monolithLoc: all 9 files unchanged and at budget; contextBytes: CLAUDE.md 42789→38788 (budget tightened to 38788), AGENTS.md 46011→42119 (budget tightened to 42119); lintWarnings: apps/web 838→837 (routePoint fix, still 1 over the 836 budget from #465/#478's unfixed remainder); apps/server unchanged-but-newly-measured-over 224 (dead code from #493's split, budget kept at 222, not this routine's to fix — #499 covers it); packages/shared unchanged 126 (at budget); suite files 396→398 (budget tightened to 398); suite cases 4194→4223 (budget tightened to 4223); strictIslandFiles unchanged (15/22) | Register accuracy (item 1) turned up nothing. Ratchet drift (item 2) was two-thirds of a pre-filed multi-cause issue rather than a fresh derivation — a good outcome of `review` and `improve` both reading the same ratchet output. Also resolved a cross-routine misattribution (#498) rather than let a real safety rule (`deps` §3b's lockfile "never") get second-guessed on bad evidence. Added `fused-composite-planner-split` and `apps-server-loadserverapp-dead-code` todos for the two pieces left for other days/routines. Fixed the 2026-08-31 row's stale "PR TBD" placeholder (now confirmed #479). Left items 3, 4, 6, 7, 8 and all other pending `todos` untouched.                                                                              |
| 2026-09-02 | Wednesday rotation (item 7, coupling splits). Register accuracy (item 1): already current — all 9 monolithLoc files matched CLAUDE.md's § File-size budgets and `balanced-coupling-priorities.md` exactly, nothing to correct. Ratchet drift (item 2, `--with-lint`): nothing to correct — all 9 tracked monoliths unchanged and at budget; `apps/server`'s 224/222 lintWarnings gap unchanged (#499's known remainder, outside this playbook's `allowedPaths`); `packages/shared`/`apps/web` at budget. The queued item-7 candidate (`fused-composite-planner-split` todo, issue #500) was already closed upstream by the time this firing ran, so per README rule 2b ("the backlog that points at you") took the two open issues reachable by `improve` alone instead: **#462** and **#473**, both the same shape of gap in `scripts/test-affected-lib.mjs` — a bundle's "every listed path exists" direction was guarded, "every suite on disk is listed" was not. Fixed #462 (`METAPHOR_BLAST_TESTS` missing 4 suites accumulated since filing: `metaphorDrawOrder`, `metaphorFusedRoutes`, `metaphorGroupPlacards`, `metaphorLabelDepth`) and #473 (`scripts/verify-strict-islands.test.mjs`, added by #466, was selected by neither `packages/eslint-config/typeCheckedIsland.js` nor `apps/server/tsconfig.strict.json` — the drift it exists to catch stayed green under `test:affected`/`pre-push`, red only in full CI). Same sweep confirmed the ledger's own `sensor-floor-inverse` todo was real: `ISOMETRIC_FLOOR_BLAST_TESTS` had the identical one-directional gap (10 `officeFloor*` suites unlisted) — fixed in the same slice rather than filed separately, and generalised the reverse-direction guard in `scripts/test-affected.test.mjs` to cover all three bundles instead of a third bespoke test. Deliberate negative case for both #462/#473 fixes (README rule 1 / `improve.md` § 4): reverted each, watched the corresponding new test go red, restored, confirmed green. Landed as [PR #515](https://github.com/acuhlmann/mermaid-gen/pull/515), CI green (11/11 checks), squash-merged. `npm run check` green both before and after (267 web test files / 2993 tests, 57 server tests, typecheck/strict islands/lint/dep-cruiser/wire/doc-paths all clean). contextBytes ticked down from the merge itself (38788→38786, 42119→42117) and suite.cases grew from the two new reverse-direction tests (4223→4236) — both genuine improvements, tightened `docs/agents/ratchet.json` to lock in as a same-day follow-up commit (register/ratchet-only, no additional `allowedPaths` file touched). Left items 3, 4, 6, 8 and all other pending `todos` untouched; added `floor-tests-doc-rows` (6 newly-listed floor suites need rows in `docs/agents/isometric-floor-tests.md` — a clean item-6 slice) and `routine-guard-postflight-message-test` (#475's still-open first finding, needs the CLI-spawn-and-assert-stdout approach since `scripts/routine-guard.mjs` itself is `ALWAYS_FORBIDDEN`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | [#462](https://github.com/acuhlmann/mermaid-gen/issues/462) (closed); [#473](https://github.com/acuhlmann/mermaid-gen/issues/473) (closed); [#515](https://github.com/acuhlmann/mermaid-gen/pull/515)       | monolithLoc: all 9 files unchanged and at budget; lintWarnings: apps/server unchanged 224 (violation persists, budget kept at 222, #499's known remainder); apps/web unchanged 835 (at budget); packages/shared unchanged 126 (at budget); contextBytes: CLAUDE.md 38788→38786, AGENTS.md 42119→42117 (both tightened to lock in); suite files unchanged 398 (at budget); suite cases 4223→4236 (budget tightened to lock in); strictIslandFiles unchanged (15/22)                                                                                                                                                      | Register accuracy and ratchet drift (items 1/2) turned up nothing to fix. The Wednesday rotation's own candidate was already resolved, so README rule 2b's improve-only backlog filled the slice instead — the same "every listed path exists but not every path on disk is listed" gap shape recurred twice (metaphor, strict-island) plus a third instance found by generalising the fix (isometric floor) rather than three separate one-off patches. Marked `sensor-floor-inverse` completed.                                                                                                                                                                                                                                                                 |
