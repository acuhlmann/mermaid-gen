# Ledger: `metaphor3d`

Durable memory for the Metaphor3D feature automation. Read the last three rows before starting;
they are what stops a cold-start run from re-polishing the corner the last one polished.

## Locked

| Date       | Decision                                                                                                                                                                                                                                                                                                                |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-30 | Playbook created. This automation ran from 2026-08-18 with its instructions in the cron blob and no ledger, budget or guard — ADR-0014 named it as one of the two jobs the shelf had not yet absorbed.                                                                                                                  |
| 2026-08-30 | Read **`expectationMatch`**, never `acceptRate`, from `benchMetaphor.js`. The accept rate is a property of the corpus and falls as the corpus grows, because each new fixture is a must-reject case. Same rule as the `anything` automation's, and for the same reason.                                                 |
| 2026-08-30 | Visual work is gated by a **screenshot in the PR body**, not by a path ban. Banning `.jsx` would be mechanically enforceable and would delete this automation's proven capability — the scene files are where the visual work is. `review`'s Spec axis is the check on an unevidenced claim.                            |
| 2026-08-30 | Findings go in **this ledger** first. Only a rule a future agent would otherwise rediscover the hard way graduates to `CLAUDE.md` / `AGENTS.md`. Twelve nights of findings went straight to the root docs and grew the Metaphor3D section to 54 KB + 27 KB mirrored, paid by every agent session in the repo.           |
| 2026-08-30 | Two MCP connectors (Google-Drive, `imagine_mcp`) were attached to this routine's trigger and used by nothing. Removed. `allowed_tools` was also missing `Skill`, `Task`, `BashOutput` and `KillBash`, so the routine **could not invoke the `verify` skill** written for it, nor run a background dev server. Restored. |

## Run log

Append one row per firing, including quiet runs.

| Date       | Slice                                                                                                                                       | Bench (`expectationMatch` / cases) | Viewports captured                                            | PR   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-30 | Queue 2 — group placards off their own members (subway + machine); carried queue 1's two in-budget doc fixes                                | 100% / 13                          | 390x844, 717x512, 1440x900 — both kinds, before and after     | #457 | Subway route names sat at `getPoint(1)` (their own terminus) and machine axle names at `-radius * 0.78` (far edge, inside the bed): the last two violators of a rule city/garden/fused/archipelago were each fixed for. Readable labels 31/51 → 42/51. Last three runs were all composite, so composite was off-limits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-31 | Queue 2 — the accent callout stopped eating the name of the item it marks (all 15 kinds, one draw-order ladder)                             | 100% / 13                          | 390x844, 717x512, 1440x900 — 6 kinds x 3, before and after    | #468 | The ledger's standing theory ("the accent stem is why 'Pay' renders 'P y'") was **half right and misattributed**. Three separate mechanisms, each invisible to the check that catches the others: the callout painted amber over the name (8.2% of the name's own area, 13/18 cases >1%, worst 30%); the stem was `depthTest:false` with `depthWrite` left TRUE, so it DELETED glyphs with no coloured pixel where the letter was; and the subway's amber route tube ends at its terminus and rises toward the camera, standing in front of the very name it terminates at — present with the whole callout hidden. Fix: a shared draw-order ladder (`metaphorDrawOrder.js`), `depthWrite={false}` on stem and pin, the accented name drawn last and depth-free, opaque chip. Accented names rendering every glyph: **10/18 → 18/18**.                                                                                                                                                             |
| 2026-09-01 | Queue 3 — the fused composite's journey layer stopped flying: routes are solved against the world's surface, and gained an ink of their own | 100% / 13                          | 390x844, 717x512, 1440x900 — 3 fixtures x 3, before and after | #488 | Last two runs were both queue 2, so queue 2 was off-limits. The river/subway spline ran straight from one island crest to the next, so it held island-top height over open water: mean clearance over the ground beneath **0.55–0.72** world units (worst 1.69), **42–86%** of each route more than a channel width clear. After `routeAlongSurface`: **0.154–0.157** mean, 0.35 worst, **0% aloft**. Two things this cost a round each to learn. A smoothstep **dome** is the right surface model — a cone or `sqrt` hemispheroid has its steepest slope at the rim, which is where the samples are. And the first correct-looking version made the journey **disappear**: the channel and the ocean were both `theme.waterColor`, so altitude had been the only thing separating them; `riverDeepColor` at a 1.7:1 floor fixed it, while the 3.4:1 type bar came back near-white and read as foam. Framing and labels measured neutral (subject pixels up in 8 of 9 shots; net +1 legible name). |

| 2026-09-02 | Queue 2 — a route name moved off its terminus platform and onto its own track, and stopped deleting the station it stood beside | 100% / 13 | 390x844, 717x512, 1440x900 — 3 fixtures x 3, before and after | #505 | Last run was queue 3, so queue 3 was off-limits. Closes `accent-vs-tube`, and the tube was **not** the mechanism: the pinned route sign was. Baseline **58 of 66** station names drawn, **17 of 24** terminus names — every route's destination missing on some viewport. Hiding the signs and re-probing returned **24/24** termini and 65/66 names, naming the culprit in one run. After moving the sign alongside its track: **65/66** names, **24/24** termini, **24/24** route names still drawn — the signs-hidden ceiling, with the signs kept. Two candidate placements were measured and thrown away (first-that-clears traded 7 termini for 3 mid-route names; an x-squeezed metric, a strictly better model of label shape, cost three names). The probe reads troika `fillOpacity` off the live scene, which needs no camera and so cannot repeat the projection lie the `verify` skill warns about. |
| 2026-09-03 | Queue 3 — fused composite landmark labels stopped stacking on top of their neighbours: each name now stands past the site's shoulder on its own bearing | 100% / 13 | 390x844, 717x512, 1440x900 — 3 fixtures x 3, before and after | #519 | Last run was queue 2, so queue 2 was off-limits. The label metric was the same troika-`fillOpacity` sweep as the subway slice — no camera involved, so no projection lie, but I ADDED `reducedMotion: 'reduce'` to the browser context. `MetaphorIntro`'s 1.4s auto-rotate reads `matchMedia('(prefers-reduced-motion: reduce)')` and skips; without it the camera was still creeping when I sampled, and back-to-back runs of the same three fixtures at three viewports drifted by up to two labels per viewport. Stable now — three runs match to the label. Baseline **88 of 148** legible (59.5%); after: **100 of 148** (67.6%), **+12 with zero regressions and no trades net** (festival desktop hit 16/16). Mechanism: `fusedCompositePlanner.js` `makeNodes` shifted a landmark's `labelOffset` from a fixed 0.58 world units to `max(0.85, site.radius * 0.6)`, and when a landmark sits at its site's centre (the 0.12 lower bound in `nodePosition`), the bearing is now a golden-angle walk by node index instead of the (0,0) origin. Two landmarks 90° apart on the same island now land ~2r apart on screen, so the pair no longer contests one slot. Visible in the toaster-desktop shot: **Chrome Throne** and **Forgiveness Lever** — both towers had been invisible on every previous run's screenshot; both are labelled now. In the commerce-desktop shot: **Inventory Service**, **Checkout** and **Browse** are all newly labelled. |
| 2026-09-04 | Queue 2 — an interchange writes its members' compound name, so a stop that shares a platform is no longer deleted from the picture | 100% / 13 | 390x844, 717x512, 1440x900 — 3 subway fixtures x 3, before and after | #534 | Last run was queue 3, so queue 3 was off-limits. Closes `interchange-loses-a-name`. The defect was structural, not a frame's declutter verdict: only `station.primary` drew a label, so **6 of 29 authored names had no troika `Text` in the scene at any viewport** — "Pack", "Preferences", "Semantic layer", "Feature store", "Serve", "Where is it?" were reachable only by hover. `subwayStationTitle` joins the members with `\n`, deduped in authored order (the canonical prompt example labels both members "Auth", and "Auth / Auth" is worse than the suppression). Two costs a multi-line label carries, both in `labelRoles.js`: `labelPlateEm` now takes **longest line for width, line count for height** (the joined string would claim a box three names wide; one line's height under-claims exactly the half the pass then lets a neighbour into), and `labelStackLiftEm` lifts the block half a line per extra line from INSIDE the screen-constant group, with the declutter registering that lifted group instead of the Billboard — troika anchors at the block's middle, so without it "Ship / Where is it?" grew down onto its own white platform, which the probe scored buried and the screenshot showed smeared across the rim. Measured: names legible **60/87 → 78/87**, every one of the nine cells improved or tied, buried 1 → 0, structurally-absent **18 → 0**. Method finding worth more than the slice: **`reducedMotion: 'reduce'` is necessary and not sufficient — a 5 s settle is too short.** Two 5 s runs scored 68 and 70 and disagreed on five phone names; the same two runs at 9 s returned 78 and 78 cell-for-cell, and the higher number is the settled one, so a 5 s baseline understates the fix. The presence metric (does a name have any `Text` at all) is camera- and declutter-independent and was stable across all four runs. |

| 2026-09-05 | Queue 6 — a relation is tappable, so the five `canLink` kinds' `renameEdge`/`deleteEdge` stopped being dead code | 100% / 13 | 390x844, 717x512, 1440x900 — 3 kinds x 3, before and after | #550 | Last run was queue 2, so queue 2 was off-limits; queue 6 had been open since 2026-09-01 waiting on exactly the § 3 proof, not on the diff. **A raycast against a fat pick tube cannot work here and the reason generalises**: a tube's radius is a WORLD size, these scenes run 14–60 units, and a tap target has to be ~24 CSS px on a phone whichever is on screen — the radius that gives a city link a thumb-sized target swallows two neighbouring layercake slabs. Projecting the route's own polyline and measuring point-to-segment distance in PIXELS puts the tolerance in the unit it is specified in and is a pure function of numbers. **"A link wins only when no item was hit" is free, not arbitrated**: the whole test runs from the canvas's `onPointerMissed`, which R3F raises only when nothing with a handler was hit and the release landed within 2 px of the press — `HoverableItem` stops its own pointer events, so a tap that reaches the picker is by definition one no item claimed. Measured, camera-free: baseline **0 of 21** link/viewport cells had any pickable route at all; after, **20 of 21** pick as `kind:'edge'`, every one of them the link aimed at, with the sky-blue highlight a **mean 0.59 px and at worst 2.6 px** from the tap pixel — and 9 of 9 empty-sky controls pick nothing. The 21st is `machine` at 390x844: the belt between two gears is entirely over the gears, so the gear wins all ten aim points; honest, and the item rule working. |

| 2026-09-06 | Queue 2 — the accent callout is sized for the reader, and left the camera fit it had been feeding | 100% / 13 | 390x844, 717x512, 1440x900 — 4 fixtures x 3, before and after | #593 | Last run was queue 6, so queue 6 was off-limits. Went in for `unaccented-crosses-the-rod` and **closed it as a non-event**: flipping the stem's `depthWrite` live on the settled scene changed **0 pixels** in all 9 baseline cells (control: flipping `depthTest` on the same meshes moved 292 px, so the toggle was live). The inversion #474 recorded is real in principle and unobservable in practice — a rod 1–4 px wide has to be crossed by a depth-tested chip that is not itself blocked, and no fixture manages it. The probe that answered it also found the bigger defect: **the marker's pin head measured 8 px wide on a 717x512 city cover and 9 px on a phone, against 43 px for a subway on desktop** — the callout was the last thing in the renderer still authored purely in world units, and these scenes run 14–60 units. Fixed by two screen-space bounds in a new `accentRodScale.js` (head floored at 22 px, never capped; stem held at 80 px both ways) plus `FRAME_IGNORE_DATA` on the rod **and on the caption plate, which had been screen-constant and inside `collectFramePoints` since it shipped**. Measured: pin head **5–43 px → 21–50 px** across 12 cells, 11 of them landing 21–31; stem capped where it ran off frame (subway desktop 168 → 119) and grown where it was too short (city phone 28 → 78); marker ink 5,223 → 7,942 px. Taking the callout out of the fit is the larger win in the pictures — the subway desktop went from occupying half the canvas to filling it. Three method costs, all in the domain file: a diff mask over emissive geometry measures the **bloom** (the pin's mask box read 200x178 px for a 21 px pin); the fit feedback is only visible against a **same-code control** (`CakeSprinkles` re-randomises per mount, 15.7k px of self-difference — the identical figure the before/after diff reported); and one uniform scale cannot serve both bounds (the galaxy wanted a wider head and a shorter stem; `min(grow, cap)` gave it a 3 px head). |

| 2026-09-07 | Queue 6/3 — the fused world's wires became tappable, and only the ones an editor can actually rename | 100% / 13 | 390x844, 717x512, 1440x900 — 3 composite fixtures x 3, before and after | #598 | Last run was queue 2, so queue 2 was off-limits. Closes `link-pick-fused`, the half of `link-pick-composite` #557 left behind. Baseline: **0 of 9 fixture/viewport cells had a single pickable route** — the fused plan is the default composite layout, so composite link editing shipped in #557 reaching exactly the `adjacent`/`overlay` branch nobody uses. After: **9 of 9**, every authored link and no inferred one (6/6, 5/5, 5/5). Of 144 taps aimed three-per-route, **36 picked the aimed link, 0 picked a different link, 0 picked nothing**, highlight 0.00 px from the tap pixel, and **0 false picks on 27 empty-sky controls** — all 108 misses were an item under the aim point taking the pointer first, which is the item-wins rule, not the picker. Reachable link/viewport cells 22 of 48, falling 10/16 desktop → 9/16 cover → **3/16 phone** (filed as `link-pick-fused-phone`; the fix is a route standing off the island rim, not a picker change). Three method costs, all now in the domain file. The `verify` skill's `updateProjectionMatrix` camera capture is the wrong camera here; `scene.onBeforeRender` is the render camera by construction. The link store is gated on `!onSelectedNodeChange`, so a harness that omits the app's selection wiring reads every route as unpickable — indistinguishable from the feature not shipping, and it cost a full round. And **the domain file's "wait for the routes to stop moving" rule was already written and I did not read it first**: `<Bounds>` is still flying at 9 s (authored `[18,14,18]` through 6 s, settled `[13.383,10.285,10.513]` by 9 s), and a 9 s read put every aim 20–69 px off a wire with entirely plausible-looking numbers. A four-line "two reads 1.2 s apart within 0.5 px" gate converged on the first retry in all 9 cells. Side effect worth keeping: extracting `fusedLinkColors`/`fusedLinkStroke`/`fusedLinkShowsPulse` took the links callback from complexity 17 to `FusedLinkRoute` at 10, so `apps/web` loses one lint warning — `docs/agents/ratchet.json` is outside this shelf's paths, so `lintWarnings.apps/web` is `blocked-by-paths` for `improve`. |

| 2026-09-08 | Queue 2 — the tree stopped being erased by its own sky: a kind that paints daylight now takes the light, the environment map and the bloom threshold with it | 100% / 13 | 390x844, 717x512, 1440x900 — 4 themes x 3 (tree) + 3 fixtures x 3 (city / river / tree-dominant composite), before and after | [#609](https://github.com/acuhlmann/mermaid-gen/pull/609) | Last run was queue 6/3, so 6 and 3 were off-limits. Not a queued todo — found in the preamble captures, and much bigger than anything on the list: **the tree scene was destroyed on 3 of the 4 themes and had been since the themes' postfx blocks were written**. `TreeScene` locks its palette privately, `TreeSky` paints `#e8f4e8` (~0.94 sRGB luminance) on every theme, and the renderer's theme ladder — a chain of `else if` over `river`/`garden`/`archipelago` — had **no `tree` branch**, so `resolveMetaphorPostfx` kept reading the dark theme's bloom cut-off: noir 0.78, arcade 0.72, blueprint 0.86, all **below** the backdrop the scene actually draws. The sky passed its own bloom filter and the radius smeared it over the subject. Measured over the canvas minus the chrome panels: `nearWhite` (luminance > 235) **0.20–0.32 → 0**, `chroma` (hued pixels) **0–0.14 → 0.208–0.368** (matching whiteboard exactly), contrast σ **16.3–32.0 → 34.4–49.5**, and **0 → 1359–6881 pixels of accent amber on all 9 previously-erased cells**. It reached composites too: a tree-dominant composite went amber **0 → 818/1167/1621**, contrast **26.0 → 87.2** on a phone. No-regression controls are pixel-identical: a noir city (amber 1304→1303, contrast 23.35 both) and a noir river (302→302, 26.0 both). Three method notes now in the domain file. **`nearWhite` + `chroma` is the washout metric and neither touches the camera** — a bloomed-out scene is bright and colourless, not dark or blank, and contrast alone cannot tell it from a legitimately flat one. **Zero accent-amber read as "the marker regressed" and was not**; the whole scene was gone, so classify the picture before the object. And the fix that matters is not the constant: the ladder is now a `DAYLIGHT_LOCKED_KINDS` table in a new `utils/metaphorSceneTheme.js`, because the bug was an **omission from a list** that lived inline in a 1200-line component where its only reader was a `useMemo` and no test could see it. `resolveTreeNatureTheme` gained a `treeNatureLocked` idempotence guard so `TreeScene`/`TreeSky` can go on resolving it. Side effect: `MetaphorRenderer.jsx` −43 lines and `apps/web` −2 lint warnings (99 → 97 across the two touched files); `docs/agents/ratchet.json` is outside this shelf's paths, so `lintWarnings.apps/web` and the `MetaphorRenderer.jsx` monolith LOC are **blocked-by-paths** for `improve`. |

| 2026-09-10 | Queue 3 — the fused world stopped being a daylight sky over a hole: a locked atmosphere now raises the surfaces it lights | 100% / 13 | 390x844, 717x512, 1440x900 — 4 composite fixtures x 4 themes x 3, plus 18 base-kind control cells, before and after | [#626](https://github.com/acuhlmann/mermaid-gen/pull/626) | Last run was queue 2, so queue 2 was off-limits; took the `composite-daylight-ground` todo, which had been parked pending a decision about whether a daylight-locked composite should tint its land. The decision, recorded here: **it must, as a floor rather than a tint.** `DAYLIGHT_LOCKED_KINDS` fixed sky, light, env map and bloom; it never touched the palette the bodies are drawn from, and a **base** river or tree does not care (those scenes read `treeMeadowColor`/`waterColor`, which the locks DO rewrite). A **fused** composite draws every grammar at once, so `WorldGround` falls back to `theme.groundColor` — `#020617` on noir — under a `#e8f4e8` sky. `raiseSurfacesForDaylight` (`utils/metaphorDaylightSurfaces.js`, called from the ladder before the mood blend) walks any surface below 0.28 sRGB luma toward the scene's OWN sky horizon and stops at the bar. Measured over 4 fixtures x 4 themes x 3 viewports + 18 controls: `nearBlack` **0.0390 → 0.0008** and `dark` **0.2142 → 0.0225** across the 36 dark-theme composite cells, median luminance on a noir river desktop **0.146 → 0.287**; the 12 whiteboard composite cells identical to 4 dp and the 6 base-city control cells at **0 changed pixels**, both by construction (a colour already clearing the bar is returned unchanged, and the whole theme object by identity). Four method notes, all in the domain file. **Whole-canvas contrast σ FALLS on every fixed cell (0.33 → 0.26) and that is the fix working** — it had been measuring a black hole against a bright sky; scored over exactly the region that was dark before, σ **doubles** (0.030 → 0.064) and tone bands go 4.7 → 6.5, up in 26 of 34 cells and down in zero. **The whiteboard cells are the noise gauge and they earned their keep**: the change provably cannot reach them, yet 18 of 21 differ pixel-wise and one by 8% of pixels at max delta 230 (a label the declutter landed elsewhere) — so a per-cell diff below that is not evidence here, and the statistics, stable to 4 dp on those same cells, are. **A neutral reference was tried and rejected**: it clears the bar and lands all three dark themes on one grey, fixing the incongruence by deleting the last theme signal a daylight world has; the sky reference is aerial perspective, the rule `SoaringBirds`/`recedeTheme` already follow. And **grouping palettes stay out of the key list** — flooring four shades of one hue collapses neighbours, which reads as two territories agreeing; the price is the still-black affinity plate, filed below. |

| 2026-09-11 | Queue 2 — the daylight lock gained its mirror: a kind that paints deep space now takes the sky keys, the bloom cut-off and its ground with it | 100% / 13 | 390x844, 717x512, 1440x900 — 3 fixtures x 4 themes x 3 (before/after), plus a 14-kind x 4-theme sweep at 1440x900 and 12 city control cells | [#638](https://github.com/acuhlmann/mermaid-gen/pull/638) | Last run was queue 3, so queue 3 was off-limits; took the `sky-lock-sweep-other-kinds` todo. **The sweep's first answer is a negative and it is worth as much as the fix**: 14 base kinds x 4 themes at 1440x900, `nearWhite` never exceeds **0.0087** anywhere, so the tree's own class — a scene forcing a BRIGHT sky under a dark theme's bloom threshold — has no second instance; `DAYLIGHT_LOCKED_KINDS` is complete. The inverse class is real and is exactly two kinds. `GalaxySky` (galaxy, and orrery through the same branch) paints from `spaceTopColor`/`spaceHorizonColor` — a **different theme channel** from the `skyTopColor`/`skyHorizonColor` every other sky sphere reads — so nothing was missing from a list and no authored value was wrong: two channels of one theme disagreed, on whiteboard by everything (`#0b1026`/`#2a1050` painted, `#b9cde4`/`#dde5ef` answered), and **six consumers believed the wrong one** — the IBL, the key light's fill and rim, the WebGL clear colour and the PNG export background, `recedeTheme`'s horizon, the bloom threshold, and `isDarkBackdrop`, which gates the accent marker's **additive** glow and was answering "too bright for additive" about a sky at 0.09 luma. Fixed by `SPACE_LOCKED_KINDS` + `resolveSpaceSkyTheme` (sky keys ← the space channel; `SPACE_BLOOM_THRESHOLD` 0.86 as a **cap**, which is blueprint's own value so it is a no-op on three of four presets) and `dimSurfacesForSpace` (a **ceiling** on `groundColor` alone, at 0.35). Measured: base galaxy/orrery whiteboard `glow` **+8–13% in all 6 cells**; the galaxy-dominant composite's median luminance **0.518 → 0.187** desktop and 0.411 → 0.192 cover, `satLit` **+17–18%** at all three viewports; and the noise gauge held — 9 dark-theme space cells and 12 `city` control cells identical to 3–4 dp, city at **0.00–0.02% changed pixels**. Four findings in the domain file, two of which cost a round. **The ceiling's key list had to be much shorter than the floor's**: the floor's argument generalises (a near-black albedo cannot be lit) but the ceiling's does not — a bright BODY in space is a lit object, and blueprint's towers are 0.85/0.91/0.94 by design, so `SPACE_SURFACE_KEYS` is `['groundColor']` alone. And **the pastel whiteboard galaxy is not this bug**: the obvious reading is "daylight is washing the stars out", and locking the hemisphere light's sky term too moved `satLit` by **0.00** on the galaxy at every viewport while costing the composite 0.08 of median luminance — tried, measured, reverted. Side effect: one `no-unused-vars` warning removed from `metaphorThemePresets.test.js` (`DEFAULT_POSTFX` now asserted on); `docs/agents/ratchet.json` is outside this shelf's paths, so `lintWarnings.apps/web` is **blocked-by-paths** for `improve`. |

| 2026-09-12 | Queue 4 — a magnitude carried between two kinds is refitted, not copied: the kind switcher stopped refusing 15 of its 196 pairs and stopped flattening 51 more | 100% / 13 | 390x844, 717x512, 1440x900 — city → layercake / tree / orrery / galaxy / garden, before and after | [#655](https://github.com/acuhlmann/mermaid-gen/pull/655) | Last two runs were queue 2 and queue 3, so both were off-limits; took `switch-to-layercake-refused`. **The todo understated it by an order of magnitude and named the wrong half as the defect.** It recorded one refused pair (`city → layercake`, tall cities only); a 60-line pure-function sweep over all 196 pairs found **15 refused** — every wide-range kind into `layercake`, plus `city → galaxy`, plus five kinds whose SECONDARY encoding can reach zero going into `city`'s `positive()` footprint — and, far worse, **51 pairs that succeed while saturating at the target's ceiling**. A refusal at least says something; a saturated encoding renders a confident, wrong picture with no error anywhere: the metaphor prompt's own worked example switches to a tree as **two identical branches**, and a five-service platform to an orrery as **three identical planets on one ring**. One mechanism behind all of it — `mapItemToKind` read the magnitude in the SOURCE kind's unit (`height` ≤ 100, `thickness` ≤ 10, `magnitude` ≤ 20, `depth` ±1) and wrote it into the target's field, then clamped. Fixed by `fitMagnitudes` (`utils/metaphorMagnitudeFit.js`) fitting the whole SET into the target's declared domain: ratio-preserving when the source's dynamic range fits (**121 of 182 pairs**), order-preserving affine when it does not or the sign domains differ, **and a strict no-op when the values already fit** — which is why all 13 pre-existing switcher tests pass byte-for-byte unmodified and the change is unreachable from any document that already worked. Measured pure: refused **15 → 0**, rank-collapsed **51 → 0**, inverted 0 → 0, secondary flattened 6 → 0 (the 28 remaining are the documented `secondary ?? default` and iceberg's deliberate "nothing honest to claim about what is hidden" guard, not flattening). Measured by render over 15 cells: error cards **6 → 0**, authored names present in the scene **45/75 → 75/75**. Two costs. **Removing a clamp broke a formula that had been reading the old unit**: the orrery's `12 - Math.min(primary, 11)` assumed raw city heights, and against a fitted 0.5–10 size it can never reach ring 1 — replaced by rank, one ring per item. Spending the full 1–12 range was tried FIRST and measured worse (foldable-cover ink **0.0958 → 0.0771, −20%**, the world as wide as the schema allows and every body a speck); one-ring-per-item returned **0.0960**, level with baseline, magnitudes still all distinct. And **the troika-`fillOpacity` legibility probe disagreed with its own screenshots** — 1 of 5 on a layercake desktop that plainly draws all five names — so it was discarded rather than reported; name-presence is camera- and declutter-independent and did agree with the frames. The sweep is now two cases in `metaphorFlatItemsCore.test.js`, and it is what pins `MAGNITUDE_DOMAIN` to the schema: an entry wider than `metaphorSchema.ts` allows makes that pair refuse and fails the suite. Two budget notes. It wanted to live in `switchMetaphorKind.test.js` and **could not** — that file is listed in `METAPHOR_BLAST_TESTS` but outside this automation's `allowedPaths`, while a new `metaphor*` suite is inside the budget and refused by the blast sweep; see `metaphor-suite-blast-list`, both escapes now recorded. And it was written FIRST as 196 separate `it()` blocks, which passed on their own and **broke `radialMenuUx.test.jsx` under full-suite load** — 270/270 on baseline, 269/270 with them, and the radial suite green in isolation either way. A pure-function sweep buys nothing from one `it()` per case and the scheduler load is not free; collapsed to two cases that name the offending pairs in the failure message, the full web suite is back to 270/270. |
| 2026-09-13 | Queue 2 — the one kind whose subject is a landscape got the sky its own haze had always been fading into, and the kind→sky chain became a table | 100% / 13 | 390x844, 717x512, 1440x900 — base terrain x 4 themes x 3 and a terrain-dominant composite x 4 themes x 3 (before/after), plus a 14-kind desktop sweep shot three times | [#672](https://github.com/acuhlmann/mermaid-gen/pull/672) | Last run was queue 4, so queue 4 was off-limits; took the `terrain-has-no-sky` todo. **`terrain` was the one base kind the renderer mounted no sky sphere for**, so the landscape was drawn against the raw `<color attach="background">` clear colour while `AdaptiveFog` faded its distance toward `theme.skyHorizonColor` and `SoaringBirds` lerped its wings the same way — a horizon band fading into something that was never a sky. Fixed with `TerrainSky` (`GradientSkySphere` from the theme's own sky keys + `SkySunGlow`, the pairing `river`/`garden`/`archipelago`/`bridge`/`cycle` already use) and, the actual deliverable, `utils/metaphorSkyBackdrops.js` — the eleven ternaries that chose a sky inside `MetaphorRenderer`'s JSX are now a table swept against `METAPHOR_BASE_KINDS`. Measured over 12 base cells: `nearBlack` noir **0.7925 → 0.1365** phone / 0.7270 → 0.1409 cover / 0.7704 → 0.1710 desktop, arcade 0.7727 → 0.4169 / 0.6845 → 0.3007 / 0.7365 → 0.4470, blueprint 0.7725 → **0.0001** / 0.6929 → 0.0001 / 0.7363 → 0.0002; whiteboard stays at 0.0000 and gains tonal range (263 → 272 distinct sky shades at desktop) because a flat fill became a gradient. **Terrain is deliberately NOT daylight-locked** — its ramp is built from `terrainBaseColor`, which all four presets author, so locking it would have bought the same number and paid the `tree-themes-read-alike` price. Three costs. **The composite probe measured the wrong thing first**: `terrain + subway` resolves to the RIVER atmosphere (subway's capability role is `path`, and `ATMOSPHERE_RULES` matches by role before it reaches terrain's row, which is last), so the first run reported a pixel-identical before/after that read exactly like the fix not reaching composites; only `cycle` and `iceberg` leave a terrain-dominant world with a terrain sky, and with `terrain + cycle` `nearBlack` goes **0.43–0.58 → 0.04–0.36** across the nine dark cells. **The 13 unchanged kinds' before/after diff is not the noise gauge** — archipelago came back 1.379% of pixels changed and galaxy at maxDelta 226, neither reachable by this change; re-shooting the after build against ITSELF reproduced all of it (1.410%, maxDelta 225), so the honest figure is `change − noise`: terrain **84.343% against a 0.001% floor**, zero everywhere else. And **the first sweep printed `done` having written zero screenshots** (`switchMetaphorKind` returns `{ ok, text }`, the generator read `r.source`, `JSON.stringify` dropped every key) — a harness that produces no cells has to throw. Both new findings are in the domain file. Two budget notes: the test cases went into `metaphorThemePresets.test.js` rather than a new file (`metaphor-suite-blast-list`, still blocked-by-paths), and the `ATMOSPHERE_RULES` ordering finding is filed below rather than fixed, because reordering that table changes the sky of every mixed world. |

_Runs from 2026-08-18 to 2026-08-30 predate this playbook; their record is the `Metaphor3D:` commit
series on `main`, and their findings are the Full-findings section of
`docs/agents/domains/metaphor3d.md` (moved out of the root context files on 2026-08-30)._

_Correction (2026-09-06, `resolve`, #527) — on the 2026-09-03 row (#519). Two of its claims are
wrong, verified against `planFusedCompositeWorld` on the fixtures themselves. The degenerate
bearing is NOT produced by "the 0.12 lower bound in `nodePosition`" — that term is what prevents
collapse (radius never below 1.8, so the radial minimum is 0.216, past the 0.05 threshold); the
only reachable entry is an authored `item.position` landing on the site centre, and no shipped
composite contains one — the golden-angle walk was exercised by no test until
`fusedCompositePlanner.test.js` drove it directly. And no two non-accent landmarks share a site
in commerce, festival or toaster, so the "siblings stacked in one slot" mechanism — including the
toaster's Chrome Throne / Forgiveness Lever / Was It My Fault? anecdote (they plan onto three
different sites, and `was-it-my-fault` is a galaxy accent) — is exercised by none of the three
measured fixtures. The 88→100 legible-name lift stands as a measurement; its recorded cause does
not._
| 2026-09-14 | Queue 3 — the fused world's territories stopped being one colour: the grouping ladder is now STOOD against the ground it is drawn on, not taken raw from the palette | 100% / 13 | 390x844, 717x512, 1440x900 — 5 fixtures x 3, before and after, plus a same-build re-shoot for the noise floor and a camera-free plate probe | [#688](https://github.com/acuhlmann/mermaid-gen/pull/688) | Last three runs were queue 2, queue 4 and queue 2, so queue 2 was off-limits; took `daylight-group-plate` + `space-group-plate`, which are **one mechanism** and were both parked on the same wrong premise. The standing rule — "a lock never walks an ordinal palette, because squeezing four shades of one hue collapses neighbours" — does not bind the plate at all: it reads `districtPalette[0]` **alone** and gets its ordinal from `tintByGroup`'s hue ladder, so there is no neighbour to collapse. **And looking for that turned up a bigger one the ledger had never seen.** `LIGHT_LADDER` only ever lifts and whiteboard's entry 0 is luma 0.91, so plate groups 2, 4 and 6 all clamp to `#ffffff` — nearest-pair distance across the eight-group ladder **0.000**, i.e. a four-district city and a four-group fused world drew territories in _one colour_ on the default theme, in the renderer and the composite alike. It survived because `metaphorGroupIdentity.test.js`'s pairwise sweep pins the ladder against `buildingColor`, a mid-tone on all four presets; the plate's base was never swept (`plate-ladder-base-sweep` below carries the two `GROUP_TINT_EARTH` bases that still are not). Fixed by `resolveGroupPlateBase`: the whole band is **placed** one side of the already-locked `groundColor`, clear by `PLATE_GROUND_SEPARATION` (0.13), above by preference and below only when the lift will not fit under `PLATE_LADDER_CEILING` — whiteboard unlocked, and only whiteboard. Only lightness moves, so the theme's hue and the ladder both survive. Measured pure over 4 themes x 8 kinds: whiteboard nearest pair **0.000 → 0.145** unlocked and 0.000 → 0.116 under the space lock; min plate-to-ground luma noir daylight **0.012 → 0.131**, blueprint daylight 0.013 → 0.132, arcade daylight 0.030 → 0.131, whiteboard space **0.562 → 0.134**; no cell regresses by more than 0.001 and unlocked noir returns byte-identical. Measured rendered (plate composited over the ground at its own alpha — camera-free, so not exposed to the projection trap): whiteboard city pair **0.020 → 0.091**, whiteboard galaxy composite 0.0055 → 0.0242, whiteboard tree composite 0.0055 → 0.0254, noir tree composite plate-to-ground **0.0055 → 0.0353**. Three costs. **The band's low end is not its base**: the lift is in HSL lightness and the bar is perceived luma, so a rung that turns hue and gives up saturation lands _below_ its base (whiteboard group 3, by 0.045) — placing from the base alone left it 0.101 from a ground it had to clear by 0.13, and the suite caught it, not a screenshot. **The todo's own proposed answer would have been wrong**: deriving the plate from `groundColor` makes group 0 — the identity rung — exactly the ground, i.e. the first territory invisible. And **a pale authored colour hides its saturation**: `#dbeafe` is `s` 0.945, visible only once darkened, so `PLATE_BASE_MAX_SAT` caps rather than scales (whiteboard and arcade move, noir and blueprint do not — which is what makes the unlocked-noir control byte-identical, 0.001–0.015% of pixels against a 0–0.026% re-shoot floor). Side find, not fixed: a 12-item fused world falls to LOD `low` on `estimatedCost` and **silently drops every affinity ring, placard and pulse** — found only because the probe reported zero plate meshes on a fixture whose plan carried three groups (`fused-lod-drops-groups`). Budget note: the new cases went into the existing `metaphorGroupIdentity.test.js` rather than a new file, `metaphor-suite-blast-list` still **blocked-by-paths**. |

| 2026-09-16 | Queue 2 — the callout leans out from behind the app's own panels, instead of being painted under them | 100% / 13 | 390x844, 717x512, 1440x900 — 15 kinds x cover and 5 kinds x phone/desktop, before and after, plus a same-build re-shoot for the noise floor and a 225k-position camera-free grid | [#703](https://github.com/acuhlmann/mermaid-gen/pull/703) | Last run was queue 3, so queue 3 was off-limits; took `accent-under-the-title-card`, which is the standing brief's "the 3D panels must never collide with the app's own menus" in its purest form. **The two numbers that allow the defect are in different files and neither is wrong.** `sceneFraming.js` reserves `ANNOTATION_HEADROOM_PX` — 26 px, one item label — above the subject's top vertex; `accentRodScale.js` holds the rod at 80 px of stem plus a 22 px head and `MetaphorAccents.jsx` keeps it out of the fit on purpose (#593, and correctly — a world-sized marker in the fit feeds a loop). So the callout stands ~102 px into air nothing reserved, and on a small canvas that air is a panel. Nothing complained because each neighbour is doing its job: the caption yields (`yieldWhenUnreadable`), the item's own name yields at `MAX_COVERED_PINNED` — the two things that would have objected remove themselves — and the stem and pin are geometry drawn under the HTML. Fixed by `accentRodLean.js`: the head's NDC box is tested against `measureChromeRects`' own rects with the declutter pass's own `coveredFraction`, and the rod leans to the cheapest exit, sideways by preference because down spends the standoff the stem exists to create. Measured **pure** over 225,358 head positions on six measured panel layouts: behind a panel **18,457 → 4,721 (74.4% rescued)**, and the base rate is the interesting half — 4.6% of positions on a 1440x900 desktop, 12.1% on a cover, 13.9% on a phone, **18.7%** on a phone carrying a composite's second panel. Measured **rendered** over 25 cells: marker ink behind panels at cover **409 → 107 px**, `river` survival **29.2% → 84.7%** (its residual is the stem's lower run, rising out of an anchor that is itself under the rail), 24 control cells **0 → 0** with marker ink identical to ±4 px. Four costs. **The alternative was measured, not argued**: the rod's height is a pixel constant now, so it COULD be reserved in the fit without the #593 loop — and 102 px of a 512 px cover is 20% of the height, clamped to `MAX_HEADROOM` 10%, charged to every accented scene for a collision that happens in one position in eight; the sweep is what rejected it. **A dodge has to be solved from the un-dodged position every frame** or it oscillates (clear means no lean means covered again), which is why `solveRodAim` reads the rod's PARENT and the unleaned local `(0, len, 0)` — and using the parent is also what makes it right inside a composite layer's transform. **The screen offset is exact and the stem pays**: adding the offset in the camera's screen plane leaves the tip's depth alone, so the rod must reach `sqrt(len² + offset²)`, which is why 38° is chosen against `accentRodScale.js`'s measured 65–116 px band (it takes 80 px to at most 101 px) rather than for looks. And **the bound is stated rather than hidden**: every one of the 4,721 unrescued positions needs **66–69 px** of travel against the 63 px cap, so those keep a vertical rod — a tilted marker still behind the card has spent the one unambiguous thing about it for nothing. Two method notes. **A same-build re-shoot is the only honest noise gauge here and it is large**: the identical build shot twice differs by 1.3–7.1% of pixels at maxΔ up to 216 (garden re-randomises its flowers, the galaxy its stars), so the per-cell pixel diff proves nothing and the marker-ink statistic, stable to ±4 px, is what does. And **do not edit a source file while a render sweep is running against a live vite dev server** — the HMR full-reload destroyed the page mid-probe and the crash (`Execution context was destroyed`) reads exactly like a browser fault; it cost a 32-cell baseline run. Side caveat on the probe itself: it finds the callout by its 5-sided pin cone, which over-matches on kinds that draw other 5-sided cones (garden flowers, a composite's canopies — `groups 2` and `groups 4` in the logs), so those cells' "marker ink" includes a little scene geometry; the behind-panel counts are 0 either way. |
| 2026-09-17 | Queue 3 — a fused world stops losing its territories when it gets big: `low` LOD is a detail budget now, not a content budget | 100% / 13 | 390x844, 717x512, 1440x900 — 2 worlds x 4 themes x 3, before and after, plus a same-build re-shoot for the noise floor and a 1,460-world camera-free planner sweep | [#715](https://github.com/acuhlmann/mermaid-gen/pull/715) | Last run was queue 2, so queue 2 was off-limits; took `fused-lod-drops-groups`, recorded as a side find on 2026-09-14 and never priced. **The defect is one conditional, and what makes it a finding is the company it was keeping.** `<AffinityGroups>` sat behind `lod !== 'low'` beside the shore foam, the island shrubs, the daylight pollen, the soaring birds, the river's motes, the tower windows, a terrace's third tier and an orb's glow — every one of which is finish. The grouping nouns are not: they are the only claim a fused world makes across its layers, and the legend promises the viewer they are drawn. So `low` was a **content** budget for exactly one item on its list. Measured pure over 1,460 planned worlds (3 shipped fixtures + every ordered pair of the 14 base kinds at 2/3/4/5/6/8/10/12 items a layer, each carrying three shared nouns): 1,279 plan at least one affinity group and **465 — 36.4% — drew none of it**, 113 of them at or under the `itemCount > 18` bar. **And the loop is tighter than the rate.** `estimatedCost` sums the primitives, then `links*2 + connectors + groups.length`, so on the 6-tree + 6-city world this todo names the ungrouped plan is **93** and `medium` and the grouped one is **96** — one over the bar. A second shared noun was free and the third was fatal: the grouping paid for its own deletion. The cost term is not wrong (three discs do cost three discs), which is why `compositeLodDetail.js` fixes the gate rather than the sum — there is no longer a tier that deletes meaning, so the loop has nothing to close on. **What `low` drops instead was measured, not argued.** Compositing each ring over the surface it is drawn on at its own alpha, over 4 themes x both surfaces (`waterColor` for an island world, `groundColor` for a plaza), as perceived luma against that surface: interior wash **0.0044–0.0628**, identity band 0.0085–0.1003, boundary rim **0.0763–0.4607**; on noir over water the rim is 0.2989 against the band's 0.0085, a factor of **35**. The wash is ~52% of the territory's fragments for the weakest signal in the set, so it is the layer whose removal is near-free — and the cheap instinct, drop the thin hairline, would have left every dark theme's ocean with no visible boundary at all. Measured rendered over 24 cells: at `low` the territory mesh count goes **0 → 6 per cell** (3 territories x band + rim) and group placards **0 → 24 of 36**, which is 3 of 3 at cover and desktop on all four themes and **0 of 3 at phone**; the 12 control cells (the same world at 4 items, which plans `high`) are **6 → 6** and 20 → 20, and their before/after pixel diff is **0.13–1.10% at maxΔ ≤ 46** against low cells at **25.6–62.0% at maxΔ 216–237**. Four costs. **The phone number is the finding, not the miss**: a 12-item composite decluttered every group placard to `fillOpacity` 0 on all four themes at either LOD, against 2 of 2 on the 4-item control — so on the viewport this domain is written for the ring IS the grouping signal, which is what makes dropping it a total loss rather than a partial one, and it is `groupIdentity.js`'s own "a tint is not a label" turned into a number. **A module can state the contract and cannot enforce it**: `affinityGroupDetail` returns `band`/`rim`/`placard` true at every tier and the suite sweeps `COMPOSITE_LOD_TIERS` for it, and a re-introduced `lod !== 'low'` around the mount would pass every one of those assertions — the case that actually guards the fix reads `FusedCompositeScene.jsx` as text (verified by re-introducing the conditional: 1 failed, 22 passed). And **two method notes, one of them already in this file**. `reducedMotion: 'reduce'` is not optional for a composite sweep and its absence looks like a result: without it two shots of the identical build differ by **0.578–15.4% of pixels at maxΔ up to 237**, and the probe reports every group placard at `fillOpacity` 0 on every viewport — which reads exactly like the placards never being drawn. That finding has been in this ledger since the subway slice and it cost two 24-cell sweeps to rediscover. With it the floor is 0.03–1.4% at maxΔ ≤ 39 on 19 of 24 cells and still **6.7–14.6% at maxΔ ~224** on the other five, bimodal because the declutter pass settles on a different stable label set — so the **scene-graph** statistic is what carries this slice: the territory mesh count was identical in all 24 cells across two runs of the same build, and only phone placard survival drifted, by ±1. Budget note: the new cases went into the existing `metaphorGroupIdentity.test.js` again rather than a new file — `metaphor-suite-blast-list` still **blocked-by-paths**. |
| 2026-09-19 | Queue 6 — the machine's relations are drive belts now: tangent to both wheels, wrapping the one they drive, and tappable on a phone for the first time | 100% / 13 | 390x844, 717x512, 1440x900 — 2 machine fixtures x 3, before and after, each with a marker overlay proving the projection | [#742](https://github.com/acuhlmann/mermaid-gen/pull/742) | Last two runs were queue 3 and queue 2, so both were off-limits; took `link-pick-machine-phone`, open since 2026-09-05 and the one base kind the fused slice's standing advice ("fix the ROUTE, stand it off the rim, never the picker") had never been tried on. **The measurement is the app's own picker, not a private raycast.** `HoverableItem.handleOver` sets `document.body.style.cursor` synchronously and stops the event, and a link is only ever picked from `onPointerMissed`, so "did an item claim this pixel" IS the verdict — 240 taps, ten aim points spread by arc length along each drawn route, on two fixtures (a 5-gear drive train, a 4-gear replication loop) x the three standing viewports. Before: **9 of 80 aim points free at 390x844 and only 4 of 8 routes reachable at all** — the entire replication fixture was untappable on a phone — against 58/80 cover and 75/80 desktop. After: **33/80, 65/80, 77/80, and 8/8 routes reachable at every viewport**, no cell worse. Three things this cost a render each. **Height is not the lever**, which is the trap the whole slice turns on: an item's name plate is a SCREEN-CONSTANT mesh inside the same `HoverableItem`, so raising the route walks it out of the gear body and into the label, which claims the pointer for the same item. **The offset is the TOOTH radius** (~1.11·r — `GearBody` puts the tips past the pitch circle), not the pitch radius, or the belt runs through the teeth. And **the belt has to lie in the WHEELS' plane**: the first version reused the `MetaphorLinks` anchors at `radius + 1.2`, and stood off the rim up there the belts floated over the bedplate, ran off its edge at 1440x900 and joined the gears to nothing the eye could follow — a clean picking win (39/40 desktop) and an obvious visual loss, caught only because this domain renders what it changes. Mid-tooth fixes both; the link CAPTION is the one piece that keeps the anchor height, because down among the gears the declutter pass, which ranks a link caption last, dropped both of the replication fixture's captions at 717x512. `MetaphorLinks` took a `route` prop rather than a third `variant`, because the belt needs the gear radii and an anchor map does not carry them. **Two probe traps, both of which produce confident wrong numbers.** Two synthetic `pointermove`s dispatched in one tick collapse into one, so park-then-aim without a gap scored **0 of 5** on item probes whose markers were dead on the gear hubs; 30 ms between them is enough. And **the camera must be elected by proof, not captured** — neither the `updateProjectionMatrix` patch nor `Mesh.prototype.onBeforeRender` elects it reliably (the shadow and environment passes overwrite the latter), and the first two sweeps reported a clean sweep of free aim points with every marker sitting in empty sky. Projecting each item's own solid box, tapping it, and keeping the camera the app answers "item" for — with the four canvas corners as the control — is what settled it: 5/5 or 4/4 with 0/4 corners on five of six cells, 3/5 on drivetrain desktop. Side find folded into `flat-world-portrait-dead-band` below: the settled fit is **`[38.9, 65.7, 39.2]` on a 390x844 phone** against `[18, 14, 18]` on desktop, so the domain file's recorded `[13.383, 10.285, 10.513]` is one viewport's number and not a settle calibration. Budget note: the new cases went into the existing `metaphorLinkRoutes.test.js` — `metaphor-suite-blast-list` still **blocked-by-paths**. |

| 2026-09-18 | Queue 2 — the camera stopped framing the glow: light is not the subject, and additive blending is the marker that says so | 100% / 13 | 390x844, 717x512, 1440x900 — 14 base kinds x 3 after, 4 changed kinds x 2 themes x 3 before and after, plus a camera-free binding-geometry decomposition over every kind | [#750](https://github.com/acuhlmann/mermaid-gen/pull/750) | Last run was queue 3, so queue 3 was off-limits. **Not a queued todo — found in the preamble captures, by asking why a layercake on a 390x844 phone was drawn 180 px wide inside a 320 px fit box.** `collectFramePoints` had a stated doctrine (text, the shadow catcher, ambience and every grounded kind's substrate are scaffolding and must not decide the framing) and a `SUBSTRATES` sweep in `metaphorSceneFraming.test.js` holding eight kinds to it; **light was never on that list**, and it is the one class a list could not have saved, because a glow is authored as a MULTIPLE of the body it lights (`radius × 7.5` on the orrery's sun, `spread × 1.55` on a galaxy cluster's halo) and therefore grows with whatever the fit decides. One predicate — `isAdditiveLight` — reaches `GlowSprite`'s 17 call sites across seven scene modules, `FloorGlowDisc`, the cluster halo ring, the stardust, the asteroid belt and the accent shaft at once, with no new flag and no new list. Measured pure (`solveFrameFit` re-run in-page over the same point cloud `SceneFrame` uses, so no projection and no camera lie), as camera distance where lower is a bigger subject: over 14 kinds x 3 viewports on whiteboard, **14 cells bigger, 0 smaller, 28 identical to 2 dp** — orrery phone **67.65 → 24.71 (+173.8%)**, galaxy desktop **52.83 → 28.01 (+88.6%)** / cover 52.99 → 30.58 (+73.3%) / phone 94.40 → 71.80 (+31.5%), layercake 65.97 → 53.04 (+24.4%) and 40.19 → 33.88 (+18.6%) at both landscape viewports, machine 77.15 → 65.77 / 29.90 → 25.27 / 29.65 → 25.06 (+17–18%), city cover and desktop +0.7%. Noir adds the accent light shaft to the same rule and the machine gains more there (+24.8% cover, +25.7% desktop). Labels drawn went **up in 4 of the 24 before/after cells and down in none**. Four costs. **The pure simulation that priced this before a line was written was WRONG, and in the direction that would have killed the slice**: it predicted +0.5% on a galaxy phone and **−0.2%** on an orrery, because it hid candidates with `o.isMesh` while `collectFramePoints` walks anything carrying a geometry — a galaxy's stardust and an orrery's asteroid belt are `Points`. The real figures for those two cells are +31.5% and +173.8%, so a simulation of a renderer rule has to run on the same object set the rule runs on. **An opacity bar was the obvious rule and would have been wrong**: a translucent body is still a body, and a fused layer muted to 0.2 by the pointer would decide where the camera stands at the next refit — the suite carries that as a control arm (an ordinary translucent plane at a LOWER opacity stays in the fit), without which the additive case passes just as well against an opacity rule. **`FactoryFloor` needed a flag a fortiori**, not a measurement: `MachinePlate` already opts out because "the gears are the subject, the plate is the bench", and this is the floor the bench stands on at 1.18× its radius. And **the first three probes all lied in ways that looked like results** — a projected fit box that ran to 15,359 px because the sky sphere is a unit sphere SCALED to 220 and a local-radius bar misses it; a per-mesh re-solve that reported single objects needing more distance than the whole scene, because `solveFrameFit` recentres; and a `fillOpacity` label probe that reported 0 of 4 names drawn on a layercake that plainly draws three. Both new guards were verified by reverting the fix: 2 failed, 41 passed. Side effect: `collectFramePoints`'s walk was split into `sample` + `visit`, so `apps/web` loses one `complexity` warning — `docs/agents/ratchet.json` is outside this shelf's paths, so `lintWarnings.apps/web` is **blocked-by-paths** for `improve`. |

| 2026-09-20 | Queue 4 — the numeric rescue ladder became a table swept against the schema, and a number the model wrote as text stopped skipping every clamp in it | 100% / 18 (was 100% / 13) | none — pure-function slice, measured by replaying documents through the real ladder | [#756](https://github.com/acuhlmann/mermaid-gen/pull/756) | Last three runs were queue 3, queue 2 and queue 6, so all three were off-limits; queue 1's remaining items are both **blocked**, so queue 4 — "driven by the bench" — was the slice. The preamble is clean (`expectationMatch` 100%, `rescueableHitRate` 100%), so the gap had to be found by **probing**, not by reading the report: a 66-case battery of realistic model slips (numeric strings, bad ids, null optionals, prose around the fence, enum synonyms, range overshoots on every kind) replayed through `validateAndPrepareMetaphorPatch` accepted **31 of 66**. Two defects, one mechanism, and the first is the ledger's favourite shape. **`rescueNumericRanges` was 238 lines of inline `if (kind === 'terrain')` blocks with every bound hand-copied, and it covered 28 of the schema's 32 numeric (kind, field) pairs** — the four missing being `city.height`, `city.footprint`, `galaxy.magnitude` and `layercake.thickness`, i.e. the PRIMARY size encoding of three kinds, one of them the kind `default-metaphor-city` sends every unrecognized document to. Every optional decoration (`cracks`, `tilt`, `torque`, `peril`) had a row; three kinds' main magnitude did not, which is what a list written in the order its kinds were added looks like. Fourth instance after `DAYLIGHT_LOCKED_KINDS`, the postfx ladder and `metaphorSkyBackdrops`. **And every one of the 80+ labelled fixes opened with `typeof === 'number'`, so the whole numeric layer was unreachable from `{"height":"8"}`** — the commonest way a model breaks JSON it otherwise got right. Fixed by `METAPHOR_NUMERIC_FIELDS` (`packages/shared/src/metaphorNumericFields.ts`) with coercion inside the same pass. Measured pure: numeric pairs with a rescue row **28/32 → 32/32**; the 66-case battery **31 → 42 accepted**, all 11 numeric; corpus **13 → 18 cases at 100% `expectationMatch`** (`acceptRate` 61.54 → 61.11, which is the playbook's point — two of the three new cases are must-rejects). Composite came free: layers route through the same table via their `as` kind, so `composite-layer-numeric` passes without a composite-specific line. Four costs. **The sweep is the deliverable and the table is the smaller half** — it derives each kind's numeric fields from the schema through public Zod surface only (`shape.items.unwrap().element.shape`, then `safeParse(1).success && !safeParse('x').success`) and asserts EQUALITY with the table, so a schema field added tomorrow fails the suite tonight; verified by reverting (dropping `city.height` and narrowing `layercake.thickness` to 8 → **5 failed, 400 passed**, two of the five the sweeps by name). **A `positive()` field's floor is ours and its ceiling is the schema's**, so the two halves are asserted differently — `max + 1e-6` must fail, but `min - 1e-6` must NOT be asserted on a positive row, because 0.1 (and archipelago's 0.5) is a rendering judgement and asserting it against the schema would be asserting our own taste. **The coercion refuses a second number on purpose**: `"12-20"` is a range and `"3 of 5"` is a ratio, so taking an end of either is fabrication dressed as a rescue — those stay strings, the Zod error names the field, and two corpus cases exist to keep them rejected. The percent rule is read off `bound.max <= 1` rather than a list of percent fields, since a hand-kept list would be the same defect one level down. **And the position pass had the worse failure, disguised as a success**: `["1","0","2"]` dropped the whole `position` and ACCEPTED the document, relocating the item to the layout's default slot with only `drop-invalid-position` to say so — a rejection costs a repair turn, that cost the author's placement silently. Method note now in the domain file: `rm apps/server/bench-results/after-*.json` after a `--tag after` run deletes three snapshots committed in 2026-05 whose tags also begin `after-`; the playbook's "read it and delete it" means the exact filename the script printed. Budget note: the sweep wanted a `metaphorNumericFields` suite of its own under `packages/shared/test/` and **could not have one** — it matches `METAPHOR_BLAST_TESTS`' reverse sweep, whose bundle is in `scripts/test-affected-lib.mjs`, outside this shelf's paths. Fifth run to land cases in a listed suite for this reason; `metaphor-suite-blast-list` still **blocked-by-paths**. Side effect: `metaphorSanitizer.ts` loses 210 lines. **Follow-up the same night ([#757](https://github.com/acuhlmann/mermaid-gen/pull/757)):** CodeQL flagged `NUMERIC_TEXT` as polynomial ReDoS minutes after the push, and it was real — written the obvious way (`[~≈]?\s*[$€£¥]?\s*`) the two whitespace runs are separated by an OPTIONAL character, so a run of spaces can be split between them in as many ways as it is long. Measured on `"~" + " ".repeat(n) + "!"`: **8.7 ms at n=2,000 rising cleanly with the square to 553 ms at n=16,000**; anchoring each `\s*` inside a group whose other member is mandatory returns **0.1 ms flat** at every n. The lesson generalises past this regex — an optional character between two `\s*` is the ambiguity, not the `\s*` itself — and CodeQL is the right sensor for it, since a timing assertion in the suite would be a flake under load. Worth recording that the finding arrived as a review comment on a PR whose own CI was green: the six required checks do not include code scanning, so auto-merge landed #756 before the comment could be acted on. |

## Todos

| Id                              | State                | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ladder-in-claude-md`           | **blocked**          | Metaphor3D is the one slot missing from `CLAUDE.md` § Validation ladder. **`CLAUDE.md` is not in this automation's `allowedPaths`**, and the playbook forbids writing findings into the root files, so this cannot be done from here. It belongs to `improve` or to a human — do not keep picking it up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `validation-kinds-10-of-15`     | done                 | Fixed 2026-08-30 (#457). All 15 kinds listed; the flowchart also gained the fence-strip rung, and the **"single-shot syntax fixer" line was simply wrong** — `repairMetaphorWithFixer` is a lite → flash → DeepSeek ladder like chart's and forms'. Nothing had listed that as drift.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `bench-baseline-stale`          | **blocked**          | `apps/server/bench-results/**` is in this automation's `forbiddenPaths` and on the repo don't-touch list, so a refreshed baseline cannot be committed from here. Same disposition as `ladder-in-claude-md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `bench-header-rescue-count`     | done                 | Fixed 2026-08-30 (#457). Counted: **17 rescue functions emitting 80+ distinct labelled fixes** — the playbook's own "~40" was low too. The "script has ~17 cases" in the old note was a miscount of `expectedAccept` occurrences; the corpus is 13.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `blast-radius-rule`             | pending              | `scripts/test-affected-lib.mjs` has no `metaphor*` rule, so `test:affected` under-selects badly here. Outside this automation's paths — belongs to `improve` queue item 4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `accent-vs-tube`                | done                 | Fixed 2026-09-02. The standing diagnosis — "the route tube rises toward the camera in front of its terminus station's name" — was wrong, or at least never the binding constraint. The tube is opaque geometry and a station name is a depth-tested label, so a tube in front does eat glyphs; but the terminus names were not being _eaten_, they were not being **drawn**. The pinned route sign standing past the terminus won the screen-space declutter against the unpinned station name, every fixture, every viewport. Hiding the signs restored 7 of 7 missing names with the tubes untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `interchange-loses-a-name`      | done                 | Fixed 2026-09-04 (see the run row). A joined station name was the right answer: 6 of 29 authored names had no `Text` at all before, none after; legible 60/87 → 78/87 with no cell worse. The longer label was indeed not free, and the price was paid twice in `labelRoles.js` rather than in the subway — a stacked sign has to report its LINE COUNT as height (or the declutter reserves half the box it draws) and has to be lifted half a line per extra line inside the screen-constant group (or troika's middle anchor grows it down onto the platform it names).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `subway-portrait-dead-band`     | pending              | A lane diagram is wide and a phone is tall, so the camera fits width and the subject occupies about a third of a 390x844 canvas — measured on `support`, the network spans y 680–1230 of 1710 with empty sky above and below. Not a framing bug (the slack is centred); the fix is a camera that maps the network's long axis to the canvas's long axis on a portrait aspect. General to every lane-shaped kind, and much bigger than one slice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `verify-probe-camera`           | **blocked**          | `apps/web/.claude/skills/verify/SKILL.md`'s label probe still picks the wrong camera, and picking by closest `aspect` is not enough either — an in-page projection of the accented label's own bounding box put 4 of 12 cases off the top of the canvas. The skill file is outside this automation's `allowedPaths`. Camera-free pixel diffs (name visible vs hidden, marker visible vs hidden) answered this slice without it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `generation-bench`              | pending              | `benchMetaphorGeneration.js` is named as open work in `docs/guide/validation.md`; the harness is slot-agnostic. Costs tokens — weekly, `--samples 3`, never nightly. File an issue before starting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `layer-key-eats-the-name`       | **blocked-by-paths** | The composite's layer key truncates the author's own layer name while the fixed `<kind> · <count>` chip beside it never yields: on a **1440x900 desktop** the toaster world renders "Memory ro…", "Appliance landma…", "Persistent thou…" — 3 of 4 — with 1100px of empty canvas beside the panel. Same defect the reading strip already had fixed ("the squeeze is spent on its chips, never on the scene's name"), and the layer key is the fused world's ONLY statement of what each grammar is. The markup is `apps/web/src/components/MetaphorOverlays.jsx` and the CSS is `apps/web/src/App.css`; **neither is in this automation's `allowedPaths`**, and `apps/web/src/styles/**` — which is in them — does not exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `link-pick-machine-phone`       | done                 | Fixed 2026-09-19 (see the run row). The diagnosis was right — "likely fix is a route that stands off the gear rim rather than a picker change" — and the scope was larger in both directions than the note said. It was never only `machine`'s belt between two meshed gears: **4 of 8 routes across two fixtures had no tappable point at all at 390x844**, 9 of 80 aim points free. And the fix is not only a picking fix — the belt is what the machine's relation should have looked like from the start, so the same change carries the legibility. The todo's "measure before deciding" earned its keep twice: standing the route off the rim at the ANCHOR height measured as a picking win (39/40 desktop) and rendered as an obvious loss, and the caption had to be left behind at the anchor height after the low belt cost the replication fixture both of its captions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `metaphor-suite-blast-list`     | **blocked-by-paths** | Hit a fifth time 2026-09-20, this time from the `packages/shared/test` side: the reverse sweep's family pattern matches a `metaphor*.test.ts` in EVERY bundle directory, not just `apps/web/test`, so the schema sweep landed in the listed `metaphorSanitizer.test.ts` instead. Hit again 2026-09-12, and the catch-22 is now fully drawn. A **new** `apps/web/test/metaphor*.test.js` cannot be added from here (`scripts/test-affected.test.mjs`'s reverse sweep demands it appear in `METAPHOR_BLAST_TESTS`, in `scripts/test-affected-lib.mjs`, which `routine:guard --reachable` gives to `improve` alone) — **and `apps/web/test/switchMetaphorKind.test.js`, which IS listed there, is outside this automation's `allowedPaths`**, so the switcher's own suite cannot be extended either. Found by `--postflight` refusing the diff, then by the sweep refusing the workaround. Both escapes are shut; the 2026-09-12 slice landed its cases in `metaphorFlatItemsCore.test.js` (listed, in budget, and cross-kind item concerns are the nearest subject). Two things `improve` could fix, either sufficient: add `apps/web/test/switchMetaphorKind.test.js` to this playbook's `allowedPaths` beside the `switchMetaphorKind.js` entry that is already there, or let the sweep tolerate a suite this shelf may write.                                                                                                                                                                                                                                                                                                                                        |
| `link-pick-composite`           | done                 | Fixed 2026-09-06 (#557). The gate takes the source: `metaphorKindHasEditableLinks(metaphor, source)` consults `compositeGraphAllowsLink` for the composite branch, and `MetaphorRenderer` passes `diagramSource` — the same call `graphEditAdapterFor` already makes for the adapter's own `canLink`, so gate and mutator cannot disagree. Composite stays out of the static list because its permission is per-document, not per-kind. `renameCompositeEdge`/`deleteCompositeEdge` were already green; #495's dead code is now reachable for the sixth kind, not the fifth. **The premise this todo carried was wrong in one load-bearing way**: composite has two route renderers, and only `LegacyCompositeScene`'s `MetaphorLinks` publishes a hit target — the default `fused` plan draws `FusedLinks`, which publishes none. Half of it remains, in `link-pick-fused` below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `link-pick-fused`               | done                 | Fixed 2026-09-07 (see the run row). `FusedLinks` publishes `LINK_PICK_USER_DATA` only for `isPickableFusedLink(link)` — the authored links, never `inferredRelationships()`' — and gained a picked state that outranks `muted`/`dimmed`. **0 of 9** fixture/viewport cells had a pickable route before, **9 of 9** after; 0 wrong-link picks and 0 false picks across 144 aims and 27 empty-sky controls. The premise held up in full, inferred-route trap included: a composite with a `tree` layer plans both kinds side by side, and the test pins `isPickableFusedLink` to `renameCompositeEdge(...).ok` rather than to the word `inferred`, so the planner learning a new inference cannot quietly widen the pickable set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `link-pick-fused-phone`         | pending              | On a 390x844 phone only **3 of 16** fused link/viewport cells are reachable by tap (toaster: 0 of 5), against 10/16 desktop and 9/16 cover. Correct per the item-wins rule — measured, all 108 misses in the sweep were an island or tower under the aim point, not one was a wrong link or a dead tap — and still a gap: the cross-layer relation a composite exists to state is the thing a phone cannot touch. Raising `LINK_PICK_TOLERANCE_PX` would do nothing, because the tap never reaches the picker. Likely fix is `FusedLinks` arcing its route to stand off the site rim (`fusedWorldSurface.js` already models the dome) instead of crest-to-crest over the islands — a legibility win independently of picking. Same shape as `link-pick-machine-phone`; measure before deciding. **Update 2026-09-19**: the same prescription was taken on `machine` and it works — a route that stands off the rim took that kind from 4/8 to 8/8 reachable routes at phone width. The fused case is the harder one (`fusedWorldSurface.js` models a dome, not a flat plate, and a fused link's ends are island CRESTS rather than wheels with a radius to offset by), but the shape of the answer is now proven rather than assumed.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `accent-pin-vs-its-own-name`    | pending              | The enlarged pin head is now covered by the accented item's OWN name plate on the two city cells (survival 6% → 1% on a 717x512 cover), because `ACCENT_ITEM_LABEL_ORDER` deliberately draws that one name above the whole callout on an opaque 0.94 chip. Correct per the ladder — a marker must never overpaint the name of the thing it marks — and still a picture where the head is clipped. The name sits at a screen-constant offset above the item, so a longer stem WOULD clear it; that trades against `STEM_TARGET_HEIGHT_PX`, which exists because a long rod ran off frame. Needs the two measured together, not either alone. **Re-measure before pricing it: the 2026-09-16 lean changed the geometry this was observed on** — a head behind a panel now stands off to one side, which moves it out from under the name plate the item draws straight above itself; the case that remains is a head with nothing to dodge, where the rod is still vertical and still under its own name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `accent-under-the-title-card`   | done                 | Fixed 2026-09-16 (see the run row). The diagnosis was right in both halves — the marker geometry does not know about the panels, and the camera safe area does not reserve for it — and the todo left the **choice between the two fixes** open, which is the whole content of this slice. Reserving for the callout is honest now (it is a pixel constant since #593, so it cannot feed the loop that took it out of the fit) and it is 20% of a foldable cover's height charged to every accented scene, against a collision a 225k-position sweep puts at one head position in eight; the lean costs nothing where nothing is wrong. Also worth recording: the case the todo names is not one fixture's bad luck — a `river` reproduced it on the first pass of a 15-kind sweep, and the pure grid says 4.6–18.7% of head positions are behind a panel depending on the viewport and how many panels it carries. Residual, by design: positions still covered after the 63 px cap may take a capped partial lean when it buys at least 15% overlap back (`PARTIAL_GAIN` in `accentRodLean.js`); when even that partial relief fails the bar, the rod stays vertical.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `flat-world-portrait-dead-band` | pending              | Generalises `subway-portrait-dead-band`, and measured this run on the layercake: at 390x844 the cake is **width-bound and fills 38% of the canvas height**, with the framed NDC box spanning y[-0.57, 0.19]. `frameDirectionForAspect` already lifts to its 52° cap, so the remaining lever is elevation past 52° — which foreshortens exactly the axis that IS the encoding on layercake and iceberg. **Azimuth is not the lever**: the domain file's "searching the camera's AZIMUTH does not earn its keep — do not redo it" was read before this run and is why the lane-diagram framing was not attempted. Needs a decision about the elevation/encoding trade before any code. **Datum from 2026-09-19**: the settled portrait fit on this machine sits at `[38.9, 65.7, 39.2]`, distance ~86, against `[18, 14, 18]` at desktop — the portrait camera is not merely lifted to the 52 deg cap, it is pushed **three times further out**, which is the same dead band measured from the camera's side.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `bridge-deck-scale`             | pending              | `FusedPath`'s `scale={[1, 0.45, 1]}` squashes the whole crossing CURVE, not the deck's profile, so a bridge sits at 45% of its planned elevation. Recorded in the domain file. Not fixed here because no composite fixture carries a `bridge` layer and this domain's rule is that a visual change is verified by rendering it. Needs a bridge fixture first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `complexity-queue-547`          | pending              | Handoff from an interactive session that closed #557/#474/#460/#575/#578 and took 9 of #547's 17 complexity rows (#569 #570 #571 #580 #581 #583). **8 remain, and all 8 move labels, so all 8 need this domain's render evidence, not just green unit tests:** `MetaphorRenderer.jsx` MetaphorRendererImpl (86); `metaphorLayouts/subwayNetworkLayout.js` subwayNetworkLayout (23); `fusedCompositePlanner.js` assignLabelRanks (19), resolveCompositeMotionTransform (18), makePresentation (16), resolveCompositeAtmosphere (16), makeSites (15); `labelDeclutter.js` resolveLabels (18) + the arrow at 286 (15); `MetaphorSceneChrome.jsx` ItemLabel (16). Re-verify each line/value before quoting - they drift. One row-family per PR; never raise a threshold, ceiling or add a suppression (that is the failure mode the queue exists to prevent); when `npm run verify:ratchet -- --with-lint` reports a lower number, tighten `lintWarnings.apps/web` in the same PR. **Two traps already paid for:** (1) extracting a helper with too many positional params trades a `complexity` warning for `max-params` - two warnings, same count, zero gain; do a second extraction and take an options object instead. (2) After adding ANY branch to a function, re-lint THAT file; `verify:ratchet` deliberately gates nothing in CI, so nothing will tell you otherwise. `labelDeclutter` carries extra debt: #578 proved the pass cannot see an opaque SCENE MESH in front of a plate (it compares labels vs labels vs DOM chrome only), and fixing #578 by placement left that blind spot - say so rather than shipping the extraction as if nothing were owed. |
| `unaccented-crosses-the-rod`    | done                 | Closed 2026-09-06 as a **non-event**, by measurement rather than by argument. Setting `depthWrite` back to `true` on the live stem and pin changed **0 pixels** in every one of the nine baseline cells; the control (flipping `depthTest` on the same two meshes) moved 292 px, so the toggle was live and the frames were deterministic. The mechanism #474 recorded is real, and reaching it needs a depth-tested chip to stand behind a rod 1–4 px wide while not being blocked by anything else — no fixture does. The wording in `metaphorDrawOrder.js` already states the trade as a cost, which was the other half of this todo; nothing to restore. Original note: #474 corrected the record rather than the pixels: because the accent stem stopped WRITING depth, an **unaccented** chip standing behind the rod now passes the depth test and, being transparent, paints across an opaque rod it used to be clipped by. Almost certainly the better picture (a name on its own card instead of a deleted glyph — the trap `accent-vs-tube` misdiagnosed), but it is a different picture and nothing on disk says by how much. Needs a render, not a reading: an unaccented name behind an accented stem, before/after, per `apps/web/.claude/skills/verify/`, and then either a wording change where the trade is stated as a cost or `depthWrite` restored for the pin alone if the crossing reads worse than the deletion did. The mechanism itself is settled and is in `docs/agents/domains/metaphor3d.md` — three renders opaque, then transmissive, then transparent, `renderOrder` a tiebreak within each.                                         |
| `sky-lock-sweep-other-kinds`    | done                 | Swept 2026-09-11 (see the run row). Both halves answered. The tree's own class has **no second instance** — 14 base kinds x 4 themes at 1440x900, `nearWhite` max 0.0087 — so `DAYLIGHT_LOCKED_KINDS` is complete. The inverse class is real and is exactly `galaxy` + `orrery`, and the premise "a scene forcing a sky its theme did not choose" was slightly off: the galaxy's sky IS the theme's, from `spaceTopColor`/`spaceHorizonColor`, a second channel nothing else reads. Fixed by `SPACE_LOCKED_KINDS`. The third case the todo named — a sky sphere and `skyHorizonColor` that merely disagree — turned up one instance that is **not** a lock question and is filed separately as `terrain-has-no-sky`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `terrain-has-no-sky`            | done                 | Fixed 2026-09-13 (see the run row). The diagnosis was right and the fix itself was one component; the **finding** is that it was possible at all — the kind-to-sky decision was eleven ternaries inside `MetaphorRenderer`'s JSX, so the one kind missing from it could not be named by any test. It is now `utils/metaphorSkyBackdrops.js`, swept against `METAPHOR_BASE_KINDS`. Third instance of that class after `DAYLIGHT_LOCKED_KINDS` and the postfx ladder.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `switch-to-layercake-refused`   | done                 | Fixed 2026-09-12 (see the run row). The diagnosis was right — a raw copy where a per-kind rescale belonged — and the **scope was wrong by an order of magnitude in both directions**. `layercake` was not the only refusing target (15 pairs refused, not 1; `city → galaxy` and five `→ city` pairs were never mentioned), and refusal was not the main harm: **51 further pairs succeeded while saturating**, which renders a wrong picture instead of an error. Generalising it to "a magnitude must be refitted between kinds, never copied" cost no more code than the layercake-only fix would have, because the table it needs is the schema's own. Lesson for the next todo written from one observed symptom: sweep the whole matrix before pricing it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `space-group-plate`             | done                 | Fixed 2026-09-14 (see the run row), together with its twin and by one mechanism. **The reason both were parked was the half of the premise that was wrong**: the ordinal-palette rule ("a floor over four shades of one hue collapses neighbours") does not bind here, because the plate reads `districtPalette[0]` **alone** and takes its ordinal from `tintByGroup`'s hue ladder — there is no neighbour to collapse. Answered as a **placement**, not a tint and not a floor: `resolveGroupPlateBase` stands the whole eight-group band one side of the already-locked `groundColor`, clear by `PLATE_GROUND_SEPARATION`. The pale wedge under "MONEY" is gone — min plate-to-ground luma on the space lock **0.562 → 0.134** pure, and the rendered composite's territories go from four identical grey wedges to four hues (nearest pair as painted 0.0055 → 0.0242).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `tree-themes-read-alike`        | pending              | With the daylight lock complete, the tree now renders near-identically on all four themes — contrast varies 34.4-36.8 at 390x844 across the four, and the only surviving theme signal is the 10% leaf/meadow tint `treeTintedPalette` applies. That is the nature lock working as authored (same contract as river/garden/archipelago), not a defect, but it means picking noir buys a user almost nothing on this kind. Widening the tint band, or letting the theme keep its MOOD while the sky stays daylight, is a product-direction question, not a bug — it is here so the next run does not "fix" it by accident.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `composite-daylight-ground`     | done                 | Fixed 2026-09-10 (see the run row). The decision it was parked on — should a daylight-locked composite tint its land — is answered as a **luminance floor, not a tint**: a surface already clearing the bar is returned byte-identical, so the change can only ever move a surface that would have rendered as a void, and whiteboard, every unlocked kind and every base locked kind are provably untouched. The premise was right and understated it: this was never only the tree, and never only the ground — `buildingColor`, `machinePlateColor` and `terrainBaseColor` are in the same position, so a four-layer river+machine+layercake+terrain fixture was added to the probe to exercise them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `daylight-group-plate`          | done                 | Fixed 2026-09-14 (see the run row). The diagnosis was right and **understated**: the noir plate was not merely dark under a daylight sky, it was **0.012 of luma from the raised ground it sits on** — the grouping nouns were all but invisible, not merely unlit. And the todo's proposed answer (derive the plate from the raised `groundColor`) would have been wrong in one load-bearing way: `tintByGroup` returns group 0 unchanged, so a plate base equal to the ground draws the FIRST territory invisibly. Placing the band a fixed step from the ground keeps every group clear of it, group 0 included. Measured 0.012 → 0.131 pure, 0.0055 → 0.0353 rendered.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `daylight-tower-albedo`         | pending              | The floor puts noir's `buildingColor` at `#3c495a`, up from `#334155` — enough to clear the bar and, in the captures, still a dark tower in daylight. Arguably correct (a dark building IS dark at noon) and worth a deliberate look: the question is whether 0.28 sRGB luma is the right bar for a BODY as opposed to a ground plane, which is the only one-size constant in the module. Needs a picture, not an argument.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

| `composite-terrain-atmosphere` | pending | `ATMOSPHERE_RULES` (`fusedCompositeSceneResolvers.js`) puts `terrain` **last**, below the `path` and `substrate` ROLES and eight kinds, so a terrain-dominant fused world takes the terrain sky in only **2 of 13** pairings (`cycle`, `iceberg`); `terrain + subway` gets a river sky because subway's capability role is `path`, and `terrain + city` gets a city one. Arguably correct — terrain's role is `field`, not `substrate` — and arguably the same argument the archipelago row won on. **Not touched this run on purpose**: reordering that table changes the sky of every mixed world, so it needs the full pairing sweep rendered, not an argument. |
| `terrain-sun-vs-night` | pending | `TerrainSky` draws `SkySunGlow` on all four themes, matching `bridge`/`cycle`/`iceberg` (the other themed kinds that draw one) and matching what `TerrainClouds` and `SoaringBirds` already do unconditionally. On noir that is a sun in a night sky — a soft bright patch top-centre in the after-shots. Consistent, and possibly wrong; the alternative is a moon/star treatment keyed off `isDarkBackdrop`, which is a product-direction question about whether a dark theme means night or means dark. Recorded so the next run does not silently flip it either way. |
| `plate-ladder-base-sweep` | pending | `metaphorGroupIdentity.test.js`'s pairwise sweep pins the ladder against `buildingColor` only, which is why the whiteboard plate collapse (`districtPalette[0]`, three groups at pixel-identical `#ffffff`) survived every run since the ladder shipped. The plate half is swept now; **`GROUP_TINT_EARTH`'s bases are not** — `treeLeafColor`/`treeSoilColor` (`fusedCompositePrimitives.jsx`) and `gardenSoilColor` (`GardenScene.jsx`) have never been checked for clipping or for separation from the surface they sit on. Same shape of defect, one strength down. |
| `fused-lod-drops-groups` | done | Fixed 2026-09-17 (see the run row). The diagnosis was right and the scope was much larger than one fixture: 465 of 1,279 grouped worlds in a 1,460-world sweep drew no territory at all, 113 of them under the `itemCount > 18` bar. The todo's own instruction — "needs the cost model looked at, not just the gate" — was looked at and **deliberately not taken**: `groups.length` in `estimatedCost` is what closes the loop (93 → 96 on the named world, so the third shared noun deletes all three), and it is also honest, because three discs do cost three discs. Removing it would have moved 465 worlds into a higher detail tier as a side effect of fixing a legibility bug, which is not a change this run could verify. Fixing the gate instead leaves the sum truthful and leaves the loop nothing to close on. |
| `group-plaque-off-the-ground` | pending | A group's floor placard stands at `group.radius * 0.86` south of the territory centre, and `group.radius` is clamped to 9 while the world disc is sized from its sites — so on both the 12-item and the 4-item composite, at every viewport and theme, at least one plaque runs off the ground entirely and lies over the background. Visible in this run's captures on all 24 cells (whiteboard cover: MEMORY and THOUGHT; arcade cover control: APPLIANCE). Pre-existing and unrelated to the LOD gate — the control cells, whose code path this run did not touch, show it identically. The fix is presumably a clamp to `plan.groundRadius` the way `subwayRouteSign` clamps to the network's own reach, but it is a placard-placement slice of its own and wants its own before/after. |
| `subject-or-scaffolding-sweep` | pending | With light out of the fit, the binding geometry on five kinds is something whose status is a JUDGEMENT rather than an oversight, and each is worth a slice of its own. Measured camera-free on 2026-09-18 as the shipped fit distance against what the `HoverableItem` subtrees alone would need, at 390x844: **terrain 121.15 vs 51.40 (2.36x)** — the heightmap, which on this one kind arguably IS the subject since elevation is the encoding; **bridge 91.60 vs 56.64 (1.62x)** — the two shore abutments; **cycle 36.09 vs 25.28 (1.43x)** — the track ring, which is the metaphor; **river 97.05 vs 80.15 (1.21x)** — the source pool and its ripple rings; **garden 36.06 vs 30.59 (1.18x)** — the bed plinths, which carry the grouping noun. None is a glow and none is a substrate disc, so none is covered by an existing rule. Take them one at a time with a render, not as a sweep. |
| `layercake-stand-now-binds` | pending | With `FloorGlowDisc` out of the fit the layercake's next binding object is `CakeStand`'s rim torus at radius `maxRadius + 1.1` (need 52.15 against the tallest slab's 54.42 at 390x844, i.e. just under it and binding at the landscape viewports). Same question as the row above — a cake stand is a plinth, and every other kind's plinth is flagged — but it wants its own before/after because the stand is what stops the cake reading as floating. |
| `portrait-ceiling-is-arithmetic` | pending | Evidence for `flat-world-portrait-dead-band` and `subway-portrait-dead-band`, so the next run does not re-derive it: for a world whose footprint is as wide as it is deep, a width-bound portrait fit gives the subject `sin(elevation) x aspect` of the canvas HEIGHT, and no elevation fixes that — at 390x844 (aspect 0.462) even a plan view caps at 0.46, and the shipped 52° cap at 0.35. Measured on the machine before this run's change: the world occupied ~27% of the canvas height with ~250 px of empty gradient above and below. The levers are therefore the LAYOUT's footprint aspect (`gridPosition` is `ceil(sqrt(count))` columns — square by construction, the worst shape for a portrait canvas) or letting the world overflow the width, **not** the camera. Both are product-shaped; neither is a framing bug. |
| `sanitizer-id-regex` | pending | **The largest surviving rejection class in the 66-case battery, 8 of the 24 that still fail.** `MetaphorItemBase.id` is `/^[a-z0-9][a-z0-9-_]*$/i`, and a model writes `"auth service"`, `"api.gateway"`, `"team/ops"`, `"line:a"`, `"café"`, `"-edge"` constantly. Nothing rescues it, so the whole document is refused over a punctuation mark. A slug pass is the obvious fix and it is **not** a one-liner, which is why it is a slice of its own rather than a rider on the numeric one: an id is a reference, so `links[].from/to`, `tree.parent`, `orrery.moon`, `galaxy.binary`, `machine.mesh`, `subway.interchange[]` and composite's cross-layer link ids must all be rewritten by the SAME map, in one pass, or the slug silently orphans every relation that pointed at the old id — `rescueLinksField` would then drop them as orphans and the document would be accepted with its edges deleted. Collisions after slugging need a suffix, and the map must be built before any consumer is rewritten. Measure it the way this run did: probe first, then fix. |
| `sanitizer-json-envelope` | pending | 5 of the battery's 24 survivors are JSON-level: prose before the object (`Here is the diagram:\n{…}`), prose around a fence (`Sure!\n\`\`\`json…\`\`\`\nHope that helps.`), a trailing comma, a `//`comment, single quotes.`stripJsonCodeFence` only matches a fence that is the WHOLE string (`^`…`$`), so a fenced block with a sentence either side is not stripped at all. The cheap half — find the outermost `{…}`span, and match a fence anywhere rather than anchored — is most of the class and carries no guessing. The expensive half (trailing commas, comments, single quotes) is a relaxed JSON parse; **do not hand-roll one**, and a dependency needs an issue first (contract rule: no new dependencies). Split accordingly. |
|`sanitizer-null-optional`| pending |`{"district": null, "note": null}`is rejected: Zod's`.optional()`refuses`null`, and no pass deletes a null-valued optional key. A model emits `null`for "no value" as readily as it omits the key. Mechanically the smallest remaining class — delete any key whose value is`null`when the schema's field is optional — but it needs the same schema-derived sweep the numeric table now has, or it becomes another hand-copied list.`null` on a REQUIRED-with-default field (`height: null`) should also delete the key so the default applies; measured, both shapes appear. |
| `sanitizer-string-overflow`| pending |`label`> 120,`scene.title`> 160,`id`> 64 are all rejected with no truncation pass, though`note` already has one (`clamp-note`, 140). Truncating a label is lossy and an id is a reference, so this is a judgement call per field rather than one rule: a long label almost certainly should be truncated with an ellipsis (the renderer decltutters it anyway), a long id should be truncated **and** remapped through the same reference map `sanitizer-id-regex`needs, and a long title should be truncated. Worth doing after the id slice, reusing its map. |
|`sanitizer-theme-camera-enums`| pending |`scene.mood`and item`glyph`/`lighting`/`health`/link `kind`all have a drop-the-invalid-value pass;`scene.theme`and`scene.camera`do not, so`{"theme":"dark"}`or`{"camera":"top"}`rejects the document although both fields have schema defaults that would cover the drop. Same omission-from-a-list shape as the numeric table, one level up, and the fix is the same: one pass over a table of (field, allowed set) swept against the schema's enums rather than five hand-written functions. Also worth checking`composite.as`: an unknown `as`drops the whole LAYER, which on a one-layer document empties`layers`and rejects everything — defaulting it to`city`the way`rescueMetaphorField`defaults the top-level kind would keep the items. |
|`group-placard-phone-declutter`| pending | On a 390x844 phone a 12-item composite draws **0 of 3** group placards on all four themes, at either LOD, because the declutter pass fades them to`fillOpacity`0; the 4-item control keeps 2 of 2. Correct per the ladder (a placard is a label like any other and a crowded canvas has to drop something) and still the case where the grouping noun is most needed and least present. Two levers worth measuring before picking one: raise the group role's`importance`against an item name, or fold the placard into the layer key the way the reading strip folds its axis chips into a`+N`. Not a bug — a product-shaped question, so it wants a render sweep and not an argument. |

## Open observations

- **Closed 2026-08-31, and the diagnosis in it was wrong.** The previous row read "the accented
  item's own name is unreadable in `subway`, and the accent stem is why", and proposed a separate
  accent anchor. Raising the anchor would have fixed none of it: the stem was destroying the name
  by **writing depth** rather than by being drawn on it, and the missing `a` in "Pay" survives with
  the entire callout hidden, because the amber route tube ends at its terminus and rises toward the
  camera. Both are recorded in `docs/agents/domains/metaphor3d.md`; the tube's own half is still
  open for an unaccented terminus (`accent-vs-tube` above). The general lesson: **an anchor guess
  is a hypothesis, and the cheapest test of one is hiding the suspect and shooting again** — one
  screenshot with the marker's `visible` set to false would have refuted this a fortnight ago.
- **A `fillOpacity`-off-live-scene probe drifts across the intro auto-rotate. Kill the rotate
  with `reducedMotion: 'reduce'` on the browser context.** The subway slice's probe read troika
  `fillOpacity` after a 4.5 s settle, and the assumption was that a 1.4 s auto-rotate wave that
  ended three seconds before the sample could not be the noise source. It was: three back-to-back
  runs of the same three composite fixtures at three viewports drifted by up to two labels per
  viewport (toaster phone 5 vs 7 vs 5 across runs). `MetaphorIntro` reads
  `matchMedia('(prefers-reduced-motion: reduce)').matches` and skips the whole intro when it is
  true, and playwright's `browser.newContext({ reducedMotion: 'reduce' })` is what sets that
  media — one line, and three consecutive runs match to the label. Adding a longer wait was
  measured and did not help (6.5 s and 8 s ran the same drift), because the drift is from the
  declutter pass having stabilised on a not-quite-final camera position, not from opacity easing.
- **The probe in `apps/web/.claude/skills/verify/SKILL.md` still lies, and picking the camera by
  closest `aspect` does not save it.** Tried this run: projecting the accented label's own geometry
  bounding box through that camera put 4 of 12 cases off the top of the canvas, with plausible
  numbers for the rest — the same shape of failure the skill already warns about. What did work,
  and needs no camera at all, is diffing the frame against itself with one object hidden: the
  bounding box of `{ name visible } != { name hidden }` IS the name's screen box, and
  `{ marker visible } != { marker hidden }` inside it is exactly what the marker costs the name.
  Two traps in that technique, both paid for here. A **pixel-count** metric is not comparable
  across a change that alters the label's chip opacity — a more opaque chip swallows the glyphs'
  white outline and halves the measured "ink" while improving legibility, which reads as a
  regression. And **hiding a label lets the declutter pass reveal a neighbour**, which grows the
  diff box until it encloses something else; the accented label being pinned makes this worse, not
  better. Where those bite, count glyphs off the crops — which is what decided this slice, and what
  the previous one did too.
