# Ledger: `canvas-graph-edit`

Durable memory for the canvas graph-edit feature automation. Read the last three rows before
starting.

## Locked

| Date       | Decision                                                                                                                                                                                                                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-30 | Playbook created. This work ran as an unversioned Cursor automation (`cursor/delivery-content-graph-editing-*`, `cursor/metaphor-*-graph-edit`, `cursor/infographic-chart-class-graph-edit`) from ~2026-08-22 and shipped all 28 families; its prompt was never in the repo.          |
| 2026-08-30 | **`DiagramCanvas.jsx` is out of `allowedPaths`.** It is on the ratchet at 1889 lines and a family slice must not grow it — extend `diagramGraphEditNodeResolve.js`, which was extracted from it for this reason (issue #363). A slice that cannot avoid the canvas files an issue.    |
| 2026-08-30 | **Four verbs, no fifth.** Reparent, multi-select and drag-to-reorder are ruled out by the recipe, not merely unimplemented.                                                                                                                                                           |
| 2026-08-30 | `useFlowchartGraphEdit` → `useCanvasGraphEdit` is a **dedicated cleanup slice**, never folded into a family slice. `docs/canvas-graph-edit.md` § Traps says so; it is a pure rename and mixing it with logic hides both.                                                              |
| 2026-08-30 | `GRAPH_EDIT_BLAST_TESTS` added to `scripts/test-affected-lib.mjs` the same day. Before it, editing one family's mutator selected that family's test and nothing else — and the 2026-08-28/29 colon-less-ER defect crossed the shared `pickParallelEdgeRef` seam between two families. |

## Run log

Append one row per firing, including quiet runs.

| Date       | Slice                              | Family                                                      | Tests before → after                                                                                | PR   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ---------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-30 | `rename-edge-non-mermaid`          | Metaphor3D city                                             | 11 → 13 (`metaphorCityEdit.test.js`)                                                                | #459 | Backlog first: #441 was already fixed on `main` (commit `c24a269`, before this firing) — closed as already resolved rather than re-fixed. Then implemented `renameCityEdge`: finds the `{from,to}` link (unique by construction — `connectCityNodes` already refuses duplicates, so no parallel-edge disambiguation needed) and sets/clears `MetaphorLinkSchema`'s optional `label`. Kept the find-link helper private to `metaphorCityEdit.js` rather than adding it to `metaphorFlatItemsCore.js`, which is **not** in this playbook's `allowedPaths` (`metaphor*Edit.js` doesn't match `metaphorFlatItemsCore.js`) — `npm run routine:guard` would have rejected the diff. Garden, chart, composite, and the `canLink: true` flat kinds (layercake/galaxy/machine/terrain, sharing one factory in `metaphorFlatKindEdit.js`) are still `fail('not-graph')` stubs — left for follow-up slices; see the updated `rename-edge-non-mermaid` todo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-31 | `rename-edge-non-mermaid-flatkind` | Metaphor3D flat kinds (layercake/galaxy/machine/terrain)    | 12 → 15 (`metaphorFlatKindEdit.test.js`)                                                            | #470 | Backlog checked first (`list_issues`, open): none names a graph-edit family, `docs/canvas-graph-edit.md`, or a mutator, so the queue order stood. Implemented `renameEdge` in `createMetaphorFlatGraphEdit` (`metaphorFlatKindEdit.js`), gated on `canLink` exactly like `connect`/`deleteEdge`: `!canLink` keeps the existing `fail('not-graph')` stub (garden-style Link-off kinds have no free edge — do not "fix" those, per the existing Locked note), `canLink` kinds find the `{from,to}` link and set/clear its `label`, same shape as `renameCityEdge`. Kept a private `findFlatLink` helper in `metaphorFlatKindEdit.js` rather than `metaphorFlatItemsCore.js` (same `allowedPaths` glob miss as last time — see Open observations). Composite's `renameEdge` stub is untouched — split into its own todo (`rename-edge-composite`) since it is a different mutator file/family, not part of "layercake/galaxy/machine/terrain share one factory". Updated the family note in `docs/canvas-graph-edit.md` § Traps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-09-01 | `issue-472-stale-tracker`          | docs only (no family)                                       | unchanged                                                                                           | #496 | Backlog first (`list_issues`, open, 14 results): #476 names a mutator but its only remaining part (item 2, widening `allowedPaths`) is playbook frontmatter — off-limits to any routine but `improve` per README rule 9, so left for it. #472 names `docs/canvas-graph-edit.md` directly: line 153's two `[#463]` links point at an issue closed by _documenting_ the Metaphor3D link-descriptor gap rather than fixing it, so the doc pointed a future reader at a dead end. Filed #495 as the live tracker for the actual descriptor work (3D link hit-testing so `renameEdge`/`deleteEdge` become reachable — likely `metaphor3d` automation's queue, not this one's, since it's scene/hit-test work outside this playbook's `allowedPaths`) and repointed both `docs/canvas-graph-edit.md:153` links at it. `npm run check` green (docs-only diff, no code touched). **Also found, not taken**: #402 (`renameFlowchartEdge`/`renameStateEdge`/`renameSequenceEdge` crossing `max-params`) names three mutators inside `allowedPaths`, but its fix requires updating the shared hook call site — this playbook's `allowedPaths` lists `useFlowchartGraphEdit.js` under `apps/web/src/hooks/`, while the file has lived at `apps/web/src/features/canvas/useFlowchartGraphEdit.js` since before this playbook existed (confirmed via `find`; the `hooks/` variant has no such file). `routine-guard` matches the literal path, so touching the real file is `blocked-by-paths`: the glob needs to move to the `features/canvas/` directory (and, ahead of the pending `hook-rename` slice, `useCanvasGraphEdit.js` at the same directory) before #402 is takeable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-09-02 | `rename-edge-composite`            | Metaphor3D composite                                        | 19 → 21 (`metaphorCompositeEdit.test.js`)                                                           | #508 | Backlog first (`list_issues`, open, 16 results): none names a graph-edit family, `docs/canvas-graph-edit.md`, or a mutator that this automation can act on — #476 (the only mutator-naming issue) is still blocked on `improve` for its item 2 (playbook `allowedPaths` widen), unchanged since 2026-08-31, so left for it again. Queue order stood: took the remaining half of `rename-edge-non-mermaid` (queue item 2), the `rename-edge-composite` todo split out 2026-08-31. Implemented `renameCompositeEdge(source, fromId, toId, label)` in `metaphorCompositeEdit.js`: parses the composite doc, then calls the already-shared `renameLinkedEdge`/`findLinkedEdge` from `metaphorFlatItemsCore.js` (added to composite's existing import from that module — no new file needed, unlike city/flatkind which had to keep private copies because `metaphorFlatItemsCore.js` isn't in `allowedPaths`; composite already imported from it for `appendLink`/`hasDirectedLink`/`purgeLinksForNode`, so reusing the shared helper here was in-budget and adds no duplication for #476 to complain about). Composite links are top-level, never layer-scoped, so this is _not_ a per-layer delegate like Add/Delete/Rename-node — confirmed `serializeMetaphorFlatDoc` (called inside `renameLinkedEdge`) and `serializeCompositeDoc` are byte-identical (`JSON.stringify(doc, null, 2)` + trailing-newline preservation), so passing a composite doc through the flat-doc serializer is safe. Updated the stale test at `metaphorCompositeEdit.test.js` (was asserting the old always-`not-graph` stub) to cover set/clear/missing-link/invalid-doc. Updated `docs/canvas-graph-edit.md` (composite bullet, city/flatkind rename-edge bullet, family list item 14) to drop composite from the "still stub `renameEdge`" list. `npx vitest run Edit --root apps/web` (the `GRAPH_EDIT_BLAST_TESTS` set): 316/316 green. `npm run check`: green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-09-03 | none — filed #523 instead          | Mermaid pie/gantt/journey                                   | unchanged                                                                                           | none | Backlog first (`list_issues`, open, 18 results): #510 names `useFlowchartGraphEdit.js` but the real file lives at `apps/web/src/features/canvas/useFlowchartGraphEdit.js`, not the `apps/web/src/hooks/` path this playbook's `allowedPaths` globs — same `blocked-by-paths` wall as #402 (see 2026-09-01 row); left for `improve`. #476's remaining part (allowedPaths widen) still `improve`'s. #495 still `metaphor3d`'s. Nothing else names this automation's scope. Fell through to queue item 4 (`gantt-journey` todo, "cheapest remaining"). Before writing a mutator, rendered actual `journey`/`gantt`/`pie` SVGs through the pinned `mermaid@^11.17.2` in a vitest+jsdom harness mirroring `renderMermaidPreview.js`'s `mermaid.render` call, to confirm the DOM hit identity each family needs (recipe step 0: "you know the hit identity the renderer stamps"). Finding: gantt bars get real `id`s but `class="task taskN"`; journey wraps each task in an unclassed, unid'd `<g>`; **pie — already marked shipped in the family table — has neither**, just `<path class="pieCircle">` with no `id` and no `node`/`timeline-node`/`cluster` class. `DiagramCanvas.jsx`'s `resolveTargetUnder` (`nodeEl = target.closest('g.node') ?? target.closest('g.timeline-node')`) and five sibling files that repeat the same selector list never match any of the three, so a canvas click never resolves a descriptor for a pie slice today — Add/Delete/Rename are unit-tested and adapter-registered but unreachable from the actual canvas, not merely untested. Filed [#523](https://github.com/acuhlmann/mermaid-gen/issues/523) with the repro and the full list of six call sites that would need to move together, rather than build gantt/journey mutators on the same broken foundation or hand-roll a DiagramCanvas.jsx fix — that file (plus `visibleDiagramLabels.js`, `embeddedDiagramFocus.js`, `focusDiagramHighlightIds.js`, `applyDiagramHighlightToSvg.js`) is out of this playbook's `allowedPaths` by the "Two hard constraints" rule, and `npm run routine:guard -- --reachable apps/web/src/components/DiagramCanvas.jsx` confirms `deps, improve, resolve, review` can take it, not this automation. `npm run check`: not run (no code changed; docs-only ledger update). Probe test file (`apps/web/test/zzz-render-probe.test.js`) was scratch-only and deleted before this commit — never landed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-09-04 | none — filed #536 instead          | Metaphor3D composite                                        | unchanged                                                                                           | TBD  | Backlog first (`list_issues`, open, 20 results): #528 names `GRAPH_EDIT_BLAST_TESTS` but only inside `scripts/test-affected.test.mjs`/`scripts/test-affected-lib.mjs`/`docs/agents/ratchet.json`, none in `allowedPaths` — `improve`'s. #510 and the `hook-rename` todo are still `blocked-by-paths`: `improve`'s PR [#531](https://github.com/acuhlmann/mermaid-gen/pull/531) (widening this playbook's `allowedPaths` to the real `apps/web/src/features/canvas/` hook location plus `metaphorFlatItemsCore.js`) is open but not yet merged, so the stale-glob wall from the 2026-09-01/09-03 rows still stands — re-check next firing. #523 (pie/gantt/journey) and #495 (Metaphor3D link hit-testing) unchanged, both still blocked on `DiagramCanvas.jsx`/scene-bridge work outside this automation. Nothing else in the backlog names this scope. Fell through to the `composite-layer-add` todo ("bigger than one slice — file an issue first"): read `metaphorCompositeEdit.js` end to end and confirmed the gap is real, not stale — `addLinkedCompositeNode` (`:215-237`) only ever resolves its target layer via `findLayerForItem` (`:111-119`), which requires an existing item id to anchor to, so a layer of a kind not already present in the scene can never be introduced; `deleteCompositeNode` (`:243-274`) has no layer-count guard, so deleting a layer's last item leaves a `{ as, items: [] }` husk in `layers[]` rather than removing the entry. Filed [#536](https://github.com/acuhlmann/mermaid-gen/issues/536) with the exact call sites and a two-mutator shape (`addCompositeLayer`/`removeCompositeLayer`), labelled `ready-for-agent` since both proposed mutators land in files already in this playbook's `allowedPaths` (`metaphor*Edit.js` glob covers `metaphorCompositeEdit.js`; `MetaphorGraphEditBridge.jsx` is also listed) — unlike #495/#523, no `DiagramCanvas.jsx` change looks required for the mutators themselves, only for the canvas-side affordance that triggers them, which the issue flags as a possible split. `npm run check`: not run (no code changed; docs-only ledger update).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-09-05 | `issue-510-edge-disambiguation`    | Flowchart / state / sequence (hook boundary, no new family) | 3050 → 3050 (`useFlowchartGraphEdit.test.jsx` + 3 mutator suites, same counts, reworded assertions) | #557 | Preflight clean, no open PR. `improve`'s [#531](https://github.com/acuhlmann/mermaid-gen/pull/531) merged 2026-09-05 (confirmed via `pull_request_read` and `routine:guard -- --reachable apps/web/src/features/canvas/useFlowchartGraphEdit.js` → now includes `canvas-graph-edit`), unblocking both `hook-rename` and #510 — re-opened `hook-rename` in Todos below. Backlog first (`list_issues`, open, 12 results): #510 names `useFlowchartGraphEdit.js:387` directly and is now reachable, so it jumped the queue ahead of `hook-rename` and `composite-layer-add` per rule 1. Took #510: `useFlowchartGraphEdit.js`'s `handleLabelCommit` built a union-of-two-dialects object for `adapter.renameEdge` (`{edgeLabel, edgeIndex, messageLabel: session.edgeLabel, messageId}`) while the `delete` branch next to it called `adapter.deleteEdge` positionally — same Data Clump the issue's own #402 was supposed to remove. Fixed by converting `renameFlowchartEdge`/`renameStateEdge`/`renameSequenceEdge` (the three mutators that actually implement edge rename) from a disambiguation-object 5th param to two positional params (`matchLabel`, `matchIndex`) — the same tail shape their sibling `delete*Edge` functions already used — so both hook call sites now share one convention: `(source, from, to, matchLabel, matchIndex)` for delete, `(source, from, to, newLabel, matchLabel, matchIndex)` for rename. Chose positional-parity over the issue's suggested `{label, index}` object shape because the object route would have cascaded into `pickParallelEdgeRef`'s and `findSequenceMessageRange`'s own signatures to avoid a silent regression on `deleteClassEdge`/`deleteErEdge` (which forward straight into `pickParallelEdgeRef` from their own positional params) — that's 8 source files + 7 tests, over this playbook's `maxFiles: 10`; the positional fix reaches the same "one convention" outcome in 4 source files (the hook + the 3 mutators) with zero change to `mermaidEdgeDisambiguation.js`, `mermaidSourceLocate.js`, `mermaidClassEdit.js`, or `mermaidErEdit.js` (their `deleteEdge`/rename-stub contracts are untouched and still correct). `flowchartEdgeDisambiguation()` in the hook now returns `{matchLabel, matchIndex}` (merging the flowchart-index/sequence-messageId parse via `??`, same precedent already used at the old delete call site) instead of three differently-named keys. Updated the 4 in-file test expectations that referenced the old `{edgeLabel, edgeIndex}`/`{messageLabel, messageId}` object args or the old `labelSession.edgeLabel`/`edgeIndex`/`messageId` field names. `npx vitest run Edit` (`GRAPH_EDIT_BLAST_TESTS`, run from `apps/web`): 350/350 green. `npm run check`: green (268 web test files / 3050 tests, 57 server tests, typecheck/strict/lint/dep-cruiser/wire/doc-paths clean). `npm run routine:guard -- --postflight canvas-graph-edit`: clean, 9/10 files. Closes #510.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-09-06 | none — re-scoped three todos       | n/a (re-check only)                                         | 368/368 (`npx vitest run Edit` from `apps/web`, GRAPH_EDIT_BLAST_TESTS)                             | none | Preflight clean, no open PR. A great deal landed on `main` since the 09-05 row (dozens of merges outside this automation — human-driven `26090x-fix-*` branches, `review`, `deps`, `office-life`), so re-checked every open todo against the current tree rather than trusting last firing's framing. Backlog (`list_issues`, open, 3 results): #452 is the digest log (not this automation's), #547 (`improve`'s complexity fold, folding #431/#447/#465/#478) still names `infographicGraphEdit.js`/`mermaidClassEdit.js`/`mermaidSourceLocate.js` inside this playbook's `allowedPaths`, but per the 2026-09-04 precedent for its predecessor #447 ("filed by `improve`'s own audit, defers to a future `improve` run") this stays `improve`'s — a lint/complexity-debt queue is not a graph-edit family slice, and `improve`'s own PRs today (#569–#583) are already working it. **`hook-rename` → newly found `blocked-by-paths`, not pending**: `apps/web/src/features/session/useCanvasInteractionRuntime.js` imports `useFlowchartGraphEdit` directly (`from '../canvas/useFlowchartGraphEdit.js'`) and calls it — the only real call site besides the hook's own test — and that file is not in this playbook's `allowedPaths` (it is not one of the two literal `apps/web/src/features/canvas/use{Flowchart,Canvas}GraphEdit.js` entries `improve`'s #531 fixed). A pure rename cannot avoid updating its one caller's import path and identifier, so the slice is blocked until `improve` adds `useCanvasInteractionRuntime.js` (or widens to the whole `features/session/` directory) to `allowedPaths`. **`gantt-journey` → re-confirmed still blocked, for a different reason than last framed**: [#523](https://github.com/acuhlmann/mermaid-gen/issues/523) (pie's `g.node`-only hit-test) closed 2026-09-05 via `stampPieSliceHitTargets` (`apps/web/src/utils/mermaidPieHitTargets.js`, wired from `renderMermaidPreview.js`, touching `DiagramCanvas.jsx`) — that commit's own message says "gantt and journey have the same shape as pie did; extending this stamp is now the documented route for them." But `mermaidPieHitTargets.js` and `renderMermaidPreview.js` are **not** in this playbook's `allowedPaths` either (only `mermaid*Edit.js`, `mermaidEdgeDisambiguation.js`, and `mermaidSourceLocate.js` are), and `DiagramCanvas.jsx` stays deliberately excluded — so building a `mermaidGanttEdit.js`/`mermaidJourneyEdit.js` mutator alone would still ship the same unreachable-from-canvas dead code #523 was filed about, just for two new families instead of one. The conceptual blocker is gone; the budget blocker is not. **`composite-layer-add` → mutator half shipped, not by this automation**: [#536](https://github.com/acuhlmann/mermaid-gen/issues/536)'s `addCompositeLayer`/`removeCompositeLayer` landed in `metaphorCompositeEdit.js` via PR #577 (`260906-fix-536-composite-layer-mutators`, merged by the owner directly — branch/PR-title prefix match neither `claude/canvas-graph-edit` nor `canvas graph edit:`/`graph edit:`, so `routine-guard`'s open-PR check correctly never saw it as this automation's). 22 → 28 tests in `metaphorCompositeEdit.test.js`. The issue's own comments (2026-09-05, 2026-09-06) record the owner's decision on seeding (duplicate an existing layer) and placement (a pick payload on the layer's placard, in `metaphorScenes/` + `MetaphorGraphEditBridge.jsx`) and explicitly say the wiring half needs the Playwright/Chromium render harness this Linux sandbox has and the mutator-half author's Windows checkout didn't — but also that `RadialActionMenu.jsx` (needed for the "Add item to this layer / Remove this layer" menu entries) is reachable by `deps, improve, resolve, review` only, **not** `metaphor3d` or `canvas-graph-edit`. Read `MetaphorGraphEditBridge.jsx` in full: it bridges exactly two pick kinds today (item, via R3F pointer events; link, via `metaphorLinkPick.js`'s screen-space hit-test) and has no third "layer" kind, so a placard pick needs a new selection mechanism originating in a `metaphorScenes/` scene file this playbook cannot reach, not just a `MetaphorGraphEditBridge.jsx` edit — left for a slice that spans this automation, `metaphor3d`, and whichever rung can touch `RadialActionMenu.jsx`, not taken solo here. `npx vitest run Edit` (`apps/web`): 368/368 green, no regression from the day's churn. `npm run check`: not run (no code changed; docs-only ledger update). |

## Todos

| Id                                 | State             | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `rename-edge-non-mermaid-flatkind` | completed         | `renameEdge` on layercake/galaxy/machine/terrain (`canLink: true`, sharing `createMetaphorFlatGraphEdit` in `metaphorFlatKindEdit.js`) — done 2026-08-31. City done 2026-08-30. Garden and the Link-off flat kinds (orrery/river/archipelago/bridge/cycle/subway/iceberg) have no free edge to rename — their stub is structurally correct, not a bug; do not "fix" those.                                                                                                                                                                                                                                                                                                             |
| `rename-edge-composite`            | completed         | `renameEdge` on composite (`metaphorCompositeEdit.js`) — done 2026-09-02.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `hook-rename`                      | blocked-by-paths  | `useFlowchartGraphEdit` → `useCanvasGraphEdit`, whole-slice, pure rename. Re-blocked 2026-09-06: the hook's one real caller, `apps/web/src/features/session/useCanvasInteractionRuntime.js`, imports it directly and is not in `allowedPaths` — a rename cannot avoid updating that import. Needs `improve` to add that file (or `features/session/`) to `allowedPaths` before this is takeable again.                                                                                                                                                                                                                                                                                 |
| `gantt-journey`                    | blocked-by-paths  | [#523](https://github.com/acuhlmann/mermaid-gen/issues/523) (the conceptual blocker) closed 2026-09-05 — pie now has a stamped hit identity and the fix commit says gantt/journey should reuse the same stamp. But the stamp lives in `mermaidPieHitTargets.js`/`renderMermaidPreview.js`, neither in `allowedPaths`, and `DiagramCanvas.jsx` stays excluded — so the budget blocker survives the conceptual one. Needs `improve` to widen `allowedPaths` to the hit-identity files before a gantt/journey mutator would be reachable rather than dead code.                                                                                                                           |
| `composite-layer-add`              | blocked (partial) | Mutator half (`addCompositeLayer`/`removeCompositeLayer`) shipped 2026-09-06 via PR #577, outside this automation (owner-authored, not `canvas-graph-edit`'s branch/PR-title convention) — see the run-log row. Remaining half (a layer-placard pick payload wired through `metaphorScenes/` + `MetaphorGraphEditBridge.jsx` + `RadialActionMenu.jsx`'s menu entries) needs a scene file outside this playbook's reach and `RadialActionMenu.jsx`, which `routine:guard --reachable` gives to `deps, improve, resolve, review` only — not `metaphor3d` or `canvas-graph-edit`. Not takeable solo by this automation; needs either an `allowedPaths` widen or a cross-automation slice. |
| `issue-441-er-table`               | completed         | #441 — already fixed on `main` before this firing (commit `c24a269`); closed with a comment 2026-08-30, no code change needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## Open observations

- `apps/web/src/utils/metaphorFlatItemsCore.js` is genuinely shared infrastructure for city/garden/the flat-kind factory, but it does **not** match this playbook's `allowedPaths` glob `apps/web/src/utils/metaphor*Edit.js` (it doesn't end in `Edit.js`). Any slice that wants a shared helper there needs the playbook's `allowedPaths` widened first, or (as done tonight) keep the helper private to the one mutator file that needs it.
- Issue #447 (three lint `complexity` warnings in graph-edit mutators, including `infographicGraphEdit.js` and `mermaidClassEdit.js`) was filed by the `improve` routine's own audit and its body explicitly defers to "a future `improve` run (or a human)" — left for `improve`, not taken as this automation's work.
- The stale `apps/web/src/hooks/` glob (found 2026-09-01, blocking #402 and #510) was fixed by `improve`'s #531 on 2026-09-05 — `allowedPaths` now names the hook's real `apps/web/src/features/canvas/` location. `hook-rename` hit a **different** wall on 2026-09-06: the hook's one caller outside its own test, `apps/web/src/features/session/useCanvasInteractionRuntime.js`, was never added — see that todo row.
- **The family table's "shipped" column means mutator + adapter + unit tests, not necessarily reachable-from-canvas.** Confirmed this the hard way for pie (#523) — the precedent already existed for Metaphor3D edges (#495) but this is the first time it's shown up on a **node**-level family, not just edge rename/delete. Before starting a mutator for any family whose table row doesn't explicitly say "canvas resolves clicks via …" (compare timeline's row, which does), render the diagram type through the pinned mermaid/AntV version first and check its DOM against the six `g.node`/`g.timeline-node`/`g.cluster` call sites listed in #523 — don't trust the table's "shipped" alone.
- **`improve`'s [#531](https://github.com/acuhlmann/mermaid-gen/pull/531) is the fix for the stale `apps/web/src/hooks/` glob** (both instances) plus adding `apps/web/src/utils/metaphorFlatItemsCore.js` to `allowedPaths` (closing #476 item 2). Open, not merged, as of 2026-09-04 — it is `improve`'s PR to drive to green (README rule 9: playbook/budget edits belong to `improve`), not this automation's. Once it merges, re-open `hook-rename` and re-check #402/#510 for reachability.
