{
  "updated": "2026-09-06",
  "$comment": "2026-09-05 (improve, Saturday — preempted by ratchet drift per README rule 2 (\"they only become the slice when they turn up something that moved the wrong way\"), so item 6 (doc drift) was not reached): register accuracy (item 1) corrected a stale DiagramCanvas.jsx figure (1889, already dropped to 1867 by an earlier merge) in CLAUDE.md/AGENTS.md/balanced-coupling-priorities.md, and refreshed balanced-coupling-priorities.md's Last-reviewed date (had read 2026-08-28 despite three prior firings' rows each claiming to refresh it -- a register-accuracy miss on this routine's own bookkeeping, not re-investigated further). Ratchet drift (item 2, --with-lint) found three violations. (1-2) contextBytes grew on both root files (CLAUDE.md 38786->39157, AGENTS.md 42117->42284): traced to a legitimate addition, not bloat -- the office-life automation registering itself in the night-ladder job count and table (both root files, per README rule 8's duplication mandate for repo-wide findings). Raised both budgets to match rather than trimming real content. (3) apps/web lintWarnings grew 836->850 from a brand-new file, apps/web/test/officeVisitTrace.mjs (the office-life automation's visit-harness instrument, landed same-day): 12 of its 13 warnings (10 no-undef on `process`, 1 complexity, 1 max-lines-per-function) traced to a genuine packages/eslint-config/index.js gap -- the test-file glob covered .js/.jsx/.ts/.tsx but not .mjs/.cjs, so a Node script placed under apps/web/test/ was linted as browser application code instead of getting the same node-globals + relaxed-threshold treatment every other test file gets. Fixed the glob (widened to include mjs/cjs) rather than moving the file. The 13th (a dead `let entries = []` initializer, unconditionally overwritten or abandoned on the catch path before any read) fixed directly in officeVisitTrace.mjs. That left apps/web at 837, still 1 over the 836 budget: a fresh, unrelated regression reintroducing the exact three renameFlowchartEdge/renameStateEdge/renameSequenceEdge max-params violations #402 already fixed once (PR #503) -- reintroduced by 5cec03b/#510/#555 (\"unify renameEdge/deleteEdge disambiguation at the hook boundary\"), which reshaped the signature back to a flat 6-param form to match delete*Edge's convention. `routine:guard --reachable` confirms the shared call site, apps/web/src/features/canvas/useFlowchartGraphEdit.js, is still outside improve's allowedPaths -- same blocker as the original issue -- so reopened #402 with a comment (rather than filing fresh; improve's maxIssues:1/24h was already spent today on #547, filed by an earlier same-day firing) instead of attempting an unsafe partial fix across only the reachable files. Budget kept at 836, no silent loosening. monolithLoc/suite: DiagramCanvas.jsx improved 1889->1867 (upstream, unrelated commit) and suite grew the right way (398->399 files, 4236->4317 cases) -- both tightened to lock in. 2b (ownership gaps): resolved office-life's ledger `visit-harness-home` blocked-by-paths row -- pointed office-life.md §2's prose at the harness's actual, already-reachable home (apps/web/test/office*) instead of widening allowedPaths to scripts/, since no file move was needed. Left the other three office-life blocked-by-paths rows (`ratchet-office-monoliths`, `officefloor-suite-blast-wall`, `app-css-chrome`) as ledger todos for a future firing -- each is a larger, separate slice of its own. `npm run check` green; `routine:guard --postflight improve` OK.  2026-09-02 (improve, Wednesday rotation — preempted by the improve-only backlog per README rule 2b): register accuracy (item 1) and ratchet drift (item 2, --with-lint) found nothing to correct — all 9 tracked monoliths at budget, apps/server's 224/222 lintWarnings gap unchanged (#499's known remainder, outside this playbook's allowedPaths). The queued Wednesday item-7 candidate (#500, fusedCompositePlanner.js LOC) was already closed upstream by the time this firing ran, so took #462 and #473 instead — both reachable by improve alone. Fixed #462 (METAPHOR_BLAST_TESTS missing 4 suites: metaphorDrawOrder, metaphorFusedRoutes, metaphorGroupPlacards, metaphorLabelDepth) and #473 (the strict-island sensor unreachable from either file that can cause its drift) in scripts/test-affected-lib.mjs, plus the ledger's own sensor-floor-inverse todo (ISOMETRIC_FLOOR_BLAST_TESTS had the identical gap, 10 officeFloor* suites unlisted) found in the same sweep — generalised the reverse-direction guard in scripts/test-affected.test.mjs to cover all three bundles. Deliberate negative case for both fixes: reverted, watched the new tests go red, restored, confirmed green. Landed as PR #515. contextBytes ticked down from the merge itself (CLAUDE.md 38788->38786, AGENTS.md 42119->42117) and suite.cases grew from the two new reverse-direction tests (4223->4236) — both genuine improvements, tightened to lock in.  2026-09-01 (improve, Tuesday rotation — preempted by ratchet drift + a backlog item that pointed only at improve): register accuracy (item 1) already current, nothing to correct. Ratchet drift (item 2, --with-lint) found four fresh violations — all four already traced and filed by the `review` routine as #499 (contextBytes on both CLAUDE.md/AGENTS.md, +2 apps/server lintWarnings, +1 apps/web lintWarnings) before this firing started, so this run worked #499 rather than re-deriving it. Of #499's three causes, two are improve's per `routine:guard --reachable` (apps/server/src/loadServerApp.js's dead code is deps/resolve/review's, not reachable from this playbook's allowedPaths — left untouched, tracked in the ledger). Fixed: (a) `routePoint` in fusedCompositePlanner.js crossed max-params (6) via #488 — grouped the trailing four call-scoped params into one options object (mechanical, single in-file call site, no logic change); verified against the file's own geometry-sensitive test suite (fusedCompositePlanner.test.js, metaphorGroupIdentity.test.js, metaphorFusedRoutes.test.js — 49/49 green, including tests that assert actual computed station/clearance positions, which is a stronger guarantee against a silent behaviour change here than a render screenshot) rather than the file's own render-and-measure-verification step, since a pure parameter-grouping refactor changes no computed value. apps/web dropped 838→837, still 1 over its 836 budget — the remainder is #465's two Metaphor3D complexity warnings plus #478's one, all three still open and unchanged, budget kept at 836 (no silent loosening). (b) contextBytes: CLAUDE.md (42789/39582) and AGENTS.md (46011/42497) both regressed because two owner commits (fe6384c, 129a997 — ADR-0017 rollout prose) mirrored the same new content into both root files per README rule 8's own duplication mandate for repo-wide findings. Traced the section against docs/routines/README.md and found most of the newly-added bullets (budget-ownership, ready-for-human, ready-for-agent-reachability) were already stated there near-verbatim — not scoped content like the Metaphor3D/Office split (nothing routine-specific to move to a domain file), just duplication of the routines shelf's own contract doc. Replaced the restated rules with one pointer to docs/routines/README.md and kept only what CLAUDE.md/AGENTS.md carry that the contract doc does not (the ratchet-gates-no-build note, ADR-0016 self-merge powers, the night-ladder table pointer, the host-split fact, the one-fleet-per-window rule) in both files identically, same as before. CLAUDE.md 42789→38788, AGENTS.md 46011→42119, both back under budget — tightened to lock in. Also resolved #498 (docs/routines/deps.md, reachable by improve alone — the backlog-that-points-at-you rule, taken ahead of the Tuesday TypeScript-leaves rotation): #498 asked whether three `deps:`-titled PRs that touched package-lock.json and shelf docs were the `deps` routine breaching its own guard, or needed a §3b carve-out. `git log` shows all four commits in that window (573af32, 0c08b90, 4c96e67, 9858652) are authored directly by the repo owner (acuhlmann@gmail.com, no Claude-Session trailer), landing the same day 129a997 records the `deps` cron trigger being created — i.e. owner bootstrap work standing up the deps.md playbook and doing the first cleanup pass before any unattended firing existed, not a routine run. The routine's own first live firing (`ledger/deps.md`'s 2026-09-01 16:30Z row) correctly declined a lockfile-requiring fix (the uuid/copilotkit pin) as always-forbidden — so the guard was never actually breached by an unattended run. Recorded that finding in `docs/routines/ledger/deps.md` and left §3b's hard \"never\" on package-lock.json unchanged (no carve-out; the finding was a misattribution, not a gap). suite.files/cases tightened to their measured 398/4223 (genuine improvement, no new dependency). Did not reach the Tuesday rotation item (5, TypeScript leaves) or items 3/4/6/7/8 — ratchet drift plus the improve-only backlog item filled the slice, per this file's own \"they only become the slice when they turn up something that moved the wrong way\" rule; #500 (fusedCompositePlanner.js LOC growth, a coupling-split candidate) left for a Wednesday item-7 firing rather than compounded with today's max-params edit to the same file.  2026-08-31 (improve, Monday rotation): register accuracy (item 1) was already current. Ratchet drift (item 2, --with-lint) found apps/web at 838, two over the 836 budget: #465's two Metaphor3D complexity warnings re-confirmed still open, plus one new one \u2014 MetaphorSceneChrome.jsx's ItemLabel crossed complexity (16 > 12) via c2f4cd5 (#468/#477, the accent draw-order fix) \u2014 filed as #478 rather than fixed in place, same render-and-measure-verification concern already established for #422/#431/#465 in this file family; budget kept at 836 (no silent loosening, both #465/#478 stay ready-for-human). Picked up the ledger's officefloorboard-useless-escape todo in the same run (pre-verified, mechanical no-useless-escape fix) \u2014 apps/web measures 837 after that fix, still 1 over budget from #465/#478's unfixed remainder. suite.files/cases tightened to their measured 396/4194 (a genuine improvement, no new dependency). Item 3 (test hardening, today's weekday slice) added companion non-empty/exact-count assertions ahead of the `expect(new Set(x).size).toBe(x.length)` uniqueness idiom in 5 test files (6 instances) that lacked one, closing a systemic vacuous-pass gap review.md \u00a72's trap checklist names explicitly; no lintWarnings/monolithLoc metric moves from this part, only the officeFloorBoard fix above does.  monolithLoc.diagramStore.js tightened 955\u2192945 and lintWarnings.apps/web tightened 846\u2192836, suite.files/cases tightened to their measured 392/4122 \u2014 all genuine improvements, need no written reason. The lint drop came from 6 dead-code fixes, each verified genuinely unreferenced before deletion: 8 imports in diagramStore.js (clearBrowserBackupSessionId, clearAllArchislopAppStorage, clearAllDiagramCachesFromStorage, clearSessionScopedArchislopStorage, isDiagramCacheSubstantial, PERSISTENT_ARCHISLOP_STORAGE_KEYS, readDiagramCache, writeDiagramCache) that were already independently re-exported via separate `export {...} from` statements \u2014 same dead-weight-import pattern as the 2026-08-25 firing's mermaidLangChainAgent.js fix; a duplicate, unused `unionSvgBBox` in embeddedDiagramFocus.js (a live copy already exists and is used in focusDiagramHighlightIds.js); one dead import each in exportDiagram.js (`MERMAID_EXPORT_MAX_WIDTH_PX`, same import-vs-re-export pattern) and metaphorLayouts/subwayNetworkLayout.js (`hash01Salted`, never referenced at all); a dead local `row` in metaphorLayouts/cityDistrictLayout.js (computed, never read \u2014 predates this firing, from the 2026-08-24 creation commit, so an old miss rather than fresh drift); and an unused `eventIndex` in the brand-new mermaidTimelineEdit.js plus a `no-useless-escape` on `\\|` inside mermaidErEdit.js's character class (both genuinely new, from this run's own graph-edit-family commits). A seventh, equally safe fix (a stray `\\{` in officeFloorBoard.js's character class, pre-existing since 2026-08-24) was found but left unfixed this run purely to stay inside maxFiles: 10 \u2014 noted as a ledger todo, not filed as an issue, since it needs no more than the one-line mechanical fix already scoped. #402's three max-params instances and #431's two complexity instances (assignLabelRanks, labelDeclutter.js) were both re-confirmed still open and unchanged \u2014 both stay ready-for-human per their own render-and-measure-verification concern. Also newly surfaced but NOT fixed this run (file budget spent; see ledger): infographicGraphEdit.js's `parseCompareForest` (complexity 15, new today from the timeline/pie/compare feature), mermaidClassEdit.js's line-307 arrow function (complexity 13, unflagged since the 2026-08-27 labeled-class fix), and mermaidSourceLocate.js's `peekDiagramDirective` (complexity 17, grown incrementally as this week's erDiagram/timeline/pie additions each added another dispatch branch) \u2014 filed as a fresh issue, tracked as a ledger todo.  2026-08-30 (fleet tune-up, not a routine run): added the contextBytes family. CLAUDE.md and AGENTS.md are read in full at the start of every session by every agent here, so a byte in either is paid by every interactive session and all seven nightly unattended runs before any work starts. They had gone 14 KB -> 136 KB and 12 KB -> 88 KB since May, doubling in the last fortnight, because docs/routines/README.md rule 8 required every durable finding in both. The Metaphor3D and Office findings moved to docs/agents/domains/, reached by a glob-scoped .cursor/rules/*.mdc, a nested CLAUDE.md, and the AGENTS.md domain index -- scoped, not deleted, and deliberately NOT counted here, because those files load only for a session touching the code they describe. Root pair 223 KB -> 74 KB. Rule 8 now says once, in the domain file; repo-wide findings still go in both. suite cases 4122 -> 4143 from the new routine-guard preflight and blast-radius tests; budget tightened to lock it in.  2026-08-30 (improve, Sunday rotation): register accuracy (item 1) was already current \u2014 no CLAUDE.md/balanced-coupling-priorities.md drift. Ratchet drift (item 2, --with-lint) found apps/web at 837, one over the 836 budget: traced via a throwaway worktree at the commit that set that budget, both new warnings came from #457 (machineGearLayout/subwayNetworkLayout complexity in the Metaphor3D layout family) \u2014 filed as #465 rather than fixed in place, same render-and-measure-verification concern already established for #422/#431 in this file family; budget kept at 836 (no silent loosening). suite.files/cases tightened to their measured 394/4169 (a genuine improvement from item 4's new sensor test file, five cases). Item 2 alone would have been another `improve` firing that never reaches items 3\u20138 \u2014 exactly the failure this file's own rotation-vs-priority-order note describes \u2014 so today (Sunday) also took item 4 (sensor gaps), the first time in ten firings items 3\u20138 have been touched: added scripts/verify-strict-islands.mjs + .test.mjs asserting SERVER_STRICT_ISLAND_FILES (packages/eslint-config/typeCheckedIsland.js) matches apps/server/tsconfig.strict.json's `include` list, with a deliberate negative case (added a bogus entry, confirmed the sensor fails with a clear diff, reverted). The two currently agree (22 files each); the sensor's job is only to catch future drift. sensor-guidance-sync (formatter.cjs GUIDANCE vs guidance.js) and the other pending sensor-gap todos are left for a future Sunday/Thursday firing, one per run per this file's own item 4 rule.",
  "metrics": {
    "monolithLoc": {
      "apps/web/src/ArchiSlop.jsx": {
        "budget": 1036,
        "initial": 1036
      },
      "apps/web/src/components/InsightsPane.jsx": {
        "budget": 1835,
        "initial": 1835
      },
      "apps/web/src/components/DiagramCanvas.jsx": {
        "budget": 1867,
        "initial": 1968
      },
      "apps/web/src/components/RadialActionMenu.jsx": {
        "budget": 1164,
        "initial": 1164
      },
      "apps/web/src/state/diagramStore.js": {
        "budget": 945,
        "initial": 955
      },
      "apps/server/src/mcp/mcpServer.js": {
        "budget": 1406,
        "initial": 1406
      },
      "apps/server/src/agents/mermaidLangChainAgent.js": {
        "budget": 1156,
        "initial": 1159
      },
      "apps/server/src/agents/infographicLangChainAgent.js": {
        "budget": 846,
        "initial": 863
      },
      "apps/server/src/routes/copilot.ts": {
        "budget": 1194,
        "initial": 1270
      }
    },
    "contextBytes": {
      "CLAUDE.md": {
        "budget": 39157,
        "initial": 136170
      },
      "AGENTS.md": {
        "budget": 42284,
        "initial": 88461
      }
    },
    "lintWarnings": {
      "packages/shared": {
        "budget": 126,
        "initial": 126
      },
      "apps/server": {
        "budget": 221,
        "initial": 229
      },
      "apps/web": {
        "budget": 828,
        "initial": 851
      }
    },
    "strictIslandFiles": {
      "apps/web/tsconfig.strict.json": {
        "budget": 15,
        "initial": 15
      },
      "apps/server/tsconfig.strict.json": {
        "budget": 22,
        "initial": 22
      }
    },
    "suite": {
      "files": {
        "budget": 401,
        "initial": 366
      },
      "cases": {
        "budget": 4368,
        "initial": 3675
      }
    }
  }
}
