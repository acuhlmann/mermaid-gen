/** Shared pacing constants for coffee / battle set pieces (desk + floor). */

export const COFFEE_BREAK_DURATION_MS = 15_000;
export const COFFEE_LINE_PACE_MS = 2200;

export const BATTLE_LINE_PACE_MS = 1900;
/** A battle with narration off still needs an end; matches the floor set piece. */
export const BATTLE_SILENT_DURATION_MS = 18_000;
