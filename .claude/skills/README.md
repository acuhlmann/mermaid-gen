# Claude Code skills in this repo

**Canonical tree:** [`.cursor/skills/`](../.cursor/skills/) — edit Mermaid and other project skills there only.

The `mermaid` entry here is a symlink to `.cursor/skills/mermaid` so Claude Code and Cursor share one copy. CopilotKit skills from `npm run setup:skills` land in gitignored `.agents/`, not here.
