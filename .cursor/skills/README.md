# Cursor / project skills

Project-local agent skills (Mermaid diagram authoring, etc.). **Edit files here only** — [`.claude/skills/mermaid`](../.claude/skills/mermaid) symlinks to this tree for Claude Code.

CopilotKit skills from `npm run setup:skills` are installed separately under gitignored `.agents/`.
