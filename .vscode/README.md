# Shared VS Code / Cursor workspace files (committed):

# extensions.json, tasks.json, launch.example.json, settings.example.json

# Personal overrides (gitignored) — copy from *.example.json when needed:

.vscode/launch.json
.vscode/settings.json
.vscode/*.local.json
