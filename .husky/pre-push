#!/usr/bin/env sh
# Pre-push: diff-scoped verification (falls back to shared-only when no diff).
# Full CI parity: npm run check:full. Skip with `git push --no-verify`.
set -e

echo "→ check:affected (diff-scoped)"
npm run check:affected
