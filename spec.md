
can you extend this project so the user can generate Mermaid and Infographic content? Add a Diagram/Infographic toggle near the Model Fast/Quality toggle that allows the user to switch. If Diagram is set (default) then keep the app as is. If Infographic is set then instead of generating Mermaid code, generate Infographic as in:
https://github.com/antvis/Infographic

Adjust the backend and frontend eg code editor to handle this. Other functions like the action buttons etc should remain as is just that they either operate over a Mermaid or Infographic diagram.

Ask me questions about this idea and approach, critique it. 